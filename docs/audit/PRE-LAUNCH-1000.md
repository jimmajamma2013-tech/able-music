# ABLE — Pre-Launch Master Audit
**900 improvement points across 9 phases**
**Created: 2026-03-17**
**Visual (Phase 2) is already complete — see Wave system in git history**
**Work phases in master sequence order: 1 → 3 → 4 → 5 → 6 → 7 → 8 → 9 → 10**

> Each improvement is scored: Impact (1-5), Effort (1-5), Risk (L/M/H), Wave (execution order within phase).
> Start each phase by doing Wave 1 items first. Within a wave, do Impact 5 before Impact 4, etc.

---

## Phase 1 — Core Product Logic
*The campaign state machine, fan capture, CTA deduplication, gig mode, tier gates, localStorage, and analytics — if any of these are wrong, everything built on top is wrong.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | `computeProfileState()` (or equivalent auto-switch function) must return `pre-release` when `now < releaseDate`, `live` when `now < releaseDate + 14 days`, and `profile` otherwise — add unit-style console assertions in dev mode that verify all three boundaries are correct | V8 | 5 | 2 | M | 1 |
| 2 | `stateOverride` in `able_v3_profile` must take absolute precedence over auto-computed state — verify the conditional chain reads `if (profile.stateOverride) return profile.stateOverride` before any date arithmetic, not after | V8 | 5 | 1 | M | 1 |
| 3 | Gig mode expiry: `able_gig_expires` stores a Unix timestamp — on every profile load, read `parseInt(localStorage.getItem('able_gig_expires'))` and compare to `Date.now()`; if expired and `stateOverride === 'gig'`, clear `stateOverride` and recompute state from release date | V8 | 5 | 2 | M | 1 |
| 4 | Post-gig state: when gig expires, profile must return to the correct previous state (pre-release / live / profile per date arithmetic) — not hard-code to `profile`; test by setting a release date 3 days out, enabling gig mode, expiring it, and verifying the state is `pre-release` | ADM | 5 | 2 | M | 1 |
| 5 | `able_gig_expires` must be cleared in admin.html when artist manually disables gig mode early — check the gig toggle OFF handler writes `localStorage.removeItem('able_gig_expires')` | ADM | 4 | 1 | L | 1 |
| 6 | Fan capture deduplication: before pushing to `able_fans`, check `able_fans.find(f => f.email.toLowerCase() === input.toLowerCase())` — if found, show a different message ("You're already on the list") not the default success state | V8 | 5 | 2 | M | 1 |
| 7 | Fan cap enforcement: free tier hard cap is 100 fans — the fan capture form must be hidden (not just greyed) when `able_fans.length >= 100` AND `able_tier === 'free'`; currently the form may still be visible and silently fail | V8 | 5 | 2 | M | 1 |
| 8 | CTA deduplication: the dedupe rule (same URL cannot appear in both Hero CTAs and Quick Action pills) must be enforced in admin.html when saving — if a URL already exists in `profile.heroCTAs`, remove it from `profile.quickActions` before saving, and vice versa | ADM | 4 | 2 | M | 1 |
| 9 | `migrateWizardKey()` must run at the very top of `initAdmin()` inside `DOMContentLoaded` — before any `safeLS('able_v3_profile')` read — to prevent the case where an artist who completed `start.html` sees an empty admin because `able_profile` hasn't been merged yet | ADM | 5 | 1 | M | 1 |
| 10 | `able_v3_profile` schema: verify every field written by `start.html` (name, vibe, feel, accent, theme, ctaType, releaseDate, releaseTitle, stateOverride) is present and readable by `able-v8.html` and `admin.html` — schema drift between wizard output and profile reader is a silent corruption risk | ALL | 5 | 2 | M | 1 |
| 11 | Release date edge case: artist sets release date, then clears it — `profile.releaseDate` becomes `null` or `""` — the state computation must handle `!releaseDate` gracefully and return `'profile'` without throwing `Invalid Date` | V8 | 4 | 1 | L | 1 |
| 12 | Release date timezone: `new Date(profile.releaseDate)` parses ISO strings as UTC, not local time — an artist who sets release date as `2026-04-01` in BST (UTC+1) will see pre-release → live transition at 1am local, not midnight — document this and decide: store as ISO with time `T00:00:00` or store UTC and convert on display | V8 | 3 | 2 | L | 1 |
| 13 | Release date set in past on wizard: if artist imports a Spotify link for a release already out, `releaseDate` is in the past — profile must show `live` state (if < 14d past) or `profile` state (if ≥14d past), not `pre-release` | STR | 4 | 2 | M | 1 |
| 14 | `able_gig_expires` value: confirm it is stored as a Unix millisecond timestamp (from `Date.now() + 86400000`), not seconds — a seconds-based timestamp compared to `Date.now()` (ms) will never expire | ADM | 5 | 1 | M | 1 |
| 15 | Tier gate enforcement: `checkTierGate()` must be called on admin.html load and on `able_tier` change — verify it actually disables Pro/Label feature panels (not just hides the upgrade button) when `able_tier === 'free'` | ADM | 5 | 2 | M | 1 |
| 16 | Free tier fan cap: when `able_fans.length` reaches 100, `checkTierGate()` or equivalent must show the upgrade nudge — currently this may only fire on admin load, not reactively when a fan signs up | ADM | 4 | 2 | M | 2 |
| 17 | `able_tier` defaults: if `able_tier` key is absent from localStorage (new user, never set), all tier gate logic must treat it as `'free'` — verify no gate check reads `localStorage.getItem('able_tier') === 'artist'` which would return false for null, not just non-artist tiers | ADM | 4 | 1 | L | 2 |
| 18 | Analytics: `trackEvent()` (or `posthog.capture()`) must fire on every CTA tap — verify there is no race condition where the page navigation fires before the event is queued, particularly on iOS Safari where `beforeunload` is unreliable; use `sendBeacon` or fire event before `window.location` assignment | V8 | 4 | 2 | M | 2 |
| 19 | Analytics deduplication: rapid double-tap on a CTA button must not fire two `cta_tapped` events — add a 300ms debounce or a `data-tracking-fired` guard on the tap handler | V8 | 3 | 1 | L | 2 |
| 20 | `able_views` event: page view must only fire once per session, not on every reload — check the session guard: `sessionStorage.getItem('able_session_viewed')` must be set before `able_views.push()` and abort if already set | V8 | 3 | 1 | L | 2 |
| 21 | `able_views` isArtist flag: the view event schema includes `isArtist` — this must be set based on whether the profile owner is viewing their own page (detect via `?owner=true` param or a localStorage flag), not just always `false` | V8 | 4 | 2 | M | 2 |
| 22 | Source attribution: `?ref=` param must be captured on page load via `new URLSearchParams(location.search).get('ref')` and stored with every subsequent fan sign-up and CTA tap in that session — use `sessionStorage` to persist across scroll without re-reading the URL | V8 | 5 | 2 | M | 2 |
| 23 | UTM params: `utm_source`, `utm_medium`, `utm_campaign` must all be captured on landing and stored in `sessionStorage` — currently only `?src=` may be captured; full UTM capture is required for post-launch attribution analysis | V8 | 4 | 2 | M | 2 |
| 24 | `?ref=email-confirm` attribution: fan arriving from confirmation email must have `source: 'email-confirm'` written into their fan object in `able_fans` (not just their view event) — the click event from the email is the most valuable attribution data in the system | V8 | 5 | 2 | M | 2 |
| 25 | `able_clicks` schema: every click event must include `sessionId` — generate a `sessionId` as `crypto.randomUUID()` on first page load and store in `sessionStorage`; attach to all click and view events | V8 | 3 | 2 | L | 2 |
| 26 | First-visit state (artist): `able-v8.html` must detect when the visiting user is the profile owner (via `?owner=true` or `localStorage.getItem('able_v3_profile')` non-null) and show a different hero CTA: "This is your page — share it" not the fan-facing sign-up form | V8 | 5 | 3 | M | 2 |
| 27 | First-visit state (fan): a fan visiting for the first time (no `sessionStorage.getItem('able_fan_visited_{slug}')`) should not see the "You're already on the list" flow — the first-visit check must be isolated to email deduplication, not a blanket session flag | V8 | 3 | 1 | L | 2 |
| 28 | Admin first-run checklist: `able_dismissed_nudges` must include the first-run checklist dismissal key — verify the checklist auto-dismiss path (when all items complete) writes the correct key: `'first-run-checklist'` | ADM | 3 | 1 | L | 2 |
| 29 | `admin_visit_dates` pruning: the key stores last 60 ISO date strings — verify the pruning logic `visitDates.slice(-60)` runs on every admin load to prevent unbounded growth | ADM | 2 | 1 | L | 3 |
| 30 | `able_starred_fans` deprecation: CONTEXT.md marks this key as deprecated (migrate to Fan.isStarred on fan object) — the star toggle in admin.html must write to `fan.isStarred` in the `able_fans` array, not to the legacy `able_starred_fans` key | ADM | 3 | 2 | M | 3 |
| 31 | `able_shows` schema: each show must include an `id` field (generated as `crypto.randomUUID()` or `Date.now().toString()`) to allow individual show deletion and reordering without index-based mutation | ADM | 4 | 2 | M | 3 |
| 32 | Shows sort order: `able_shows` must be sorted by `date` ascending on render in both `admin.html` and `able-v8.html` — a show added out of order must appear at the correct position in the events list | V8 | 3 | 1 | L | 3 |
| 33 | `featured` show flag: when `show.featured = true`, that show must appear first in the events section on `able-v8.html` regardless of date order — verify the render sort logic checks `featured` before date | V8 | 3 | 2 | M | 3 |
| 34 | Gig mode auto-trigger: `able-v8.html` auto-gig logic reads `able_shows` and compares `show.doorsTime` to `Date.now()` — verify the time comparison is correct: gig activates when `now >= doorsTime` and `now < doorsTime + 4h` (not `now > doorsTime + 4h`) | V8 | 5 | 2 | M | 3 |
| 35 | Campaign state on profile: when `stateOverride === 'gig'` is active on `able-v8.html`, the fan sign-up section must show the gig-specific copy ("I'm on tonight") not the default "Stay close" copy — verify `updateFanCapture(state)` handles the `'gig'` case | V8 | 4 | 2 | M | 3 |
| 36 | Pre-release countdown: the countdown in `pre-release` state must recompute every second (not just on page load) — verify `setInterval(() => renderCountdown(), 1000)` is running, not a single `renderCountdown()` call in init | V8 | 4 | 2 | M | 3 |
| 37 | Countdown display: if `releaseDate` is less than 24h away, the countdown must show hours/minutes/seconds, not just days — verify the countdown render handles `daysLeft === 0` without showing "0 days" | V8 | 3 | 1 | L | 3 |
| 38 | `able_v3_profile` write from admin: every save in admin.html must write the full profile object back to `localStorage.setItem('able_v3_profile', JSON.stringify(profile))` — partial writes (only updating one key) risk corrupting fields not touched in that save | ADM | 5 | 2 | M | 3 |
| 39 | Save feedback: every save action in admin.html must produce visible feedback within 300ms — check that `showToast('Saved')` or equivalent fires after `localStorage.setItem()` in every save handler, not just the main profile save | ADM | 4 | 2 | M | 3 |
| 40 | `safeLS()` wrapper: all localStorage reads across `able-v8.html`, `admin.html`, and `start.html` must use `safeLS()` or equivalent try/catch wrapper — a single uncaught `JSON.parse` on corrupted data will blank the entire page | ALL | 5 | 3 | H | 3 |
| 41 | `safeSet()` wrapper: all localStorage writes must use `safeSet()` or equivalent that catches `QuotaExceededError` — when storage is full, the artist's profile data must be preserved and old `able_clicks` events pruned first | ALL | 4 | 2 | M | 3 |
| 42 | `able_clips` schema: verify each clip in `able_clips` has `{id, url, platform, title, ts}` — the `platform` field (`'youtube'` / `'tiktok'`) is used to render the correct iframe src pattern; missing it will cause silent embed failures | V8 | 3 | 1 | L | 3 |
| 43 | Clips iframe containment: every `<iframe>` in the Clips section must have `max-width: 100%` and `aspect-ratio: 9/16` — verify no clip iframe overflows its container at 375px | V8 | 4 | 1 | L | 3 |
| 44 | Profile null state: `able-v8.html` must handle the case where `able_v3_profile` is null (artist navigates directly to their profile before completing onboarding) — show a graceful empty owner state ("Finish setting up →") not a blank page or JS error | V8 | 5 | 2 | M | 3 |
| 45 | Admin null profile: `admin.html` must handle `able_v3_profile === null` gracefully — redirect to `start.html` or show a "Complete setup" prompt rather than rendering an empty dashboard with broken stat cards | ADM | 5 | 2 | M | 3 |
| 46 | `derivePlatformFromUrl()`: verify this function correctly identifies Spotify (`open.spotify.com`), Apple Music (`music.apple.com`), SoundCloud (`soundcloud.com`), and Bandcamp (`bandcamp.com`) URLs — a miss means the release platform icon is wrong on the profile | ADM | 3 | 1 | L | 4 |
| 47 | Release cards on profile: `able-v8.html` must render release cards from `able_v3_profile.releases` — verify the release render function reads the correct key name and handles an empty releases array without throwing | V8 | 4 | 1 | L | 4 |
| 48 | Credits live links: confirmed credits (handle set) must render as `<a href="/[handle]">Name</a>` at full opacity; unconfirmed credits must render as `<span>Name</span>` at 70% opacity — verify the conditional in the credits render function checks `credit.handle` not `credit.handle !== undefined` | V8 | 5 | 1 | L | 4 |
| 49 | Admin post-gig greeting: `buildGreetingSub()` must fire the `'post-gig'` branch — "Last night at {venue}. {N} fans joined." — when the current time is within 12 hours after gig end (i.e. `able_gig_expires` has passed but less than 12h ago) | ADM | 4 | 2 | M | 4 |
| 50 | Greeting sub-line: `countFansAfterTimestamp()` must count fans whose `ts` is greater than the gig start time (not the expiry time) to correctly attribute fans who signed up during the show | ADM | 3 | 1 | L | 4 |
| 51 | `applyGreeting()` timing: the 2.5s fade from "Good to meet you." to contextual sub on first visit must use CSS transition, not `setTimeout` with a hard `innerHTML` replace — a hard replace will cause CLS and is not reduced-motion safe | ADM | 3 | 2 | M | 4 |
| 52 | Campaign HQ consequence copy: `MODE_DESCS` object must have distinct, correct copy for all 4 states — verify `MODE_DESCS['gig']` does not fall back to `MODE_DESCS['profile']` if the key is misspelled | ADM | 4 | 1 | L | 4 |
| 53 | Campaign state on admin: `#chqModeDesc` must update whenever the artist changes the campaign mode radio/select — verify the change handler calls `renderModeDesc(newState)` and does not require a page reload to show the updated description | ADM | 4 | 1 | L | 4 |
| 54 | Profile completeness signal: admin.html must show new artists what's missing — if `!profile.name || !profile.heroCTAs.length || !profile.artwork` the first-run checklist must surface these gaps, not show an empty progress bar | ADM | 5 | 2 | M | 4 |
| 55 | First-artist experience: the wizard done screen must show "Your page is live." (exact copy, 3 words) and a "See your page →" CTA linking to `able-v8.html?owner=true` — not a generic "You're all set" message | STR | 5 | 2 | M | 4 |
| 56 | `initDone()` in start.html: verify `artist-welcome.js` Netlify function call is fire-and-forget (wrapped in try/catch, not awaited) — a network failure on welcome email must not block the wizard done screen from rendering | STR | 4 | 1 | L | 4 |
| 57 | Spotify import: `linktree-import.js` and `spotify-import.js` Netlify functions must be deployed and reachable at `/api/linktree-import` and `/api/spotify-import` — verify `netlify.toml` `[[functions]]` block references the correct directory | FN | 5 | 2 | M | 4 |
| 58 | `oEmbed proxy`: `oembed-proxy.js` Netlify function must validate the incoming `url` parameter against an allowlist (`open.spotify.com`, `soundcloud.com`, `music.apple.com`) before fetching — arbitrary URL fetching is an SSRF risk | FN | 5 | 2 | H | 4 |
| 59 | `ticketmaster-import.js`: the function must handle a missing `TICKETMASTER_API_KEY` env var gracefully — return a 503 with a clear error message, not a 500 with a raw `undefined` string in the Ticketmaster API URL | FN | 3 | 1 | L | 4 |
| 60 | `artist-welcome.js`: the welcome email must be sent with `from: 'noreply@ablemusic.co'` not a raw Resend test address — verify the `from` field is set correctly and matches the verified Resend domain | FN | 4 | 1 | M | 4 |
| 61 | Snap cards CRUD: deleting a snap card in admin.html must write the updated array back to `able_v3_profile.snapCards` (not a separate key) — verify the delete handler reads the current profile, filters the card, and writes the full profile | ADM | 3 | 1 | L | 5 |
| 62 | Snap card free tier: free tier allows 1 snap card — the "Add snap card" button must be disabled and show a gold lock overlay when `profile.snapCards.length >= 1 && tier === 'free'` | ADM | 4 | 2 | M | 5 |
| 63 | `able_dismissed_nudges` guard: before showing any nudge, check `dismissed.includes(nudgeId)` — verify this check uses the correct nudge ID string (e.g. `'presave-cta'`, `'add-show'`, `'first-run-checklist'`) and does not use index-based access | ADM | 3 | 1 | L | 5 |
| 64 | `admin_visit_dates` for nudge timing: the day-0 bio nudge must only fire when `admin_visit_dates.length === 1` (first ever visit), not every visit — verify the nudge logic reads visit count, not session count | ADM | 3 | 1 | L | 5 |
| 65 | Lifecycle stage `data-stage`: `#page-home` `data-stage` attribute must be set to `'gig'` when gig mode is active, `'new'` when first-run checklist is present, `'active'` otherwise — verify this is applied before CSS accent cues render | ADM | 3 | 2 | M | 5 |
| 66 | Days-active streak stat: `admin_visit_dates` consecutive day calculation must handle gaps — if an artist misses a day, the streak resets to 1, not to the total number of unique visit days | ADM | 2 | 2 | L | 5 |
| 67 | Analytics `page_viewed` event: verify PostHog `page_viewed` fires on `DOMContentLoaded` with `{ page: 'artist_profile', artist_slug: slug, theme: currentTheme, state: campaignState }` — missing properties make post-launch analysis harder | V8 | 3 | 1 | L | 5 |
| 68 | PostHog `fan_signed_up` event: must include `{ source: ref, campaign_state: state, tier: tier }` — the campaign state at sign-up time is the single most valuable CRM field and must not be missing | V8 | 5 | 1 | L | 5 |
| 69 | PostHog `cta_tapped` event: must include `{ label, type, zone: 'hero'|'pills'|'section', url }` — zone classification is what makes the CTA architecture auditable post-launch | V8 | 4 | 1 | L | 5 |
| 70 | PostHog init: `posthog.init()` call must be inside a try/catch — a CSP block or network failure must not cause an uncaught error that prevents `DOMContentLoaded` from completing | V8 | 4 | 1 | L | 5 |
| 71 | Anti-self-visit: `able_views` and PostHog `page_viewed` must not fire when the profile owner is visiting their own page — detect via `?owner=true` param or `localStorage.getItem('able_v3_profile')` non-null and set `isArtist: true`, then conditionally skip or flag the event | V8 | 4 | 2 | M | 5 |
| 72 | `fan_following` key in fan.html: when a fan follows an artist from the confirmation email flow (`?artist=slug&ref=email-confirm`), `initArrival()` must push the slug to `fan_following` — verify the slug is correctly parsed from the URL param, not hardcoded | FN | 4 | 1 | L | 5 |
| 73 | `fan_location` key: verify `fan_location` stores `{city, country}` (not lat/lng for V1) and that the Near Me location input in `fan.html` writes the correct object shape | FN | 2 | 1 | L | 5 |
| 74 | `able_v3_profile.accent` validation: if an artist sets an accent colour via the colour picker, verify the hex value is stored with a `#` prefix (not without) — `applyDerivedTokens()` passing a raw hex without `#` to `color-mix()` will silently fail | ADM | 3 | 1 | L | 5 |
| 75 | Theme persistence: `profile.theme` must be stored in `able_v3_profile` and applied on every `able-v8.html` load before first paint — a flash of the default dark theme before the artist's chosen theme is applied is a polish failure | V8 | 4 | 2 | M | 5 |
| 76 | Vibe system: all 7 vibes (Electronic, Hip Hop, R&B, Indie, Pop, Rock, Acoustic) must resolve to a `data-vibe` attribute on `<body>` that has corresponding CSS rules in the stylesheet — a missing vibe rule leaves the page in the default state silently | V8 | 3 | 2 | M | 5 |
| 77 | Feel quadrant: `data-feel` attribute must be set from `profile.feel` on `able-v8.html` load — the 4 feels (raw/refined/intimate/bold) drive font weight and spacing personality; a missing feel leaves the page looking generic | V8 | 3 | 1 | L | 5 |
| 78 | `applyDerivedTokens()`: verify it derives `--color-accent-rgb` (for `rgba()` usage), `--color-on-accent` (text colour on accent button), and `--color-ambient` (tinted background glow) from `--color-accent` — any missing derivation will cause a visual failure that is hard to track | V8 | 4 | 2 | M | 5 |
| 79 | Hero CTA max 2: if admin.html allows adding more than 2 hero CTAs, the third must be silently dropped on render — verify `profile.heroCTAs.slice(0, 2)` is used when rendering, not the full array | V8 | 4 | 1 | L | 6 |
| 80 | Quick Action pills overflow: the overflow toggle ("+ N more") must correctly count hidden pills — verify the count uses `pills.length - visibleCount`, not a hardcoded number | V8 | 3 | 1 | L | 6 |
| 81 | `able_fans` JSON integrity: if `able_fans` contains a corrupted entry (e.g. `null` or a non-object), the fan list render in admin must skip the bad entry, not throw — wrap each fan render in a try/catch or filter out non-objects before mapping | ADM | 4 | 1 | L | 6 |
| 82 | Fan export CSV: the export function must include `email, ts, source, optIn, consentVersion` — verify `consentVersion` is being written to each fan object at sign-up time (the GDPR consent text may change; versioning lets you prove which version each fan consented to) | ADM | 5 | 2 | M | 6 |
| 83 | Fan export on free tier: CSV export must be available on every tier including free — verify `checkTierGate()` does not gate the export button | ADM | 5 | 1 | L | 6 |
| 84 | Snap card char limit: snap card body text must be limited to 280 characters to prevent overflow on the profile — verify the admin textarea has `maxlength="280"` and the profile render clips at 280 chars | ADM | 2 | 1 | L | 6 |
| 85 | `able_clips` max length: impose a maximum of 12 clips for V1 — an unbounded clips array will make the profile page scroll-heavy and degrade performance | ADM | 2 | 1 | L | 6 |
| 86 | Shows `doorsTime` format: verify `doorsTime` is stored as a full ISO timestamp (not just `"19:30"`) to allow correct `Date.now()` comparison in the auto-gig logic — a time-only string will produce `NaN` on `new Date(show.doorsTime)` | ADM | 5 | 2 | M | 6 |
| 87 | Shows `doorsTime` input: the doors time input in admin.html must be `type="time"` to give mobile users a native time picker — verify this is not `type="text"` which requires manual parsing | ADM | 3 | 1 | L | 6 |
| 88 | `able_v3_profile` max size: the profile object with all fields (bio 160 chars, 2 hero CTAs, 6 pills, 4 releases each with 3 credits, 4 snap cards, 12 clips) must stay well under 100KB — log the serialised size in dev mode and alert if over 80KB | ALL | 3 | 2 | L | 6 |
| 89 | Platform pills render: `profile.platformLinks` must filter out any entry with an empty `url` before rendering — an empty-URL pill renders a broken `<a href="">` that navigates to the current page | V8 | 3 | 1 | L | 6 |
| 90 | Merch section: `profile.merch` must handle `[]` (empty array) gracefully — the merch section must not render at all (not render an empty section header) when no merch items are configured | V8 | 2 | 1 | L | 6 |
| 91 | Support section: `profile.supportUrl` empty check — if the artist hasn't set a support URL, the support section must be hidden on the profile, not render a broken link | V8 | 3 | 1 | L | 6 |
| 92 | World map in admin: `able_shows` date entries must be correctly mapped to the calendar view — verify the date format written by the shows form (`YYYY-MM-DD`) matches what the world map calendar expects | ADM | 3 | 2 | M | 6 |
| 93 | `topbarViewBtn` href: verify the "View your page" button in admin.html sets `href` to `able-v8.html` (not `able-v7.html`) — there are known stale refs to the old filename; session 17 corrected 5 but verify all are resolved | ADM | 4 | 1 | L | 6 |
| 94 | `@supports (view-transition-name: none)` guard: all 3 files (able-v8.html, admin.html, start.html) must have this guard to prevent view transition API calls on browsers that don't support it — verify the guard is present post-session-15 | ALL | 3 | 1 | L | 6 |
| 95 | `shared/tokens.css`: verify `shared/tokens.css` is linked in every active page (`able-v8.html`, `admin.html`, `start.html`, `landing.html`) via `<link rel="stylesheet" href="shared/tokens.css">` before any page-specific styles | ALL | 4 | 1 | L | 6 |
| 96 | `shared/able.js`: if `safeLS()` and `safeSet()` are defined in `shared/able.js`, verify all 4 pages include `<script src="shared/able.js">` before their inline `<script>` blocks — a missing include means the wrappers are undefined and raw localStorage calls throw | ALL | 5 | 1 | H | 6 |
| 97 | Landing CTA link: the primary CTA on `landing.html` must link to `start.html` not `able-v8.html` — a cold visitor who clicks "Your page is free →" should enter the onboarding wizard, not land on an empty profile | LND | 5 | 1 | L | 6 |
| 98 | `able_profile` legacy key cleanup: after `migrateWizardKey()` runs, `able_profile` must be removed (`localStorage.removeItem('able_profile')`) — leaving stale keys in storage risks a future code path reading the wrong key | ADM | 3 | 1 | L | 6 |
| 99 | `fan_confirmation.js` artist context: verify the fan confirmation email includes `?artist={slug}&ref=email-confirm` in the fan dashboard CTA link — this was spec-confirmed done in session 11 but regression-check after any email template change | FN | 5 | 1 | L | 6 |
| 100 | End-to-end data flow smoke test: after any change to `start.html`, run a full wizard → profile → admin data flow check: complete wizard, read `able_v3_profile` in console, confirm name/accent/vibe/heroCTAs are present, load `able-v8.html` and verify profile renders correctly, open `admin.html` and verify stats/fans/campaign state all load without errors | ALL | 5 | 3 | M | 6 |

---

## Phase 3 — Mobile UX
*90% of traffic hits able-v8.html on a phone. If fan sign-up has friction, if tap targets are small, if iOS Safari breaks the keyboard — conversion drops to zero.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | Fan sign-up email input: `type="email"` must be set — iOS Safari shows the email keyboard (@ key prominent) — verify `<input type="email" name="email">` not `type="text"` on the fan capture form | V8 | 5 | 1 | L | 1 |
| 2 | Fan capture submit button: minimum 44px height on the submit button — measure in DevTools at 375px; if current height is 40px or less, add `min-height: 44px` and verify it doesn't break layout | V8 | 5 | 1 | L | 1 |
| 3 | iOS auto-zoom: fan capture email input must have `font-size: 16px` minimum — if it's `14px` or `15px`, iOS Safari will zoom the viewport on focus, breaking the layout — verify computed font-size at 375px | V8 | 5 | 1 | L | 1 |
| 4 | All admin.html form inputs: verify every text/email/number input in admin.html has `font-size: 16px` or larger — the release URL field, bio textarea, snap card body, and show venue input are common offenders for sub-16px that trigger iOS zoom | ADM | 5 | 2 | L | 1 |
| 5 | Fan sign-up return key: submitting the email field by pressing Return/Go on iOS must trigger form submission — verify the form has `action` or the input has a `keydown` listener that calls `submitFanCapture()` on Enter | V8 | 5 | 1 | L | 1 |
| 6 | Fan capture success state: the success message must appear within 200ms of form submission (localStorage write is synchronous, so this is achievable) — verify there is no artificial delay or animation that makes the confirmation feel slow | V8 | 5 | 1 | L | 1 |
| 7 | Fan sign-up form single field: verify the fan capture form is genuinely single-field (email only) — no name field, no checkbox on the initial capture form; GDPR consent text must be visible text near the button, not a hidden checkbox | V8 | 5 | 1 | L | 1 |
| 8 | Hero CTA buttons: both hero CTAs must have `min-height: 44px` and `min-width: 44px` — measure at 375px; pill-style ghost buttons often collapse below 44px on narrow screens | V8 | 5 | 1 | L | 1 |
| 9 | Quick Action pills: each pill must have a tap target of at least 44px height — pill text is typically 13-14px, making the pill itself ~32px; add `padding: var(--sp-3) var(--sp-4)` to ensure 44px minimum | V8 | 5 | 1 | L | 1 |
| 10 | Admin bottom nav: all 5 tab buttons must be at least 44px tall — measure the computed height of `.bottom-nav__btn` or equivalent at 375px; icon + label combos often sit at 40px | ADM | 5 | 1 | L | 1 |
| 11 | Admin Campaign HQ buttons: "Enable gig mode", "Set release date", and campaign state selectors must all have `min-height: 44px` — these are critical actions that are easy to mis-tap on a small screen | ADM | 4 | 1 | L | 1 |
| 12 | Start.html wizard next/back buttons: `min-height: 44px` — the "Next" button is the primary action on every step; measure at 375px and verify it meets the minimum | STR | 5 | 1 | L | 1 |
| 13 | Icon-only buttons: any button that is icon-only (close, delete, edit, star) must have a minimum 44×44px tap target even if the visible icon is 20-24px — use `padding` to expand the hit area, not `min-width`/`min-height` on the icon itself | ALL | 5 | 2 | L | 1 |
| 14 | Thumb zone: the primary fan sign-up CTA on able-v8.html must sit below the fold midpoint on a 390px screen — a CTA that requires scrolling to the very bottom puts it out of easy thumb reach; verify the fan capture section is within the bottom 60% of the viewport in the most common `profile` state | V8 | 5 | 2 | M | 2 |
| 15 | Thumb zone admin: the "Save" button in admin.html profile edit form must be reachable without stretching — on 390px iPhone, if "Save" is at the very top of a long form, it requires two hands; consider sticky save bar at bottom of form sections | ADM | 4 | 3 | M | 2 |
| 16 | iOS safe area: the bottom nav in admin.html must add `padding-bottom: env(safe-area-inset-bottom)` to avoid the iOS home indicator overlapping the last tab — verify on a device with a home indicator (iPhone X onwards) | ADM | 4 | 1 | L | 2 |
| 17 | iOS safe area on profile: the "Made with ABLE" footer must clear the safe area — `padding-bottom: calc(var(--sp-4) + env(safe-area-inset-bottom))` — verify on 390px | V8 | 3 | 1 | L | 2 |
| 18 | iOS keyboard fixed position: the admin bottom nav is `position: fixed; bottom: 0` — when the iOS keyboard opens (e.g. editing bio), the nav must not jump or overlap the keyboard — test by focusing the bio textarea and verifying the nav doesn't shift | ADM | 5 | 2 | M | 2 |
| 19 | iOS keyboard scroll jump: when a form input is focused on iOS, the page scrolls to position the input above the keyboard — verify the focused element is not obscured by any fixed header or the keyboard itself across all admin forms | ADM | 4 | 2 | M | 2 |
| 20 | Fixed header on profile: the sticky artist bar (`.hero__sticky` or equivalent) that appears on scroll must account for `env(safe-area-inset-top)` — on notched iPhones, a `top: 0` fixed element sits behind the notch | V8 | 3 | 1 | L | 2 |
| 21 | Scroll behaviour on iOS: `-webkit-overflow-scrolling: touch` is deprecated; verify no component uses this — instead use `overscroll-behavior: contain` on scroll containers to prevent scroll chaining | ALL | 2 | 1 | L | 2 |
| 22 | Scroll jank prevention: any element with `backdrop-filter` (glass theme, sticky bar) must have `will-change: transform` or `transform: translateZ(0)` applied to create a compositing layer and prevent repaint on scroll | V8 | 3 | 2 | M | 2 |
| 23 | Horizontal scroll at 375px: verify `overflow-x: hidden` is on `<html>` or `<body>` — a single element wider than 375px (an uncontained image, a long CTA label, an unbroken URL string) will cause horizontal scroll and break the mobile layout | ALL | 4 | 2 | M | 2 |
| 24 | Platform pills overflow: the Quick Action pills row must wrap cleanly at 375px — verify the pills container uses `flex-wrap: wrap` and that wrapped pills align left, not stretch to full width | V8 | 3 | 1 | L | 2 |
| 25 | Release card width: release cards in the music section must not overflow at 375px — if cards use `width: 320px` fixed they will overflow by 5px on iPhone SE; use `width: min(320px, calc(100vw - var(--sp-8)))` | V8 | 3 | 1 | L | 2 |
| 26 | Bento grid events: the bento grid events section must collapse to a single-column stack at 375px — verify `grid-template-columns` uses `repeat(auto-fill, minmax(...)` or a breakpoint media query | V8 | 3 | 2 | M | 2 |
| 27 | Merch grid: same as events — merch bento grid must not produce two narrow columns that are barely legible at 375px; min column width should be `min(160px, calc(50vw - var(--sp-6)))` | V8 | 3 | 2 | M | 2 |
| 28 | Countdown display: in pre-release state, the countdown ring/number must be readable at 375px without font-size overflowing its container — measure at iPhone SE width | V8 | 3 | 1 | L | 2 |
| 29 | Clips iframe at 375px: `aspect-ratio: 9/16` on a 375px wide container produces a 667px tall iframe — if the iframe is full-width and 667px tall, it dominates the entire viewport; cap height at `min(66vh, 500px)` | V8 | 4 | 1 | L | 2 |
| 30 | `input[type="date"]` on iOS: date inputs in admin.html (release date, show date) must use `type="date"` to show the native iOS date picker — verify these are not `type="text"` which would require manual date format parsing | ADM | 4 | 1 | L | 2 |
| 31 | `input[type="time"]` on iOS: doors time input must use `type="time"` for native iOS time picker — a text input for time requires the artist to type `19:30` correctly; the native picker prevents format errors | ADM | 3 | 1 | L | 2 |
| 32 | `input[type="url"]` for links: all URL inputs (platform link, CTA URL, ticket URL, support URL) must use `type="url"` — iOS shows a keyboard with `.com` key and URL-optimised layout | ADM | 3 | 1 | L | 2 |
| 33 | `autocomplete` attributes: fan capture email field must have `autocomplete="email"` — iOS Safari uses this for AutoFill, reducing sign-up friction significantly | V8 | 4 | 1 | L | 2 |
| 34 | Admin form `autocomplete`: artist name field must have `autocomplete="name"`, email field `autocomplete="email"` — AutoFill speeds up profile completion in the onboarding flow | STR | 3 | 1 | L | 2 |
| 35 | `inputmode` attribute: search/filter inputs in admin fan list must have `inputmode="search"` to show the search keyboard on iOS (which has a Go/Search button instead of Return) | ADM | 2 | 1 | L | 2 |
| 36 | Slow network fan capture: on a throttled 3G connection, the fan sign-up must show a loading state (spinner or button text change to "Saving…") within 300ms of tap — localStorage write is synchronous so there should be no actual delay, but verify the success state renders immediately after the write | V8 | 4 | 1 | L | 3 |
| 37 | Slow network image loading: artwork and Unsplash images must have `loading="lazy"` on all images below the fold, and `loading="eager"` only on the hero artwork — verify this attribute is set on all `<img>` tags | V8 | 3 | 1 | L | 3 |
| 38 | Slow network embed loading: Spotify / SoundCloud iframes must not block the page paint — verify all `<iframe>` tags have `loading="lazy"` | V8 | 3 | 1 | L | 3 |
| 39 | Slow network admin: admin.html must not show a blank stats panel while computing from localStorage — all stat values must be computed synchronously from localStorage on `DOMContentLoaded` and rendered immediately, not after a `setTimeout` | ADM | 4 | 1 | L | 3 |
| 40 | Skeleton screens: if admin.html ever fetches from Supabase or a Netlify function (currently: Ticketmaster import), the panel that awaits the response must show a skeleton state — a blank panel with a spinner is better than a blank panel with nothing | ADM | 3 | 2 | M | 3 |
| 41 | Swipe gesture conflict: bottom sheets in admin.html (if any) must use vertical swipe to dismiss — any horizontal swipe handler on a panel that sits at the viewport edge will conflict with the iOS back gesture | ADM | 4 | 2 | M | 3 |
| 42 | Touch target spacing: buttons placed next to each other (e.g. edit/delete on a snap card row) must have at least 8px gap between tap targets to prevent accidental taps — verify at 375px | ADM | 4 | 1 | L | 3 |
| 43 | `touch-action: manipulation` on all interactive elements: `* { touch-action: manipulation }` is confirmed in STATUS.md for able-v7.html — verify this rule is also present in admin.html and start.html to eliminate 300ms tap delay on older iOS | ADM | 4 | 1 | L | 3 |
| 44 | Long press prevention: draggable/reorderable snap card list and clips list in admin must prevent the default iOS long-press context menu from firing during drag — add `oncontextmenu="return false"` or `touch-action: none` on drag handles | ADM | 2 | 1 | L | 3 |
| 45 | Landscape at 667×375: verify able-v8.html in landscape — the hero section must not take up the full viewport height (100vh is 375px in landscape), forcing the user to scroll past a blank hero before seeing content | V8 | 4 | 2 | M | 3 |
| 46 | Landscape admin: admin.html in landscape must not clip the bottom nav — verify that `position: fixed; bottom: 0` nav is still visible when the viewport is 375px tall and the content area fills 375px minus the nav | ADM | 3 | 2 | M | 3 |
| 47 | Landscape start.html: the wizard phone preview must scale down gracefully in landscape — at 667×375, the preview phone may overflow the viewport height; add `max-height: 80vh` on the preview container | STR | 3 | 1 | L | 3 |
| 48 | Landscape landing.html: the hero section must remain visually coherent at 667×375 (iPhone landscape) — the hero copy and CTA must both be visible without scrolling | LND | 3 | 2 | M | 3 |
| 49 | Viewport meta tag: all 4 pages must have `<meta name="viewport" content="width=device-width, initial-scale=1">` — `user-scalable=no` must NOT be present (it prevents accessibility zoom and is an accessibility violation) | ALL | 5 | 1 | L | 3 |
| 50 | `prefers-reduced-motion` on all pages: verify start.html and landing.html have the same comprehensive `prefers-reduced-motion` CSS block as able-v8.html — session 19 confirms able-v8.html has this; landing.html was updated in session 19 for pulse/marquee/drift, but start.html needs verification | STR | 4 | 2 | L | 3 |
| 51 | Fan capture auto-focus: when the fan sign-up section scrolls into view, the email input must NOT auto-focus — auto-focus on scroll causes iOS to open the keyboard unexpectedly, jumping the page position | V8 | 4 | 1 | L | 4 |
| 52 | Fan capture manual focus: a tap on the email input must focus it immediately — verify there is no `e.preventDefault()` or `pointer-events: none` on the input's parent element that could intercept the tap | V8 | 3 | 1 | L | 4 |
| 53 | iOS Safari position: fixed elements that include `transform` will lose `position: fixed` behaviour in iOS Safari — verify no fixed-position element (sticky nav, admin bottom bar) has a `transform` property applied | ALL | 4 | 2 | M | 4 |
| 54 | Modal/sheet z-index on iOS: modals and bottom sheets must use `position: fixed` with `z-index` high enough to sit above the iOS status bar content area — verify modals are not clipped by the notch | ADM | 3 | 2 | M | 4 |
| 55 | Colour picker tap target in admin: the artist accent colour picker must have a tap target of at least 44×44px — a small `<input type="color">` swatch is often ~30px; wrap in a `<label>` with sufficient padding | ADM | 3 | 1 | L | 4 |
| 56 | Dropdown/select on mobile: any `<select>` element in admin.html (campaign state, tier, vibe) must render the native iOS picker — do not suppress the native `<select>` with `appearance: none` without providing a custom accessible replacement | ADM | 3 | 1 | L | 4 |
| 57 | Snap card image upload: if snap cards support image upload, the file input must have `accept="image/*"` and `capture` attribute consideration for mobile — verify the file picker opens the photo library on iOS | ADM | 2 | 1 | L | 4 |
| 58 | Link copy button: "Copy link" buttons (UTM copy links, QR code share) must use `navigator.clipboard.writeText()` with a fallback to `document.execCommand('copy')` — iOS Safari supports Clipboard API only in secure contexts; verify it's not called in a `setTimeout` | ADM | 4 | 1 | L | 4 |
| 59 | QR code download: `downloadSbQR()` must use a `<a download>` link approach, not `window.open()` — iOS Safari blocks `window.open()` outside user gesture handlers | ADM | 3 | 1 | L | 4 |
| 60 | Share API: the "Share my page" button in admin or wizard done screen must use `navigator.share()` when available (iOS 12.2+) with a fallback to clipboard copy — `navigator.share()` opens the native iOS share sheet which is far more effective than a copy-only button | ADM | 4 | 2 | M | 4 |
| 61 | Font scaling: admin.html must use `rem` units for font sizes (not `px`) so they scale with the user's preferred text size — an artist who has increased their iOS text size should see admin content scale up | ADM | 3 | 3 | M | 4 |
| 62 | Pinch to zoom: verify no page sets `user-scalable=no` or `maximum-scale=1.0` in the viewport meta — these prevent accessibility zoom and are blocked by WCAG 1.4.4 | ALL | 5 | 1 | L | 4 |
| 63 | Text selection: verify `user-select: none` is not applied to content text (it should only be on UI chrome like button labels and icon buttons) — fans must be able to select and copy an artist's bio | V8 | 2 | 1 | L | 4 |
| 64 | Overflow hidden on body: `overflow: hidden` on `<body>` must be removed after a modal closes — a common bug where body overflow is set to `hidden` when a modal opens and not restored, leaving the page unscrollable | ADM | 4 | 1 | M | 4 |
| 65 | Momentum scrolling: scroll containers inside modals and bottom sheets (e.g. fan list, clip list) must use `overflow-y: auto` with `-webkit-overflow-scrolling: touch` removed — iOS Safari applies momentum scrolling automatically to `overflow-y: auto` | ADM | 2 | 1 | L | 5 |
| 66 | Pull-to-refresh conflict: if the fan capture section is at the top of a scroll area, iOS pull-to-refresh may fire when fans try to scroll up — verify the main content area uses `overscroll-behavior-y: contain` to prevent this | V8 | 3 | 1 | L | 5 |
| 67 | Bottom sheet drag to dismiss: admin modals or sheets that support drag-to-dismiss must ensure the drag handle has a 44×44px touch area — a 4px drag handle is untappable | ADM | 3 | 2 | M | 5 |
| 68 | Focus management after modal close: when a bottom sheet or modal closes in admin.html, focus must return to the element that triggered it — verify `triggerEl.focus()` is called in the modal close handler | ADM | 3 | 2 | M | 5 |
| 69 | Keyboard dismiss: pressing Escape must close any open modal or sheet — verify `keydown` listener checks for `key === 'Escape'` in all modal/sheet components | ADM | 3 | 1 | L | 5 |
| 70 | Number input on mobile: show count input and any numeric admin field must use `inputmode="numeric"` (not `type="number"`) if the value must be a positive integer — `type="number"` shows a stepper on desktop but `inputmode="numeric"` gives the number pad on mobile without steppers | ADM | 2 | 1 | L | 5 |
| 71 | Tap target audit tool: run a Playwright screenshot at 375px with DevTools element size overlay and document any interactive element under 44px — this is the definitive baseline check, not a manual audit | ALL | 4 | 2 | L | 5 |
| 72 | Landing CTA at 375px: the landing page primary "Your page is free →" CTA button must have `min-height: 44px` and must not be cut off at 375px width | LND | 5 | 1 | L | 5 |
| 73 | Landing nav links: landing.html navigation links must have 44px tap targets — the "Sign in" and "How it works" nav links are typically smaller than 44px | LND | 3 | 1 | L | 5 |
| 74 | Error state on fan capture: if an invalid email is entered and submitted, the error message must appear adjacent to the input (not above or below the button) and must not cause the layout to shift (reserve space with `min-height` on the error container) | V8 | 4 | 2 | M | 5 |
| 75 | Error state timing: the `E11` error message delayed reveal (400ms after shake) must not conflict with the iOS keyboard dismiss — if the user taps outside the input after an error, the keyboard dismisses and then the error message appears 400ms later at a different position | V8 | 3 | 2 | M | 5 |
| 76 | Wizard step navigation: the Back button on each wizard step must be at least 44px — verify `.wiz-back` or equivalent has `min-height: 44px` | STR | 3 | 1 | L | 5 |
| 77 | Wizard accent colour swatch: the colour swatch buttons in step 2 (artist identity) must be at least 44×44px — small colour dots are common mobile UX failures | STR | 4 | 1 | L | 5 |
| 78 | Wizard feel selection: the feel quadrant cards must have large enough tap targets on mobile — at 375px, four cards in a 2×2 grid gives each card ~160×80px which is fine; verify they don't compress below 44px tall | STR | 3 | 1 | L | 5 |
| 79 | Wizard phone preview: the live preview phone in start.html must not overflow its container at 375px — the preview is decorative but an overflowing iframe causes horizontal scroll on the wizard itself | STR | 4 | 1 | L | 5 |
| 80 | Admin edit round-trip: "Preview your page →" opens `able-v8.html` — on mobile, this opens in the same tab (not a new tab); verify the back button on the profile returns to admin.html with state preserved (no blank admin on back navigation) | ADM | 4 | 2 | M | 5 |
| 81 | Stats grid at 375px: the 2×2 stats grid in admin.html (confirmed session 14) must render without overlap at 375px — verify `grid-template-columns: 1fr 1fr` and `gap: var(--sp-3)` produce a correct layout | ADM | 4 | 1 | L | 5 |
| 82 | Topbar at 375px: admin topbar overflow fix confirmed in session 14 — regression-check that the topbar doesn't overflow after any admin.html changes | ADM | 3 | 1 | L | 5 |
| 83 | Fan list scroll: the fan list in admin.html must scroll smoothly on mobile when there are 50+ fans — verify it uses `overflow-y: auto` on a fixed-height container rather than causing the whole page to scroll | ADM | 3 | 2 | M | 5 |
| 84 | Export fans button: the export button must have a visible loading state on mobile because CSV generation from 100 fan records can take 200-300ms | ADM | 2 | 1 | L | 6 |
| 85 | Gig mode toggle: the gig mode enable button in Campaign HQ must be at least 44px tall and have a 44px touch area — this is a critical action on the day of a show when the artist is in a rush | ADM | 5 | 1 | L | 6 |
| 86 | Release date input on mobile: `<input type="date">` on iOS Safari presents the native date picker which is correct — verify the input does not have a custom date picker overlay that conflicts with the native one | ADM | 3 | 1 | L | 6 |
| 87 | Long bio text: bios up to 160 characters must render without overflow at 375px — long words in an artist's name or bio (e.g. a URL accidentally typed in the bio) must wrap, not overflow | V8 | 3 | 1 | L | 6 |
| 88 | Snap card body: snap card long text must use `overflow-wrap: break-word` to prevent a long word or URL from causing horizontal overflow on the profile | V8 | 2 | 1 | L | 6 |
| 89 | Release title overflow: a long release title (e.g. "The Ballad of a Thin Man Running Through the Night") must truncate with `text-overflow: ellipsis` on the release card, not overflow the card width | V8 | 2 | 1 | L | 6 |
| 90 | Merch item title: same as release title — long merch names must truncate, not overflow the merch card | V8 | 2 | 1 | L | 6 |
| 91 | Vibe pill label in admin: long vibe names ("Electronic", "Hip Hop") must fit in their pill at 375px without truncation — verify the pill uses `white-space: nowrap` and the pill container allows wrapping | ADM | 2 | 1 | L | 6 |
| 92 | `prefers-color-scheme` and theme: if the user's OS is in dark mode and the artist's page is the Light theme, the page must still render in Light theme — verify `data-theme="light"` takes precedence over `@media (prefers-color-scheme: dark)` | V8 | 3 | 2 | M | 6 |
| 93 | iOS 17 Safari scroll anchor: iOS 17 changed scroll anchor behaviour — verify that the admin page sections don't jump when the keyboard opens near a scroll anchor point | ADM | 3 | 2 | M | 6 |
| 94 | Dynamic island / notch: on iPhone 15 Pro, the dynamic island is at the top centre — verify the admin topbar content (ABLE logo, title) is not obscured by the dynamic island at the `env(safe-area-inset-top)` boundary | ADM | 2 | 1 | L | 6 |
| 95 | Android Chrome compatibility: `backdrop-filter` (used in glass theme and sticky bar) is supported in Chrome Android 76+ — verify there is a `@supports (backdrop-filter: blur(1px))` guard so non-supporting browsers get a solid background fallback | V8 | 3 | 2 | M | 6 |
| 96 | Android back gesture: Android's edge swipe back gesture is on the left edge — verify no horizontal drag gesture in the app conflicts with this, particularly any left-edge drawer or swipeable list item | ADM | 3 | 1 | L | 6 |
| 97 | Landscape fan capture: in landscape mode on a 375px-tall viewport, the fan capture section must be visible without requiring the user to scroll past 100vh of content — verify the section order puts fan capture within the first 150% of viewport height | V8 | 3 | 2 | M | 6 |
| 98 | Landscape Clips section: Clips in landscape (9/16 aspect ratio) will be 208px wide on a 375px tall viewport — verify clips don't crowd together or overflow in landscape | V8 | 2 | 1 | L | 6 |
| 99 | Real-device PWA install test: install ABLE to the home screen on a real iPhone and verify: (1) the app icon shows correctly (not broken), (2) the app opens to `able-v8.html` or admin correctly, (3) the status bar colour matches the theme — Playwright screenshots cannot verify this | ALL | 4 | 2 | M | 6 |
| 100 | Mobile UX smoke test: after any significant change to able-v8.html or admin.html, run a Playwright screenshot at 375px width and 812px height (iPhone SE viewport) for both pages and visually verify: no horizontal scroll, all buttons visible, no text overflow, fan capture form present and correctly sized | ALL | 5 | 2 | L | 6 |

---

## Phase 4 — Copy & Voice
*Every phrase that sounds like a startup or marketing tool is a trust failure. These are not polish items — they are conversion items.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | Landing headline: current above-the-fold copy must lead with felt artist pain, not strategic ownership argument — use: "300,000 people through your bio link this year. Nothing to show for it." as the hook (GPT review confirmed emotional trigger > strategic argument for cold acquisition) | LND | 5 | 2 | L | 1 |
| 2 | Landing sub-headline: the product description must be: "A music-native bio link that turns moments of attention into owned fan relationships, while adapting automatically to your release cycle." — this exact string from GPT review is the best single-sentence product description | LND | 5 | 1 | L | 1 |
| 3 | Landing primary CTA: must read "Your page is free →" not "Get started free" or "Sign up free" — confirmed fix in STATUS.md but regression-check after any landing.html edit | LND | 5 | 1 | L | 1 |
| 4 | Banned phrase scan — "superfans": grep all 4 HTML files for "superfan" — zero instances allowed; replace with "your most dedicated listeners" or "the people who show up" | ALL | 5 | 1 | L | 1 |
| 5 | Banned phrase scan — "grow your audience": grep for "grow your audience", "grow your fanbase", "growing your" — zero instances allowed; replace with "reach people who care" | ALL | 5 | 1 | L | 1 |
| 6 | Banned phrase scan — "monetise": grep for "monetis" (catches British and American spelling) — zero instances; replace with "let people support you directly" | ALL | 5 | 1 | L | 1 |
| 7 | Banned phrase scan — "engage": grep for "engage your" — zero instances on any user-facing copy; internal code comments are exempt | ALL | 4 | 1 | L | 1 |
| 8 | Banned phrase scan — "content creator": grep for "content creator" — zero instances; replace with "artist" | ALL | 4 | 1 | L | 1 |
| 9 | Banned phrase scan — exclamation marks in dashboard: grep admin.html for `!` in any user-facing string (not in HTML attributes or JS conditions) — zero allowed; remove or rewrite every instance | ADM | 4 | 1 | L | 1 |
| 10 | Banned phrase scan — "Get started": grep for "Get started" case-insensitive — zero on any page; replace with direct action copy ("Your page is free →", "A few details first") | ALL | 4 | 1 | L | 1 |
| 11 | Banned phrase scan — "You're all set": grep for "all set" — zero instances; replace with "Your page is live." | ALL | 4 | 1 | L | 1 |
| 12 | Banned phrase scan — "going viral": grep for "viral" — zero instances; if in landing features section, remove the concept entirely | ALL | 5 | 1 | L | 1 |
| 13 | Wizard done screen: must read "Your page is live." (3 words, period, no exclamation mark) — verify `initDone()` sets the heading to exactly this string | STR | 5 | 1 | L | 1 |
| 14 | Wizard done CTA: "See your page →" must link to `able-v8.html?owner=true` — not "View profile" or "Go to your page" | STR | 5 | 1 | L | 1 |
| 15 | Admin greeting: "Good to see you, [Name]." — the period, not an exclamation mark, is load-bearing — verify `applyGreeting()` produces this exact format | ADM | 4 | 1 | L | 1 |
| 16 | Admin first meeting: "Good to meet you." (not "Welcome!" or "Hello!") — verify the first-ever-visit path in `applyGreeting()` produces this exact string | ADM | 4 | 1 | L | 1 |
| 17 | Admin day-of-show: "You're on tonight." must replace the normal greeting when gig mode is active — verify this is the exact string in `buildGreetingSub()` `'gig'` branch | ADM | 5 | 1 | L | 1 |
| 18 | Post-gig greeting: "Last night at {venue}. {N} fans joined." — the period between venue and fan count is intentional pacing; verify `buildGreetingSub()` `'post-gig'` branch produces this exact format | ADM | 5 | 1 | L | 2 |
| 19 | First fan ever copy: "Your first fan. This is how every list starts." — verify `checkFirstFanMoment()` surfaces exactly this string, not the session 11 version "That email is yours — no platform decides whether they hear from you." which was an earlier draft | ADM | 5 | 1 | L | 2 |
| 20 | Fan list empty state: must read "When fans sign up on your page, they'll appear here. Your list. Your relationship. No algorithm in the way." — verify the empty state copy in admin.html fan list panel uses this exact string from CONTEXT.md | ADM | 5 | 1 | L | 2 |
| 21 | Error copy: must read "That didn't go through. Try again?" — verify every error state in admin.html and start.html uses this or a contextually appropriate variant, not "Something went wrong." or "Error: [code]" | ALL | 4 | 2 | L | 2 |
| 22 | Magic link email: "Here's your link. It expires in 1 hour." — no boilerplate, no footer copy, no "Welcome to ABLE!" — verify `artist-welcome.js` template uses this voice | FN | 4 | 1 | L | 2 |
| 23 | Support fees copy: "0% taken by ABLE. Stripe standard fee only." — verify this exact string appears in the support setup section of admin.html | ADM | 4 | 1 | L | 2 |
| 24 | "Made with ABLE ✦": the ✦ character (U+2726) must appear in exactly one place across the entire platform — verify it's only in the profile footer, not in admin, landing, or start | V8 | 3 | 1 | L | 2 |
| 25 | Campaign state labels: the four states must use exactly these labels everywhere they appear: "Profile", "Pre-release", "Live", "Gig" — with the exact capitalisation; not "PRE-RELEASE" or "pre_release" in user-facing copy | ADM | 4 | 1 | L | 2 |
| 26 | Campaign mode jargon: the GPT review flagged "Pre-release", "Live", "Gig" as jargon for artists unfamiliar with campaign thinking — each mode in admin.html must have a one-sentence plain-English explanation; verify `MODE_DESCS` copy uses plain language, not product feature names | ADM | 5 | 2 | L | 2 |
| 27 | Pre-release mode copy on profile: in pre-release state, the fan capture heading must read something like "I'm building something. Be the first to hear it." — not "Pre-release mode active" which is product voice, not artist voice | V8 | 5 | 2 | M | 2 |
| 28 | Live mode copy on profile: in live state, the hero area must emphasise the release; the stream CTA must read "Listen now" not "Stream" or "Play" — "Listen now" is direct and human | V8 | 4 | 1 | L | 2 |
| 29 | Gig mode copy on profile: the "on tonight" tag must read "I'm on tonight" (first-person artist voice), not "Gig mode" or "Tonight" — verify the `gig` state chip in the hero renders the artist-voice string | V8 | 5 | 1 | L | 2 |
| 30 | Profile state "profile" copy: the default state must present the artist's most recent release with a stream CTA, not show "Profile mode" or any mode indicator — the artist's identity is the default, not a named state | V8 | 4 | 2 | M | 2 |
| 31 | Fan capture CTA button per state: the sign-up button copy must change by campaign state — profile: "Stay close." / pre-release: "Hear it first." / live: "I'll let you know what's next." / gig: "I'm here tonight." — verify `updateFanCapture(state)` sets the correct button copy per state | V8 | 5 | 2 | M | 2 |
| 32 | Fan trust text: below the sign-up form, the trust line must read "Just the artist. No spam." — not "We'll never share your email" (platform voice) or "Subscribe for updates" (too newsletter-ish) | V8 | 4 | 1 | L | 2 |
| 33 | Fan confirmation email copy: the confirmation email arrives from the artist, not ABLE — the subject line must reference the artist name, not "ABLE notification"; the body must be in the artist's voice | FN | 5 | 2 | M | 2 |
| 34 | Vocabulary consistency — "Snap cards": audit all 4 pages and all copy for "snap cards" vs "updates" vs "moments" — decide on one term per CONTEXT.md D10 and replace all instances; the term "Snap cards" is used in code; "Updates" may appear in copy | ALL | 4 | 2 | L | 3 |
| 35 | Vocabulary consistency — "Campaign": "Campaign HQ" is an internal admin label — it must not appear anywhere on the artist-facing profile or in fan-facing copy | V8 | 3 | 1 | L | 3 |
| 36 | Vocabulary consistency — "Profile": "profile mode" or "profile state" must never appear in fan-facing copy — the fan never needs to know what state the page is in | V8 | 3 | 1 | L | 3 |
| 37 | Vocabulary consistency — "tier": do not use "tier" in user-facing copy — use "your plan" instead ("Your plan includes...") | ALL | 3 | 1 | L | 3 |
| 38 | Vocabulary consistency — "ABLE": always all-caps — grep for " Able " (with spaces) in all HTML — zero instances allowed | ALL | 3 | 1 | L | 3 |
| 39 | Upgrade copy: every gold lock overlay must name what the artist gets — "Snap cards for your biggest moments. Artist plan." not "Upgrade to unlock" — verify all `checkTierGate()` overlay copy in admin.html | ADM | 5 | 2 | L | 3 |
| 40 | Fan cap upgrade copy: the fan cap progress bar upgrade CTA must read something direct ("You're at 80 of 100. When you're ready, the Artist plan removes the cap.") not "Upgrade to keep growing" | ADM | 5 | 1 | L | 3 |
| 41 | Broadcast gate copy: the broadcast panel gold lock must read "Send to everyone who signed up. Artist Pro." — not "Upgrade for email broadcasts" | ADM | 4 | 1 | L | 3 |
| 42 | Analytics gate copy: the advanced analytics gold lock must read "See where your fans come from and which CTAs they tap. Artist Pro." | ADM | 4 | 1 | L | 3 |
| 43 | CRM gate copy: if fan CRM full features are gated, the overlay must read "Your full fan list with sign-up source and campaign state. Artist Pro." | ADM | 4 | 1 | L | 3 |
| 44 | Landing pricing copy: the free tier CTA must read "Your page is free →" — the Artist plan CTA must read "Start free, pay monthly" — the Artist Pro CTA must read "Everything in Artist, plus broadcasts" — no "Most popular" badge | LND | 4 | 1 | L | 3 |
| 45 | Landing features copy: the competitive feature table closing row must be "You own the emails. Forever." — confirmed correct per session 17; regression-check after any landing.html table edit | LND | 4 | 1 | L | 3 |
| 46 | Landing FAQ: no "convert" (banned word per STATUS.md session 17) — grep landing.html FAQ for "convert" — zero instances | LND | 3 | 1 | L | 3 |
| 47 | Onboarding step 0 message: "A few details first." — not "Let's get started" (confirmed fix) — regression-check | STR | 4 | 1 | L | 3 |
| 48 | Onboarding step labels: each step label must describe what the artist is doing ("Your sound", "Who you are", "Your release") not what the system is doing ("Step 1: Identity configuration") | STR | 4 | 2 | L | 3 |
| 49 | Onboarding hint copy: every field hint in the wizard must be written in the artist's frame of reference — "What do people call you?" not "Enter your artist name" | STR | 3 | 2 | L | 3 |
| 50 | Onboarding import copy: the Spotify import step must read "Found you on Spotify" (if successful) or "Paste your Spotify link" — not "Import your Spotify profile data" | STR | 3 | 1 | L | 3 |
| 51 | Admin "Profile completeness" signal: the first-run checklist items must use direct language — "Add your name" / "Choose your accent colour" / "Add a CTA" — not "Complete step 1: Profile configuration" | ADM | 4 | 2 | L | 4 |
| 52 | Admin section headers: all admin section headers (Campaign HQ, Your Fans, Snap Cards, Your World, Connections) must match the exact copy in the spec — verify against `docs/pages/admin/DESIGN-SPEC.md` | ADM | 3 | 2 | L | 4 |
| 53 | "Your World" moments panel copy: the world map empty state must read something like "Add your first show and it'll appear on the map." — not "No moments yet" | ADM | 3 | 1 | L | 4 |
| 54 | Connections panel empty state: must read "When you connect your platforms (Spotify, Apple, Bandcamp), your stats appear here." — not "No connections" | ADM | 3 | 1 | L | 4 |
| 55 | Snap card empty state on profile: if no snap cards configured, the section must be hidden entirely — not show "No updates yet" which is a fan-facing message for an artist-configured section | V8 | 4 | 1 | L | 4 |
| 56 | Events empty state on profile (fan view): must be hidden when no shows — not "No upcoming shows" which implies the artist is inactive | V8 | 4 | 1 | L | 4 |
| 57 | Events empty state on profile (owner view): must show "Add a show →" placeholder card — not "No events added" | V8 | 3 | 1 | L | 4 |
| 58 | Music section empty state (owner view): must show "Add your release →" placeholder — not "No releases added" or "Your music will appear here" | V8 | 3 | 1 | L | 4 |
| 59 | Support section empty state (owner view): must show "Set up support →" link to admin — not "Support section disabled" | V8 | 2 | 1 | L | 4 |
| 60 | Recommendations section copy: verify recommendations section heading uses the correct spec copy — not "People like you might also like" (algorithmic framing) but something artist-voice like "Who else I'd point you to" | V8 | 3 | 1 | L | 4 |
| 61 | Pre-release countdown heading: must be artist-voice — "It's coming." or "{Title} drops {date}." — not "Countdown to release" | V8 | 5 | 1 | L | 4 |
| 62 | Pre-save CTA copy: must read "Save it now." or "Add it to your library." — not "Pre-save" (industry jargon) or "Save release" | V8 | 4 | 1 | L | 4 |
| 63 | Live release CTA copy: "Listen now." — not "Stream now" or "Play" | V8 | 4 | 1 | L | 4 |
| 64 | Gig tickets CTA copy: "Get your ticket." — not "Buy tickets" or "Tickets available" | V8 | 4 | 1 | L | 4 |
| 65 | Platform pills copy: platform pills should show the platform name only — "Spotify", "Apple Music", "Bandcamp" — not "Listen on Spotify" or "Follow on Spotify" (too long for a pill) | V8 | 2 | 1 | L | 4 |
| 66 | Merch CTA: "Get this." or link copy matching the merch item name — not "Buy now" or "Purchase" | V8 | 3 | 1 | L | 4 |
| 67 | Admin save feedback: when the artist saves their profile, the toast must read "Saved." (period, not exclamation) — not "Profile saved!" or "Changes saved!" | ADM | 3 | 1 | L | 4 |
| 68 | Admin delete confirmation: destructive actions must confirm with "Remove [item]?" not "Are you sure you want to delete this?" — shorter is less alarming and more in voice | ADM | 3 | 2 | M | 4 |
| 69 | Tour announcements tone: if a shows announcement mechanism exists (auto snap card from add show), the generated copy must be artist-voice: "I'll be at {venue} on {date}." — not "New show announced!" | ADM | 4 | 2 | M | 5 |
| 70 | Bio placeholder text: the bio textarea placeholder must read "What do you want people to know about you?" — not "Write your bio here" or "Tell fans about yourself" | ADM | 3 | 1 | L | 5 |
| 71 | CTA URL placeholder: the URL input in admin CTA form must have a practical placeholder — "https://open.spotify.com/…" — not "Enter URL" | ADM | 2 | 1 | L | 5 |
| 72 | CTA label placeholder: the label input for a custom CTA must have a placeholder like "Stream now" or "Listen here" — not "Button text" | ADM | 2 | 1 | L | 5 |
| 73 | Snap card title placeholder: "What's happening?" — not "Title" | ADM | 2 | 1 | L | 5 |
| 74 | Show venue placeholder: "Fabric, London" — not "Venue name" | ADM | 2 | 1 | L | 5 |
| 75 | "Dashboard" banned: grep admin.html for the word "dashboard" in user-facing copy — replace with "Your page", "Your list", or equivalent ABLE voice; "Dashboard" is SaaS jargon | ADM | 4 | 2 | L | 5 |
| 76 | "Analytics" framing: the analytics section must not lead with the word "Analytics" as the primary heading — use "What's moving" or the artist-focused framing from the spec | ADM | 3 | 1 | L | 5 |
| 77 | Stats framing: verify the dashboard stats panel shows value context alongside raw numbers — "Your 47 fans reach like 188 followers. 4× more likely to buy tickets." is more motivating than bare "47 fans" | ADM | 5 | 3 | M | 5 |
| 78 | Credits copy: unconfirmed credits on the profile must appear at 70% opacity as plain text — no tooltip or label saying "unverified" which implies distrust — just opacity reduction | V8 | 3 | 1 | L | 5 |
| 79 | QR code label in admin: must read "Put this on your flyers." — not "QR Code" or "Download QR Code" | ADM | 3 | 1 | L | 5 |
| 80 | UTM copy links in admin: the Instagram UTM copy link must show the URL in a readable truncated form — not the full raw URL — with a "Copy" button; the label must read "For your Instagram bio" not "UTM link" | ADM | 3 | 1 | L | 5 |
| 81 | Upgrade bar copy: "47 of 100 fans." period, not exclamation — and "When you're ready, the Artist plan removes the cap." not "Upgrade now to get more fans!" | ADM | 4 | 1 | L | 5 |
| 82 | Campaign HQ heading: "Campaign HQ" is correct and in spec — do not simplify to "Page settings" or "Your page state" | ADM | 3 | 1 | L | 5 |
| 83 | "Export" vs "Download": the fan list export must use the word "Export" not "Download" — "Export" implies portability and ownership which is the ABLE promise | ADM | 2 | 1 | L | 5 |
| 84 | Footer on landing: the landing footer copy must not include "© 2024" — update to "© 2026 ABLE" and verify the year is not hardcoded but dynamically set with `new Date().getFullYear()` | LND | 3 | 1 | L | 5 |
| 85 | Landing social proof: if any social proof strip exists ("Artists are already using ABLE"), it must use real numbers or be removed entirely — fake or placeholder numbers destroy trust | LND | 5 | 2 | M | 5 |
| 86 | `privacy.html` voice: the privacy policy is a legal document but must still use plain English ("We store your email address and when you signed up") not legalese — ensure the GDPR notice reads as direct, honest communication | V8 | 4 | 2 | L | 6 |
| 87 | `terms.html` voice: same principle — plain language, no passive voice, direct statements about rights | V8 | 3 | 2 | L | 6 |
| 88 | GDPR consent text on fan capture: the visible consent text must read "Just the artist. No spam." — but this is NOT GDPR-compliant Article 7 consent on its own — the text must be: "By signing up, you agree to receive updates from [Artist Name]. ABLE stores your email on their behalf. [Privacy policy link]" | V8 | 5 | 2 | M | 6 |
| 89 | Fan unsubscribe copy: the unsubscribe link at the bottom of every fan email must read "Stop getting updates" — not "Unsubscribe" which sounds like a newsletter, not an artist relationship | FN | 3 | 1 | L | 6 |
| 90 | Onboarding progress copy: the step indicator copy (e.g. "Step 2 of 4") must appear but never say "You're halfway there!" — just the step number | STR | 2 | 1 | L | 6 |
| 91 | Clips section heading on profile: must be artist-voice — "Recent clips" or simply the word the spec uses — not "Video clips" or "My content" | V8 | 3 | 1 | L | 6 |
| 92 | "Sign in" vs "Log in": landing.html uses "Sign in" — verify this is consistent across all pages and emails | ALL | 2 | 1 | L | 6 |
| 93 | Loading copy: if any loading state has text, it must be minimal — "Loading…" is fine, "Please wait while we load your profile…" is not | ALL | 2 | 1 | L | 6 |
| 94 | No em-dash to replace real content: verify no placeholder copy uses "—" (em dash) where actual copy should be — a common sign of unfinished copy in a template | ALL | 2 | 1 | L | 6 |
| 95 | World map copy: "Your World" is the correct section header — verify this is in place and not "Your Gig Map" or "Tour Map" | ADM | 2 | 1 | L | 6 |
| 96 | Recommendations heading on profile: the artist-curated recommendations section heading must be in first person — "Who else I'd point you to" or the spec-approved variant | V8 | 3 | 1 | L | 6 |
| 97 | Admin section "Your Fans": the correct heading is "Your Fans" not "Fan list" or "Fans" — verify it matches the spec | ADM | 2 | 1 | L | 6 |
| 98 | Copy review process: before any new copy string is committed to any page, it must pass a three-question check: (1) Would a credible artist be embarrassed by this? (2) Does it sound like a startup? (3) Does it use any banned word? — document this as a required pre-commit step | ALL | 4 | 1 | L | 6 |
| 99 | `docs/systems/copy/SPEC.md` audit: verify all copy strings currently live on each page are listed in the spec — any string not in the spec is undocumented and at risk of being overwritten with incorrect copy in future sessions | ALL | 3 | 3 | L | 6 |
| 100 | Banned phrase regression test: add a Playwright test that greps the rendered DOM of each page for banned phrases ("superfan", "grow your audience", "monetise", "content creator", "viral", "Get started!", "You're all set") — this should be a CI-level check that catches regressions automatically | ALL | 5 | 3 | L | 6 |

---

## Phase 5 — Typography & Spacing
*Type is the first thing that signals quality. Inconsistent spacing makes a premium product look amateur.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | Mobile type minimum: grep all 4 pages for `font-size` values below `14px` — any value below `14px` is at risk of being unreadable; any below `12px` is WCAG non-compliant for meaningful text | ALL | 5 | 1 | L | 1 |
| 2 | iOS auto-zoom minimum: all form inputs must have `font-size: 16px` or larger to prevent iOS Safari viewport zoom — grep for `font-size: 14px` and `font-size: 15px` inside `input`, `textarea`, `select` rules | ALL | 5 | 1 | L | 1 |
| 3 | Type scale definition: verify `--text-xs` through `--text-5xl` tokens are defined in `shared/tokens.css` or at `:root` in each page — any font-size value not referenced via a token is a drift risk | ALL | 4 | 2 | L | 1 |
| 4 | Admin typography scale: admin.html has been noted (GPT review, UI score 8.0) as lacking a formal type hierarchy — add `--dash-text-xs` (11px), `--dash-text-sm` (13px), `--dash-text-base` (15px), `--dash-text-lg` (17px), `--dash-text-xl` (20px) tokens to the `--dash-*` namespace and apply consistently | ADM | 5 | 3 | M | 1 |
| 5 | Admin section titles: all admin section headers (Campaign HQ, Your Fans, Snap Cards) must use Barlow Condensed — `font-family: var(--font-d)` — this is the single most-impactful typography change for admin quality (GPT review confirmed) | ADM | 5 | 2 | M | 1 |
| 6 | Admin body text: admin body text must use Plus Jakarta Sans — `font-family: var(--font)` — verify no admin text is falling back to the system default sans-serif | ADM | 4 | 1 | L | 1 |
| 7 | Profile display font: artist name in the hero must use Barlow Condensed — `font-family: var(--font-display)` — verify `applyIdentity()` or the hero render does not reset this to DM Sans | V8 | 4 | 1 | L | 1 |
| 8 | Profile body text: all body copy on able-v8.html must use DM Sans — `font-family: var(--font)` — no Plus Jakarta Sans or system font on the artist profile | V8 | 4 | 1 | L | 1 |
| 9 | Font cross-contamination: grep admin.html for `var(--font)` (DM Sans) and grep able-v8.html for `'Plus Jakarta Sans'` — zero instances of the wrong font on the wrong surface | ALL | 4 | 1 | L | 1 |
| 10 | Landing font: landing.html uses a different editorial font (Fraunces or similar) — document this as deliberate in the brand identity spec, or align it with Barlow Condensed for display and DM Sans for body — the current gap creates a quality discontinuity on the landing→onboarding transition | LND | 4 | 2 | M | 1 |
| 11 | Font loading — unused weights: verify Google Fonts `@import` for DM Sans only loads weights actually used (400, 500, possibly 600) — if the current `@import` loads 300,400,500,600,700, the 300 and 700 weights are likely unused and add ~20KB to the font load | V8 | 3 | 1 | L | 2 |
| 12 | Font loading — Plus Jakarta Sans: same audit for admin.html — load only 400, 500, 600 if those are the only weights used | ADM | 3 | 1 | L | 2 |
| 13 | Font loading — Barlow Condensed: load only 600 and 700 (the weights used for hero names and stat values) — not the full variable range | ALL | 3 | 1 | L | 2 |
| 14 | `font-display: swap`: verify all Google Fonts `@import` URLs include `&display=swap` — without this, text is invisible until fonts load (FOIT), causing a blank page flash on slow connections | ALL | 5 | 1 | L | 2 |
| 15 | `font-display: optional` for display fonts: Barlow Condensed (hero names, stat values) could use `display=optional` — if it doesn't load before paint, fall back to the system font rather than flashing unstyled | ALL | 3 | 2 | M | 2 |
| 16 | Spacing scale — `--sp-N` tokens: verify all padding, margin, and gap values across all 4 pages use `var(--sp-N)` tokens from the 4px grid — grep for raw `px` values in `padding`, `margin`, `gap` rules (excluding borders and shadows) | ALL | 4 | 3 | M | 2 |
| 17 | Admin spacing rhythm: section spacing between Campaign HQ, stats, fan list, snap cards must be 32px (`var(--sp-8)`) — GPT review noted 20-24px is too dense; the extra 8px makes the dashboard breathe without looking sparse | ADM | 5 | 2 | M | 2 |
| 18 | Profile section rhythm: vertical gap between hero, music, events, merch, snap cards, support sections on able-v8.html must use consistent `--sp-12` (48px) or `--sp-10` (40px) — not a mix of `32px`, `40px`, and `48px` | V8 | 4 | 2 | M | 2 |
| 19 | Landing section rhythm: the proof strip → features → pricing sections on landing.html must breathe — verify `--sp-16` (64px) or `--sp-20` (80px) between major sections, not cramped `32px` | LND | 4 | 2 | M | 2 |
| 20 | Wizard step spacing: internal spacing between form fields in start.html must be consistent — `--sp-5` (20px) or `--sp-6` (24px) gap between fields; verify not a mix of `16px`, `20px`, `24px` raw values | STR | 3 | 2 | M | 2 |
| 21 | Admin card padding: all admin cards must use exactly `--sp-5` (20px) padding — GPT review recommended a single standard (14px radius, 10% opacity border, 18-20px padding) to unify the 5 card variants | ADM | 5 | 3 | M | 2 |
| 22 | Admin card border-radius: all admin cards must use `--r-lg` (14px) — verify no card uses `8px`, `10px`, or `16px` raw values | ADM | 4 | 2 | M | 2 |
| 23 | Profile card border-radius: all profile section cards must use `--r-lg` or `--r-xl` — verify consistency across release cards, snap cards, events bento grid | V8 | 3 | 2 | M | 2 |
| 24 | Line height — display text: Barlow Condensed hero names must use `line-height: 1.0` or `line-height: 1.1` (tight) — verify not defaulting to the body `line-height: 1.6` which would double-space the hero name | V8 | 4 | 1 | L | 3 |
| 25 | Line height — body text: bio text, snap card body, FAQ answers must use `line-height: 1.6` or `line-height: var(--lh-reading)` — verify no body text block has `line-height: 1.2` (too tight to read) | ALL | 4 | 1 | L | 3 |
| 26 | Line height — stat values: large stat numbers (fan count, click count) in admin must use `line-height: 1.0` — a stat value of "2,847" with `line-height: 1.6` wastes vertical space | ADM | 2 | 1 | L | 3 |
| 27 | Paragraph measure — bio: artist bio text on the profile must be constrained to `max-width: 65ch` or `max-width: 580px` — a bio that spans 100% of a 430px screen is ~75 characters wide which is at the upper readable limit; add a max-width to the bio container | V8 | 4 | 2 | M | 3 |
| 28 | Paragraph measure — landing: landing page body copy (feature descriptions, FAQ answers) must be constrained to `max-width: 70ch` — without this constraint, text on a wide desktop screen becomes unreadable | LND | 3 | 1 | L | 3 |
| 29 | Paragraph measure — admin: admin text panels (campaign mode descriptions, nudge card copy) must be constrained to `max-width: 60ch` — narrower measure for shorter explanatory text | ADM | 3 | 1 | L | 3 |
| 30 | Eyebrow text letter-spacing: all uppercase label text (section headers, stat labels, pill labels) must use `letter-spacing: 0.08em` to `0.12em` — verify not using `1px` or `2px` raw pixel letter-spacing | ALL | 3 | 2 | L | 3 |
| 31 | Stat label letter-spacing: admin stat labels ("FANS", "CLICKS", "VIEWS") must use consistent `letter-spacing: 0.1em` — not a mix of `0.08em` and `0.12em` | ADM | 2 | 1 | L | 3 |
| 32 | Eyebrow text transform: uppercase label text must use `text-transform: uppercase` not all-caps manually typed — this allows the source copy to remain natural case in HTML | ALL | 3 | 1 | L | 3 |
| 33 | Hero name font-size at 375px: artist name in Barlow Condensed must scale from 48px (default) to a maximum of 56px on wider screens — verify no hero name breaks to 3+ lines at 375px even for long names (20+ chars); add `max-font-size` clamp or word-break handling | V8 | 4 | 2 | M | 3 |
| 34 | Hero name overflow: `A11` artist name scale compression (lerp 48px→24px on scroll) must not cause the name to overflow the sticky bar container at any intermediate size — verify the compression animation has `overflow: hidden` on the container | V8 | 3 | 2 | M | 3 |
| 35 | Countdown font-size: the pre-release countdown number must use `--text-5xl` or `--text-4xl` Barlow Condensed — verify the countdown ring number is not a raw `64px` or `80px` value | V8 | 3 | 1 | L | 3 |
| 36 | Landing headline font-size: the landing hero headline must be at least `--text-4xl` (or `48px`) on desktop and scale down to `--text-3xl` (or `36px`) on mobile — verify this is not set as a fixed `48px` that doesn't scale | LND | 4 | 2 | M | 3 |
| 37 | Start.html wizard step font-size: step instructions must use `--text-lg` or `--text-xl` — not `24px` raw value which would not scale with the token system | STR | 3 | 1 | L | 3 |
| 38 | Admin stats value font-size: the large stat values (fan count, click count) must use Barlow Condensed at `--text-4xl` or `--text-5xl` — verify not using Plus Jakarta Sans for stat numbers | ADM | 4 | 1 | L | 4 |
| 39 | `clamp()` for fluid type: the hero artist name should use `font-size: clamp(36px, 12vw, 56px)` so it scales smoothly from 375px to 430px without an abrupt media query breakpoint | V8 | 3 | 2 | M | 4 |
| 40 | `clamp()` for landing headline: the landing hero headline should use `font-size: clamp(32px, 8vw, 56px)` for fluid scaling | LND | 3 | 2 | M | 4 |
| 41 | Truncation consistency: all truncated text (release titles, merch names, snap card titles) must use `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` — not CSS `-webkit-line-clamp` which behaves differently across browsers for single-line truncation | ALL | 3 | 2 | L | 4 |
| 42 | Multi-line clamp: for bio text previews on admin (if bio is shown in condensed form), use `-webkit-line-clamp: 3` with the full standard syntax — not a custom JS truncation | ADM | 2 | 1 | L | 4 |
| 43 | Token documentation: every `--text-*`, `--sp-*`, `--lh-*`, `--ls-*` token used in `shared/tokens.css` must have a comment documenting its intended use — undocumented tokens get used in unintended ways by future build sessions | ALL | 3 | 2 | L | 4 |
| 44 | Font stack fallback: the DM Sans Google Fonts import must have a fallback stack: `font-family: 'DM Sans', system-ui, -apple-system, sans-serif` — if Google Fonts fails to load, the page degrades to a clean system font, not Comic Sans | V8 | 3 | 1 | L | 4 |
| 45 | Admin font stack fallback: Plus Jakarta Sans must have `font-family: 'Plus Jakarta Sans', system-ui, -apple-system, sans-serif` | ADM | 3 | 1 | L | 4 |
| 46 | Barlow Condensed fallback: `font-family: 'Barlow Condensed', 'Arial Narrow', sans-serif` — `Arial Narrow` is the closest system font for display text | ALL | 2 | 1 | L | 4 |
| 47 | Admin card spacing audit: `--sp-5` (20px) internal padding should be consistent across all admin cards — Campaign HQ card, stats card, fan list card, snap cards, connections — currently 5 variants; unify to one | ADM | 5 | 3 | M | 4 |
| 48 | Section header bottom margin: every section header (Campaign HQ, Your Fans) must have `margin-bottom: var(--sp-4)` (16px) between the header and the first content item — verify no section has `margin-bottom: 8px` (too tight) or `margin-bottom: 24px` (too loose) | ADM | 3 | 2 | L | 4 |
| 49 | Button text size: all primary and secondary buttons must use `--text-sm` (14px) or `--text-base` (15-16px) — not `12px` (too small to read on mobile) or `18px` (too large for a button) | ALL | 3 | 1 | L | 5 |
| 50 | Input label size: form field labels in admin.html and start.html must use `--text-sm` (13-14px) — not `12px` (hard to read) | ALL | 3 | 1 | L | 5 |
| 51 | Input field font-size: all form inputs must use `--text-base` (16px) for the value text — both for legibility and iOS auto-zoom prevention | ALL | 5 | 1 | L | 5 |
| 52 | Placeholder text opacity: input placeholder text must be at `opacity: 0.5` or `color: var(--dash-t3)` — not `opacity: 0.35` which is below the 3:1 contrast ratio for placeholder text on some themes | ALL | 3 | 1 | L | 5 |
| 53 | `--sp-N` grid consistency: the 4px grid means valid values are 4, 8, 12, 16, 20, 24, 28, 32, 40, 48, 64 — grep for any spacing value that is not on this grid (e.g. `padding: 10px`, `margin: 18px`) and correct to the nearest grid value | ALL | 3 | 3 | M | 5 |
| 54 | Mobile section padding: all page sections must have `padding-inline: var(--sp-5)` (20px) at 375px — no section should bleed to the screen edge | ALL | 4 | 2 | M | 5 |
| 55 | Admin topbar height: the admin topbar must have a consistent height — likely 56px or `--sp-14` — verify this is tokenised and not set as `height: 56px` raw | ADM | 2 | 1 | L | 5 |
| 56 | Admin bottom nav height: 60px or `--sp-15` — verify consistent across pages that use the nav | ADM | 2 | 1 | L | 5 |
| 57 | Icon size consistency: all icons in admin.html must use the same size token — `20px` for inline icons, `24px` for nav icons — verify not a mix of `18px`, `20px`, `22px`, `24px` raw values; `1.5px` stroke weight throughout | ADM | 3 | 2 | M | 5 |
| 58 | Icon size on profile: all icons on able-v8.html (platform pill icons, section header icons) must be consistently `20px` — not a mix of sizes | V8 | 2 | 1 | L | 5 |
| 59 | Landing features section weight: landing features section cards must have `24px` card padding and `13px` label size — GPT review noted the features section is underweight relative to the hero; tightening the type and padding makes it feel more considered | LND | 4 | 2 | M | 5 |
| 60 | Landing pricing card spacing: the three pricing cards must have equal padding and consistent type scale — verify the Artist card (middle) has exactly the same structure as Free and Pro | LND | 3 | 2 | M | 5 |
| 61 | Fan.html tab bar: 12px tab labels are too small — increase to 13px; strengthen selected state weight to 600 — GPT review noted this specifically | FN | 3 | 1 | L | 5 |
| 62 | Barlow Condensed usage rule: never use Barlow Condensed for body copy or paragraph text — only for hero names, large stats, countdown numbers, section titles in admin — document this in `shared/tokens.css` as a comment | ALL | 4 | 1 | L | 5 |
| 63 | Uppercase tracking on profile: section headers on able-v8.html (MUSIC, EVENTS, MERCH) must use consistent `letter-spacing: 0.1em` — not a mix of `0.05em` and `0.15em` | V8 | 2 | 1 | L | 5 |
| 64 | Snap card body line height: snap card body text must use `line-height: 1.5` — tighter than paragraph text because snap cards are short, punchy content | V8 | 2 | 1 | L | 5 |
| 65 | Release card title size: release card title must use `--text-base` (15-16px) Barlow Condensed or DM Sans at medium weight — not the same size as the artist name | V8 | 2 | 1 | L | 5 |
| 66 | Events date format: show dates must be formatted as "Mon 14 Apr" (abbreviated day + date + abbreviated month) at `--text-sm` — consistent across the profile events section and admin world map | V8 | 2 | 1 | L | 5 |
| 67 | Tab font-size on admin nav: admin bottom nav labels must be `12px` (correct for a mobile nav) — verify not `10px` (too small) or `14px` (too big) | ADM | 3 | 1 | L | 6 |
| 68 | Spacing between hero CTAs: the two hero CTAs must have `--sp-3` (12px) gap — not `8px` (too tight, looks like one element) or `16px` (too loose) | V8 | 3 | 1 | L | 6 |
| 69 | Spacing between platform pills: Quick Action pills must have `--sp-2` (8px) gap — consistent; verify not `4px` which makes them feel crowded | V8 | 3 | 1 | L | 6 |
| 70 | Section header → first item spacing: on able-v8.html, the gap between a section header and the first card below it must be `--sp-4` (16px) — not `8px` (too tight) or `24px` (breaks the visual connection) | V8 | 3 | 1 | L | 6 |
| 71 | Stat card internal spacing: stat value and stat label within each stat card must have `--sp-1` (4px) gap — tight pairing communicates they are a unit | ADM | 2 | 1 | L | 6 |
| 72 | `word-break: break-word` on all text containers: all card and section text containers must have `word-break: break-word` to prevent long URLs or unbroken words from overflowing | ALL | 3 | 1 | L | 6 |
| 73 | Barlow Condensed weight 700: stat values in admin and hero names on profile must use `font-weight: 700` (bold) Barlow Condensed — `font-weight: 400` Barlow Condensed looks too light for display use | ALL | 3 | 1 | L | 6 |
| 74 | Plus Jakarta Sans weight in admin: admin body text should use weight 400; labels weight 500; section values weight 600 — verify not all defaulting to weight 400 | ADM | 3 | 1 | L | 6 |
| 75 | DM Sans weight on profile: bio text weight 400; CTA button labels weight 500; section labels weight 500 — verify correct weights applied | V8 | 3 | 1 | L | 6 |
| 76 | First-run checklist type: the first-run checklist items in admin must use `--text-sm` DM-San... Plus Jakarta Sans at weight 400 — not larger or heavier which would compete with Campaign HQ | ADM | 2 | 1 | L | 6 |
| 77 | Nudge card type: nudge cards in admin must use the same type treatment as regular cards — `--dash-text-sm` body, `--dash-text-base` title | ADM | 2 | 1 | L | 6 |
| 78 | Landing comparison table type: the feature comparison table must use `--text-sm` for feature names and tick/cross icons at `16px` — not large text that makes the table feel heavy | LND | 3 | 2 | M | 6 |
| 79 | `shared/tokens.css` sync: run a grep across all 4 pages for `font-size`, `line-height`, `letter-spacing`, `padding`, `margin`, `gap` values that are NOT using CSS custom properties — this is the definitive check that the type and spacing system is fully tokenised | ALL | 4 | 3 | L | 6 |
| 80 | Typography documentation: for each page, maintain a type inventory in the relevant `DESIGN-SPEC.md` — which text elements use which token — this prevents drift across sessions | ALL | 3 | 3 | L | 6 |
| 81 | Admin Barlow Condensed for headings: "Campaign HQ" heading, "Your Fans" heading, "Snap Cards" heading must all use `font-family: var(--font-d)` at `font-size: var(--dash-text-xl)` and `font-weight: 700` | ADM | 5 | 2 | M | 6 |
| 82 | Admin label size: admin stat labels ("FANS", "CLICKS", "VIEWS") must be `--dash-text-xs` (11px) uppercase — verify not `10px` (too small) or `14px` (too large for a label) | ADM | 3 | 1 | L | 6 |
| 83 | Mobile font-size clamp: landing hero headline should use `clamp(28px, 8vw, 52px)` to scale from 375px to 1440px without abrupt breakpoints | LND | 3 | 2 | M | 6 |
| 84 | Wizard done screen headline: "Your page is live." must be set at `--text-4xl` (or `40px`) Barlow Condensed — large enough to feel like a moment | STR | 4 | 1 | L | 6 |
| 85 | Admin connections panel type: connected platform labels must use `--dash-text-sm` DM... Plus Jakarta Sans at weight 500 — consistent with other admin label treatments | ADM | 2 | 1 | L | 6 |
| 86 | Horizontal rule spacing: any `<hr>` or visual divider between admin sections must use `margin-block: var(--sp-6)` (24px) — not `1px` lines with `margin: 0` that add no visual breathing room | ADM | 2 | 1 | L | 6 |
| 87 | Snap card title type on profile: snap card titles must use `--text-base` DM Sans at weight 600 — the title is the primary hook; too-light weight (400) makes them forgettable | V8 | 3 | 1 | L | 6 |
| 88 | Show/hide animation spacing: when expanding a section (e.g. accordion in admin) the spacing animation must use CSS `max-height` transition with `overflow: hidden` — not a JS-driven height calculation that causes layout jank | ADM | 3 | 2 | M | 6 |
| 89 | Monospaced values: UTM link URLs displayed in admin (copy links for Instagram/TikTok bio) should use `font-family: monospace` at `--text-xs` — this clearly signals "this is a URL" without needing a label | ADM | 2 | 1 | L | 6 |
| 90 | Code-like elements: if any copy shows a clipboard-copy URL (QR code URL, UTM link), it must be in a code-like treatment — `background: var(--dash-bg-2)`, monospace font, `border-radius: var(--r-sm)` | ADM | 2 | 1 | L | 6 |
| 91 | Letter-spacing on Barlow: Barlow Condensed headlines should have `letter-spacing: -0.02em` (slight negative tracking) at large sizes (40px+) — tight tracking on condensed sans makes display text feel intentional rather than loose | V8 | 3 | 1 | L | 6 |
| 92 | Letter-spacing on DM Sans body: body text should use default letter-spacing (0em) — no positive letter-spacing on body text sizes as it reduces legibility | ALL | 2 | 1 | L | 6 |
| 93 | Line length on error messages: error messages must be constrained to `max-width: 40ch` — short errors on a long line look like they belong to a different element | ALL | 2 | 1 | L | 6 |
| 94 | Tooltip type: any tooltip text (if used) must be `--text-xs` at a minimum 4.5:1 contrast — small tooltip text on a dark background is a frequent contrast failure | ALL | 3 | 1 | L | 6 |
| 95 | Orphan prevention: landing.html long-form text (FAQ answers, feature descriptions) should avoid single-word final lines — use `&nbsp;` or `text-wrap: balance` on paragraph headings | LND | 2 | 1 | L | 6 |
| 96 | `text-wrap: balance` on headings: apply `text-wrap: balance` to all heading elements (h1, h2, h3) on landing.html for more even line breaks on responsive layouts | LND | 3 | 1 | L | 6 |
| 97 | Admin greeting font: the greeting "Good to see you, [Name]." must use Barlow Condensed for the artist name part and Plus Jakarta Sans for the rest, OR use Barlow Condensed for the whole greeting — not mixing fonts mid-sentence | ADM | 3 | 2 | M | 6 |
| 98 | Type scale documentation in tokens.css: add a comment block to `shared/tokens.css` mapping each `--text-*` token to its intended use: "text-xs: 11px — stat labels, captions, legal" etc. | ALL | 3 | 2 | L | 6 |
| 99 | Spacing scale documentation: same as above for `--sp-*` tokens — document the 4px grid and intended uses for each increment | ALL | 3 | 2 | L | 6 |
| 100 | Full type audit: run a Playwright screenshot of each page at 375px and 1440px, then overlay a grid and manually verify all text elements sit on the type scale — this is the definitive closure check for Phase 5 | ALL | 4 | 3 | L | 6 |

---

## Phase 6 — Colour & Contrast
*WCAG AA is a legal baseline, not a target. Every contrast failure is also a trust failure for any artist whose audience includes fans with visual impairments.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | `--dash-t3` WCAG floor: `--dash-t3: #767676` on `--dash-card: #f8f5f0` (cream) must achieve 4.5:1 — calculate: #767676 on #f8f5f0 = 4.52:1 which just passes; verify this hasn't drifted from any recent admin.html edit | ADM | 5 | 1 | L | 1 |
| 2 | `--dash-t3` in daylight: cream (#f8f5f0) + #767676 text in outdoor sun appears lower contrast due to glare — consider increasing to #717171 (4.6:1) as a safety margin, while staying above the 4.5:1 floor | ADM | 4 | 1 | L | 1 |
| 3 | Accent colour on dark theme: verify the default artist accent (#e05242) on `--color-card: #12152a` achieves 4.5:1 — calculate: #e05242 on #12152a ≈ 3.9:1, which FAILS for normal text; for large text (18px+ or 14px bold) the 3:1 threshold applies and #e05242 passes — verify accent is never used for sub-18px normal-weight body text | V8 | 5 | 2 | M | 1 |
| 4 | Accent minimum luminance constraint: `applyDerivedTokens()` must enforce a minimum contrast for the artist-set accent against the page background — if an artist sets a dark accent (#1a1a2e) on a dark background, the fan capture button becomes invisible; clamp luminance to ensure at least 3:1 on large text | V8 | 5 | 2 | M | 1 |
| 5 | Fan capture button text on accent: the fan capture submit button uses `--color-accent` background — the text on it uses `--color-on-accent` derived by `applyDerivedTokens()` — verify the derivation always produces a colour with 4.5:1 contrast against the artist's chosen accent | V8 | 5 | 2 | M | 1 |
| 6 | Hero CTA primary button: primary CTA uses accent fill — verify `--color-on-accent` text achieves 4.5:1 on the accent background for all 7 default vibe accents | V8 | 5 | 1 | L | 1 |
| 7 | Focus ring visibility dark theme: the focus ring `box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--acc)` — on dark theme, `--bg: #0d0e1a` inner ring and `--acc` (artist accent) outer ring — verify the outer ring is visible against a dark card background, which requires the accent to achieve 3:1 against `#12152a` | V8 | 4 | 1 | L | 1 |
| 8 | Focus ring admin: admin focus ring is `box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--acc)` where `--bg: #09090f` and `--acc: #f4b942` (amber) — #f4b942 on #09090f = approximately 8:1, which passes; verify no admin element overrides the focus ring with a lower-contrast variant | ADM | 4 | 1 | L | 1 |
| 9 | `outline: none` audit: grep all 4 pages for `outline: none` and `outline: 0` — every instance must have a replacement focus indicator (box-shadow focus ring) on the same or parent selector | ALL | 5 | 2 | L | 1 |
| 10 | Light theme body text: on `--color-bg: #f0ede8` (cream), the main body text token must achieve 4.5:1 — if body text is `--color-text: #1a1a2e`, calculate #1a1a2e on #f0ede8 = approximately 13.5:1, which passes — verify no light-theme text rule overrides with a grey that drops below 4.5:1 | V8 | 5 | 1 | L | 1 |
| 11 | Light theme muted text: the secondary/muted text in light theme must achieve 4.5:1 against #f0ede8 — if it's `opacity: 0.68` on `#1a1a2e`, compute the effective colour and verify the contrast ratio; at 68% opacity the effective colour ≈ #6b6b80 on #f0ede8 = approximately 3.8:1 which FAILS — adjust opacity to 0.75 minimum | V8 | 5 | 2 | M | 1 |
| 12 | Glass theme text: in glass theme, text is rendered over a blurred background image — no fixed contrast ratio can guarantee compliance; the text must have a text-shadow or background scrim that ensures 4.5:1 in all conditions — verify a `background: rgba(0,0,0,0.5)` scrim or equivalent is present on all text containers in glass mode | V8 | 5 | 2 | M | 1 |
| 13 | Glass theme fan capture: the fan capture form in glass theme must have a background that ensures the email input field is legible — a fully transparent input on a background image will fail contrast for most images | V8 | 4 | 2 | M | 2 |
| 14 | Contrast theme body text: in contrast theme (`--color-bg: #000000`), all body text must achieve 7:1 (WCAG AAA) — if text is `#ffffff` on `#000000` = 21:1, which passes; verify no element introduces a mid-grey on black that drops below 7:1 | V8 | 4 | 2 | M | 2 |
| 15 | Contrast theme accent: in contrast theme, the artist accent must be overridden to pure white or a high-luminance colour — an artist whose accent is `#e05242` (3.9:1 on #000) fails WCAG AAA; force the accent to `#ffffff` in contrast mode | V8 | 4 | 2 | M | 2 |
| 16 | Contrast theme interactive elements: all interactive elements in contrast theme must meet 3:1 for non-text contrast (WCAG 1.4.11) — button borders, input borders, icon boundaries | V8 | 4 | 2 | M | 2 |
| 17 | Hardcoded hex grep: grep all 4 HTML files for `#[0-9a-fA-F]{3,6}` not inside `:root` or `[data-theme]` variable declarations and not inside SVG fill attributes — every hit is a hardcoded value that must be replaced with a token | ALL | 5 | 3 | M | 2 |
| 18 | Hardcoded `#888` regression: session 11 fixed `--dash-t3 #888888 → #767676` and replaced hardcoded `#888` — verify no new `#888` values have crept back in subsequent sessions; grep for `#888` and `#888888` | ADM | 4 | 1 | L | 2 |
| 19 | Hardcoded `#aaa` regression: session 18 fixed `#aaa → var(--t2/t3)` in admin.html — verify no new `#aaa` instances; grep for `#aaa` and `#aaaaaa` | ADM | 4 | 1 | L | 2 |
| 20 | Admin amber `--acc: #f4b942`: on dark admin background `--bg: #09090f`, amber at 4.5:1 test — #f4b942 on #09090f = approximately 8.2:1 which passes for all text sizes; no change needed, but document this in the design system | ADM | 3 | 1 | L | 2 |
| 21 | Admin amber on card background: `--acc: #f4b942` on `--dash-card: #f8f5f0` (cream) = approximately 1.6:1 which FAILS — amber must NEVER be used as text on the cream card; it is a UI accent only (buttons, active states, indicators) | ADM | 5 | 2 | M | 2 |
| 22 | Admin text on dark background: when admin is in dark mode (`--bg: #09090f`), primary text must be `var(--dash-text)` which is `#1a1a2e`... wait — on dark `--bg: #09090f`, text must be a light value; verify `--dash-text` is correctly defined for both light and dark contexts in admin | ADM | 5 | 2 | M | 2 |
| 23 | Status colours — pre-release: the pre-release indicator colour must achieve 3:1 against the card background for non-text contrast and 4.5:1 for any text label — verify the purple/violet accent used for pre-release mode | V8 | 4 | 2 | M | 2 |
| 24 | Status colours — live: the live release indicator (green accent) must achieve 3:1 against dark background — a mid-green like `#22c55e` on `#12152a` achieves approximately 5.5:1, passing; verify the specific green token used | V8 | 4 | 1 | L | 2 |
| 25 | Status colours — gig: the gig badge glow pulse uses the artist accent — verify the gig badge text achieves 4.5:1 against the badge background (which may be the accent colour itself) | V8 | 4 | 1 | L | 2 |
| 26 | Status colours for colour-blindness: the four campaign states must be distinguishable for deuteranopia (red-green colour blindness) — verify that: (1) live (green) and gig (accent) are differentiated by more than hue alone (e.g. by label text or icon shape), (2) pre-release (purple) and profile (neutral) are distinct | V8 | 4 | 2 | M | 3 |
| 27 | Error state colour: the error colour (`--err` token from start.html session 19) must achieve 4.5:1 against its container background — verify `--err` on white or cream background passes | ALL | 5 | 1 | L | 3 |
| 28 | Warning state colour: `--warn` token (amber-ish warning) must achieve 4.5:1 against its container — if warning is displayed as text, not just as a border/icon | ALL | 4 | 1 | L | 3 |
| 29 | Success state colour: `--ok` token (green) must achieve 4.5:1 against its container background when used for text labels | ALL | 4 | 1 | L | 3 |
| 30 | Bio char count colours: `--err/--ok/--warn` tokens correctly applied to the bio char count in admin (fades amber at 80+ chars, red at 110+) — confirm the red at 110+ is the `--err` token, not a hardcoded `#ff0000` | ADM | 3 | 1 | L | 3 |
| 31 | Light theme — admin: admin.html in light mode (if available) — verify the cream palette (`--dash-card: #f8f5f0`) with `--dash-text: #1a1a2e` achieves 13+:1 contrast, and all interactive element borders achieve 3:1 | ADM | 4 | 2 | M | 3 |
| 32 | Light theme — dark text on profile: in light theme, muted text must not be below 4.5:1 on cream — eyebrow labels, placeholder text, and metadata (release dates, venue names) are the common offenders | V8 | 4 | 2 | M | 3 |
| 33 | Opacity-based colour: all `opacity`-reduced text must be audited — an element with `color: #ffffff; opacity: 0.6` on dark background may compute as an effective grey that fails 4.5:1 — calculate effective contrast for every opacity-reduced text element | ALL | 4 | 2 | M | 3 |
| 34 | Credits 70% opacity: unconfirmed credits render at 70% opacity — if the text colour is `#ffffff` and `opacity: 0.7`, effective colour ≈ `#b3b3b3` on `#12152a` = approximately 5.2:1 which passes; verify this computation | V8 | 3 | 1 | L | 3 |
| 35 | Release card metadata: release year, genre label on release cards — typically `opacity: 0.6` on dark card — compute effective contrast; if it fails, increase opacity to 0.7 | V8 | 3 | 1 | L | 3 |
| 36 | Platform pill text: pill text on dark pill background — verify contrast of pill label against pill background at default and hover states | V8 | 3 | 1 | L | 3 |
| 37 | Platform pill hover state: hover state must not reduce contrast — verify `.pill:hover` rule doesn't lighten text below 4.5:1 | V8 | 3 | 1 | L | 3 |
| 38 | Admin gold lock overlay: gold lock overlay text must be readable — `#f4b942` amber on a semi-transparent blurred Pro feature background — verify contrast | ADM | 4 | 1 | L | 3 |
| 39 | Gig countdown badge: gig countdown timer text on the gig badge — if badge is accent-coloured and text is white, verify white achieves 4.5:1 against all 7 vibe accents | V8 | 4 | 2 | M | 3 |
| 40 | Pre-release ambient intensification: the ambient glow at opacity 0.28 must not reduce the legibility of text layered on top — verify any text element with the ambient glow behind it maintains 4.5:1 | V8 | 3 | 2 | M | 3 |
| 41 | Dark mode media query: if the OS is set to light mode, the profile must respect the artist's chosen theme — verify `@media (prefers-color-scheme: light)` does not override a dark-themed artist's page | V8 | 3 | 2 | M | 4 |
| 42 | Dark mode admin: verify admin.html has a `@media (prefers-color-scheme: dark)` rule that applies the correct dark admin palette — admin's primary background is already dark (`#09090f`) but the light-mode cream cards may need override | ADM | 3 | 2 | M | 4 |
| 43 | System preference → localStorage override: `profile.theme` stored in `able_v3_profile` must override system preference — verify the theme application logic checks localStorage first before reading `prefers-color-scheme` | V8 | 3 | 2 | M | 4 |
| 44 | Focus ring on glass theme: the focus ring `box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--acc)` in glass theme — `--bg` is semi-transparent, so the inner ring may not be visible against the blurred background image — test with a light-coloured background image | V8 | 4 | 2 | M | 4 |
| 45 | Focus ring on light theme profile: on light theme, `--bg: #f0ede8` inner ring and accent outer ring — the accent must achieve 3:1 against the cream background for the ring to be visible | V8 | 4 | 1 | L | 4 |
| 46 | Disabled button contrast: disabled buttons must achieve 3:1 contrast as a non-text UI component — `opacity: 0.4` on a white button on a dark background may reduce contrast below 3:1; use a specific disabled token colour instead | ALL | 4 | 2 | M | 4 |
| 47 | Placeholder text contrast: input placeholder text must achieve 3:1 against the input background (WCAG 1.4.3 applies to placeholder text) — a 50% opacity white on dark input = approximately 2.8:1 which FAILS; use a fixed colour value instead of opacity | ALL | 4 | 2 | M | 4 |
| 48 | Input border contrast: input field borders must achieve 3:1 against their background (WCAG 1.4.11 non-text contrast) — a subtle `1px solid rgba(255,255,255,0.1)` border on dark background may fail; test at all themes | ALL | 4 | 2 | M | 4 |
| 49 | Icon contrast: all icons used as meaningful UI (not decorative) must achieve 3:1 against their background — navigation icons in admin, section header icons on profile, close/dismiss icons | ALL | 4 | 2 | M | 4 |
| 50 | SVG fill colour: inline SVGs with `fill="currentColor"` inherit the current text colour — verify all meaningful SVG icons achieve 3:1 contrast in every theme by checking the `color` value of their containing element | ALL | 3 | 2 | M | 4 |
| 51 | Scrollbar contrast: custom scrollbar styles (if any) must have 3:1 contrast against the scrollbar track; if no custom scrollbar, the default system scrollbar is exempt | ALL | 2 | 1 | L | 4 |
| 52 | Selection highlight: `::selection` background colour (if custom-defined) must ensure sufficient contrast for the selected text | ALL | 2 | 1 | L | 4 |
| 53 | Marquee/animation colour: the removed landing marquee (session 17) must have been fully removed — verify no animated text strip remains that could have contrast issues | LND | 2 | 1 | L | 4 |
| 54 | Toast contrast: success toast `"Saved."` and error toast `"That didn't go through."` must have sufficient contrast — if toast uses amber on dark: #f4b942 on #09090f = 8.2:1, passes | ADM | 4 | 1 | L | 4 |
| 55 | Progress bar contrast: the fan cap progress bar in admin must have 3:1 contrast between fill colour and empty track — if fill is amber and track is dark grey, verify | ADM | 3 | 1 | L | 5 |
| 56 | Countdown ring contrast: the pre-release countdown ring (SVG circle) must achieve 3:1 as a non-text UI component against the page background | V8 | 3 | 1 | L | 5 |
| 57 | Chip/badge backgrounds: campaign state chips (pre-release, live, gig) must achieve 3:1 as a UI component and 4.5:1 for any text inside them | V8 | 4 | 2 | M | 5 |
| 58 | Skeleton screen contrast: loading skeleton grey must achieve 3:1 against the card background — pure `rgba(255,255,255,0.05)` shimmer on a dark card may fail | ALL | 2 | 1 | L | 5 |
| 59 | Dark background `--color-bg: #0d0e1a`: verify this is the exact value in all dark-theme CSS rules — not `#0d0e19` or `#0e0e1a` which are near-identical but not tokenised | V8 | 3 | 1 | L | 5 |
| 60 | Admin dark background `--bg: #09090f`: verify no rule uses `#09090e` or `#0a090f` — token drift is invisible visually but creates maintenance debt | ADM | 2 | 1 | L | 5 |
| 61 | `--color-card: #12152a`: verify all profile section cards use this exact token — no card uses `#11142a` or `#131629` | V8 | 2 | 1 | L | 5 |
| 62 | White text body on profile dark theme: primary body text `#ffffff` (or near-white `--color-text`) on `#0d0e1a` = 15.6:1 — easily passes; verify no theme rule reduces white below 7:1 on the dark background | V8 | 2 | 1 | L | 5 |
| 63 | Artist accent at 7 vibes: each of the 7 vibe default accents must be tested against `#0d0e1a` (dark theme) for large-text (18px+) 3:1 compliance — particularly the warmer accents (acoustic warm amber, indie muted teal) which may be darker | V8 | 5 | 2 | M | 5 |
| 64 | WCAG automated check: run `axe-core` or `pa11y` against all 4 pages at their default state — capture the automated contrast failures report and cross-reference with manual fixes in this phase | ALL | 5 | 3 | M | 5 |
| 65 | Contrast theme force-override: in contrast mode, verify ALL CSS custom property tokens are overridden — not just the main background and text, but also secondary text, borders, placeholders, and focus rings | V8 | 4 | 3 | M | 5 |
| 66 | Glass theme fallback: browsers without `backdrop-filter` support (Firefox < 103, some Android WebView) must receive a semi-opaque solid background fallback — use `@supports (backdrop-filter: blur(1px))` guard | V8 | 4 | 2 | M | 5 |
| 67 | Glass iframe wrapping (P0-6 from UI PATH-TO-10): iframes in glass theme must have a containing background to prevent see-through iframe borders — this was flagged in GPT UI review; verify it's fixed | V8 | 3 | 2 | M | 5 |
| 68 | WCAG 1.4.11 non-text contrast audit: all UI components (buttons, form inputs, icons used for interaction, state indicators) must achieve 3:1 against adjacent colours — run a systematic check of every interactive element on each page | ALL | 5 | 4 | M | 5 |
| 69 | Colour-only information: no state must be communicated by colour alone — every campaign state chip must have a text label in addition to any colour indicator; every error must have text in addition to a red border | ALL | 5 | 2 | L | 5 |
| 70 | Landing accent colour: landing.html uses a fixed accent for the pricing CTAs — verify this accent achieves 4.5:1 against the CTA button text and 3:1 as a non-text UI component | LND | 4 | 2 | M | 5 |
| 71 | Landing hero text: landing hero headline must achieve 4.5:1 against the hero background — if hero uses a background image or gradient, test at the point of lowest contrast | LND | 4 | 2 | M | 5 |
| 72 | Landing Fraunces font: if Fraunces is used for editorial pull quotes, verify its dark colour achieves 4.5:1 against the landing page background | LND | 3 | 1 | L | 5 |
| 73 | Fan confirmation email colour: the fan confirmation email HTML must use inline styles with explicit hex values (not CSS variables, which email clients ignore) — the artist accent colour must be embedded as a literal hex value when the email is generated | FN | 5 | 2 | M | 5 |
| 74 | Email text contrast: the fan confirmation email body text must achieve 4.5:1 in both light and dark email clients — use `#1a1a2e` for text (not grey) to ensure visibility in both contexts | FN | 4 | 2 | M | 5 |
| 75 | Upgrade overlay blur: the gold lock blurred preview overlay must preserve 4.5:1 for the overlay text regardless of the underlying blurred content colour — use a solid or semi-solid overlay background, not a transparent one | ADM | 4 | 2 | M | 6 |
| 76 | Live state indicator: the green "live" indicator dot must achieve 3:1 against the dark card background — a `#22c55e` dot on `#12152a` ≈ 5.5:1, passing; regression-check after any card background colour change | V8 | 3 | 1 | L | 6 |
| 77 | Pre-release indicator: the purple/violet pre-release accent must achieve 3:1 on the dark card background — a deep purple like `#7c3aed` on `#12152a` ≈ 3.7:1, passing for non-text; verify | V8 | 3 | 1 | L | 6 |
| 78 | Contrast-check PRE-LAUNCH gate: before first real user, run a full colour contrast audit using the `contrast()` CSS function or a tool like Chrome DevTools Accessibility panel — capture results and fix all failures | ALL | 5 | 3 | M | 6 |
| 79 | Token grep final: after all Phase 6 fixes, grep all 4 HTML files for hex colour values not in `:root` or `[data-theme]` blocks — count must be 0 | ALL | 4 | 2 | L | 6 |
| 80 | Admin gig countdown bar: the gig mode depletion bar (C16 in STATUS.md) must achieve 3:1 as a UI component — the fill colour (amber) against the track (dark grey) | ADM | 3 | 1 | L | 6 |
| 81 | Dark theme `--color-card` alt: if any component uses a slightly lighter card (e.g. `#151829`) for visual layering, verify this alt card achieves 3:1 against `--color-bg: #0d0e1a` as a surface separator | V8 | 2 | 1 | L | 6 |
| 82 | Stats card in admin (light mode): the stat value and label on the cream card must achieve 4.5:1 — `--dash-text: #1a1a2e` on `--dash-card: #f8f5f0` = 13+:1, passing; regression-check | ADM | 2 | 1 | L | 6 |
| 83 | Connections panel "connected" indicator: the green dot or amber glow on a connected platform must achieve 3:1 against the card background | ADM | 3 | 1 | L | 6 |
| 84 | First-run checklist item complete state: a completed checklist item shown in muted colour must achieve 4.5:1 — if muted via `opacity: 0.5` the effective contrast may fail | ADM | 3 | 1 | L | 6 |
| 85 | Copy/link buttons: "Copy" button labels (UTM copy, QR download) on amber accent must have white or near-white text that achieves 4.5:1 against amber — white on #f4b942 = approximately 1.8:1 which FAILS; use dark text on amber buttons instead | ADM | 5 | 2 | M | 6 |
| 86 | Admin accent glow on connections (UI PATH-TO-10 item): the subtle accent glow on connected platform state — verify the glow does not reduce text contrast within the card by lightening the background near the text | ADM | 2 | 2 | M | 6 |
| 87 | Vibe body colour tokens: each of the 7 vibes may override `--color-bg` with a vibe-appropriate variation — verify all vibe-specific backgrounds have been tested for body text contrast | V8 | 4 | 3 | M | 6 |
| 88 | Release type left border: `data-release-type` CSS colours (album=accent/ep=purple/single=green/mixtape=amber from session 17) must achieve 3:1 against the card background as a non-text UI component | V8 | 3 | 1 | L | 6 |
| 89 | `--color-ambient` tint: the ambient background tint derived from accent must not reduce body text contrast — if the tint darkens or lightens the card, verify text still achieves 4.5:1 | V8 | 3 | 2 | M | 6 |
| 90 | High-contrast preference: `@media (forced-colors: active)` (Windows High Contrast mode) — all interactive elements must remain visually distinct — use `forced-colors: none` only as a last resort; prefer designing in a way that works with forced colours | ALL | 3 | 3 | M | 6 |
| 91 | Dark text on amber: text that appears on the amber `--acc: #f4b942` background in admin (active state text, primary button labels) must use `#1a1a2e` not `#ffffff` — dark text on amber = approximately 8.2:1 (pass); white on amber = approximately 1.8:1 (fail) | ADM | 5 | 2 | M | 6 |
| 92 | Pre-save CTA button contrast: in pre-release state, the pre-save CTA uses accent fill — same analysis as fan capture button — verify `--color-on-accent` text achieves 4.5:1 | V8 | 4 | 1 | L | 6 |
| 93 | Ghost button contrast: the secondary ghost button (border + transparent background) — the border must achieve 3:1 against the page background; the text must achieve 4.5:1 against the page background | V8 | 4 | 1 | L | 6 |
| 94 | Start.html error token: `--err` and `--ok` and `--warn` are now tokenised in start.html (session 19) — verify these same tokens are defined in admin.html and able-v8.html, not just start.html | ALL | 4 | 1 | L | 6 |
| 95 | Contrast theme CTA: in contrast mode, all CTAs must use `background: #ffffff` with `color: #000000` text — 21:1 contrast — verify contrast theme overrides accent-fill buttons | V8 | 4 | 1 | L | 6 |
| 96 | Colour audit documentation: after completing Phase 6, document all confirmed contrast ratios in a `docs/systems/COLOUR-AUDIT.md` file — specific ratios for every token pair that matters — this prevents future regressions by making the expected ratios explicit | ALL | 4 | 3 | L | 6 |
| 97 | Contrast ratio calculator: embed a dev-mode contrast ratio checker in the artist profile — when `?debug=colours` param is present, overlay contrast ratios on all visible text elements (useful for vibe-specific accent debugging) | V8 | 3 | 3 | L | 6 |
| 98 | Glass theme artwork dependency: the glass theme requires a background artwork to be meaningful — if no artwork is set, the glass theme must fall back to the dark theme gracefully, not show a transparent card on a white background | V8 | 4 | 2 | M | 6 |
| 99 | Theme toggle accessibility: the theme selector in the admin profile panel must be labelled correctly for screen readers — `<fieldset>` with `<legend>Page theme</legend>` wrapping the theme radio inputs | ADM | 3 | 2 | M | 6 |
| 100 | Phase 6 closure test: run a Playwright screenshot at all 4 themes (dark/light/glass/contrast) for able-v8.html, then run `axe-core` against each screenshot — zero critical colour contrast violations | V8 | 5 | 3 | M | 6 |

---

## Phase 7 — Interactions & Motion
*Motion is ABLE's personality. Every interaction either reinforces "premium, considered" or erodes it. There is no neutral.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | `transition: all` elimination: grep all 4 HTML files for `transition: all` — count must be 0; every `transition: all` causes the browser to compute changes on every CSS property on every frame, degrading performance | ALL | 5 | 2 | M | 1 |
| 2 | Transition property specificity: every `transition` rule must name exactly the properties it affects — `transition: background-color var(--dur) var(--ease-decel), color var(--dur) var(--ease-decel)` not `transition: background 0.15s ease` | ALL | 4 | 3 | M | 1 |
| 3 | `--dur-fast`, `--dur`, `--dur-slow` tokens: verify these timing tokens are defined in `shared/tokens.css` — `--dur-fast: 0.14s`, `--dur: 0.16s`, `--dur-slow: 0.22s` — and used in all transition rules | ALL | 4 | 2 | M | 1 |
| 4 | Raw timing values grep: grep all 4 pages for `0.15s`, `0.2s`, `0.3s`, `0.25s` in transition rules — every hit is an untokenised duration; replace with the nearest token | ALL | 4 | 2 | M | 1 |
| 5 | `--ease-decel` token: `cubic-bezier(0.25, 0.46, 0.45, 0.94)` must be defined as `--ease-decel` in tokens — all UI state change transitions (hover, focus, colour change) must use this easing | ALL | 4 | 2 | M | 1 |
| 6 | `--ease-spring` token: `cubic-bezier(0.34, 1.56, 0.64, 1)` must be defined as `--ease-spring` — used only for physical/spatial movements: cards appearing, sheets opening, button press and release | ALL | 4 | 2 | M | 1 |
| 7 | Spring easing on opacity: spring easing must NEVER be applied to `opacity` transitions — a spring easing on opacity causes the element to briefly become more opaque than 1.0 (the overshoot) which is visually wrong; use `--ease-decel` for opacity | ALL | 5 | 2 | L | 1 |
| 8 | Spring easing on colour: spring easing must not be applied to `color` or `background-color` transitions — use `--ease-decel` for colour changes | ALL | 3 | 1 | L | 1 |
| 9 | `prefers-reduced-motion` completeness: every `@keyframes` animation block must have a corresponding `@media (prefers-reduced-motion: reduce)` override that either removes the animation entirely or reduces it to an immediate opacity change — STATUS.md confirms this is thorough on able-v8.html; verify admin.html and start.html | ADM | 5 | 3 | M | 1 |
| 10 | `prefers-reduced-motion` on transitions: `transition` duration must also be reduced in motion-reduced mode — add `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { transition-duration: 0.01ms !important; animation-duration: 0.01ms !important; } }` as a global safety net | ALL | 5 | 1 | L | 1 |
| 11 | Hover state completeness: every interactive element must have a visible hover state — button background lightens/darkens by 10%, link colour shifts, card lifts with `box-shadow` change — grep for any `:hover` rule that only changes `cursor` and nothing visual | ALL | 4 | 3 | M | 2 |
| 12 | Hover on admin cards: admin stat cards, fan list items, snap card rows must all have a hover state — `background-color` shift of ~5% or `box-shadow` lift | ADM | 4 | 2 | M | 2 |
| 13 | Hover on platform pills: Quick Action pills on the profile must have a hover state — not just `cursor: pointer`; the pill should lighten/darken or show a subtle border highlight | V8 | 4 | 1 | L | 2 |
| 14 | Hover on hero CTA primary: the primary accent fill CTA must have a hover state — `background-color` lightened by 10% using `color-mix(in srgb, var(--color-accent) 90%, white)` | V8 | 4 | 1 | L | 2 |
| 15 | Hover on ghost button: the secondary ghost CTA must have a hover state — fill with `color-mix(in srgb, var(--color-accent) 15%, transparent)` to hint at the accent | V8 | 4 | 1 | L | 2 |
| 16 | No hover-only states: every interaction state defined with `:hover` must have an equivalent `:focus-visible` state — mobile users never trigger hover, so no feature must rely on hover alone | ALL | 5 | 3 | M | 2 |
| 17 | Pressed/active state: every tappable element must have `transform: scale(0.97)` on `:active` with `--ease-spring` easing — the physical "press down" feedback is ABLE's personality on mobile | ALL | 5 | 2 | M | 2 |
| 18 | Press and release: the `:active` `scale(0.97)` must return to `scale(1.0)` with spring easing on release — verify the release transition uses `--ease-spring` not `--ease-decel` | ALL | 4 | 2 | M | 2 |
| 19 | CTA press flash (B3/B4): the CTA press flash `::after` opacity pulse is confirmed in STATUS.md — verify it's still working after any recent CTA changes and does not fire in reduced-motion mode | V8 | 3 | 1 | L | 2 |
| 20 | Focus ring glow pattern: all focusable elements must use `box-shadow: 0 0 0 2px var(--bg), 0 0 0 4px var(--acc)` — a two-layer glow, not a flat `outline: 2px solid` — the CONTEXT.md P0 bug list confirms this was identified; verify it's fixed on all pages | ALL | 5 | 2 | M | 2 |
| 21 | Focus visible only: focus rings must only appear on `:focus-visible` (keyboard focus), not on `:focus` (which fires on mouse click too) — `button:focus:not(:focus-visible) { box-shadow: none }` | ALL | 4 | 2 | M | 2 |
| 22 | Admin focus ring colour: admin uses `--acc: #f4b942` for the focus ring outer layer — verify this rule is applied to every interactive element in admin.html, including the bottom nav buttons | ADM | 4 | 2 | M | 2 |
| 23 | Profile focus ring colour: profile uses the artist accent `--color-accent` for the focus ring outer layer — verify `applyDerivedTokens()` sets `--acc` (used by the focus ring rule) to the artist accent | V8 | 4 | 1 | L | 2 |
| 24 | Disabled state visual: disabled buttons and inputs must be at 40% opacity with `cursor: not-allowed` — not just `pointer-events: none` which prevents interaction but gives no visual feedback | ALL | 4 | 2 | M | 3 |
| 25 | Disabled state ARIA: disabled elements must have `aria-disabled="true"` — not just `disabled` attribute which removes the element from the tab order; `aria-disabled` keeps it in the tab order with screen reader announcement | ALL | 5 | 2 | M | 3 |
| 26 | Gold lock disabled state: Pro-gated features in admin must be visually disabled (blurred preview + gold overlay) AND have `aria-disabled="true"` on the locked container | ADM | 4 | 2 | M | 3 |
| 27 | Loading states for all async operations: every operation that may take >100ms must show a loading state — specifically: Spotify import, Linktree import, Ticketmaster import, fan-confirmation email send, artist-welcome email send | ALL | 4 | 3 | M | 3 |
| 28 | Import loading state: during Spotify/Linktree import in start.html, the "Import" button must change to "Importing…" with a spinner — not just become unresponsive | STR | 4 | 2 | M | 3 |
| 29 | Ticketmaster loading state: the Ticketmaster import button in admin world map must show "Importing shows…" while the Netlify function runs | ADM | 3 | 2 | M | 3 |
| 30 | Email send loading state: "artist-welcome.js" is fire-and-forget so no loading state is needed for the artist; but the wizard done screen must render immediately without waiting for the email — verify `initDone()` does not await the email send | STR | 4 | 1 | L | 3 |
| 31 | Skeleton screens for stat cards: admin stat cards must use a loading skeleton animation on first render before localStorage data is available — typically localStorage reads are synchronous and instant, so this may only apply if data comes from a future Supabase call | ADM | 2 | 2 | M | 3 |
| 32 | Skeleton pulse animation: all skeleton loading states must use a pulsing shimmer (`background: linear-gradient(90deg, ...)` with `@keyframes shimmer`) — not a static grey block | ALL | 3 | 2 | M | 3 |
| 33 | Success state — fan sign-up: when a fan successfully signs up, the form must transform into a success state with ceremony — not just disappear; suggest: the form fades out and a "You're on [Artist Name]'s list." message fades in with a brief scale animation | V8 | 5 | 2 | M | 3 |
| 34 | Success state — profile save: when the artist saves their profile in admin, a toast appears ("Saved.") and the save button briefly returns to its default state — the toast must animate in and out, not appear/disappear instantly | ADM | 4 | 2 | M | 3 |
| 35 | Success state — first fan: `checkFirstFanMoment()` in admin already surfaces the first fan — verify the card appears with an entrance animation (fade + slight upward translate) not just appearing instantly | ADM | 4 | 2 | M | 3 |
| 36 | Toast animation: admin toast notifications must enter with `transform: translateY(100%) → translateY(0)` over `0.22s` with `--ease-decel` — and auto-dismiss after 3s with a reverse exit animation | ADM | 3 | 2 | M | 3 |
| 37 | Campaign state chip transition: when the campaign state changes (e.g. artist enables gig mode), the state chip on the profile must update with a crossfade, not an instant swap | V8 | 3 | 2 | M | 3 |
| 38 | Countdown ring animation: the pre-release countdown ring must animate (the ring depletes over time, not jumps) — verify the SVG stroke-dashoffset is animated with `transition: stroke-dashoffset 1s linear` | V8 | 3 | 2 | M | 4 |
| 39 | Section entrance (I2): section headers fade-in on scroll is confirmed in STATUS.md — verify this uses `IntersectionObserver` with `threshold: 0.1` and the fade uses `opacity: 0 → 1` with `--ease-decel` over `--dur-slow` | V8 | 3 | 1 | L | 4 |
| 40 | Platform pill entrance (A10/D3): horizontal wave entrance (translateX -8px→0, 220ms, 50ms stagger) is confirmed — verify it fires once per page load, not on every scroll | V8 | 3 | 1 | L | 4 |
| 41 | Stats counter animation (G14): admin stats counter animation on first load is confirmed — verify it uses `requestAnimationFrame` easing, not a `setInterval` step counter which produces choppy animation | ADM | 3 | 1 | L | 4 |
| 42 | Fan list stagger (D13): fan list stagger animation on first admin load is confirmed — verify each fan row appears with `animation-delay: Nms` calculated from its index | ADM | 2 | 1 | L | 4 |
| 43 | Progress bar spring (E10): wizard progress bar spring easing (`--spring, 0.55s`) is confirmed — verify the progress bar uses CSS `transition: width 0.55s var(--ease-spring)` not a JS animation | STR | 3 | 1 | L | 4 |
| 44 | Artist name scroll compression (A11): lerp 48px→24px over hero scroll height is confirmed — verify the `requestAnimationFrame` loop is correctly cancelled when the element is not in the viewport to prevent memory leaks | V8 | 3 | 2 | M | 4 |
| 45 | Sticky artist bar entrance: the sticky artist bar must fade in smoothly when triggered at 70% hero scroll — verify `opacity: 0 → 1` transition over `--dur-slow` | V8 | 3 | 1 | L | 4 |
| 46 | Bio char count fade (E3): bio char count fades in amber at 80+ chars is confirmed — the fade-in must use `opacity: 0 → 1` with `transition: opacity var(--dur-slow) var(--ease-decel)` | ADM | 2 | 1 | L | 4 |
| 47 | Gig badge glow pulse (C7): `badge-glow-pulse` keyframe on `.hero__state-chip--gig::after` is confirmed — verify this keyframe has a `prefers-reduced-motion` override that removes the pulse | V8 | 3 | 1 | L | 4 |
| 48 | Pre-release ambient intensification (H9): ambient opacity `0.12 + 0.16 × (1 - daysLeft/14)` clamped to `0.12–0.28` is confirmed — verify the animation is smooth (uses CSS transition on `--ambient-opacity` custom property) not a JS step function | V8 | 3 | 2 | M | 4 |
| 49 | First-run checklist exit (S15): `markFrcDone()` adds `.completing` class for 250ms reverse animation before `.done` — verify the animation completes fully before the DOM element is hidden | ADM | 3 | 1 | L | 4 |
| 50 | QR code render animation: `renderSbQR()` should animate the QR code in with a brief fade — not appear instantly which feels abrupt for what is a meaningful reveal | ADM | 2 | 2 | L | 4 |
| 51 | Vibe-specific motion personality: Electronic vibe uses snap/stiff motion (low spring overshoot), Acoustic vibe uses slower lingering transitions — verify `data-vibe` changes the `--ease-spring` and `--dur` values per the CONTEXT.md spec | V8 | 4 | 3 | M | 5 |
| 52 | `data-feel` motion impact: verify each feel (raw/refined/intimate/bold) affects motion parameters — e.g. `raw` feel uses more aggressive spring (higher overshoot), `intimate` uses slower deceleration | V8 | 3 | 3 | M | 5 |
| 53 | Drag animation for reorderable lists: snap card reordering and clip reordering in admin must have a visual drag indicator — the dragged item must appear "lifted" (elevated shadow) while dragging | ADM | 3 | 3 | M | 5 |
| 54 | Drop animation: when a dragged item is dropped in a new position, the items below must animate into their new positions, not jump | ADM | 3 | 3 | M | 5 |
| 55 | Colour picker preview animation: when the artist changes their accent colour in admin, the profile preview (if visible) must update with a smooth `transition: color 0.3s, background-color 0.3s` | ADM | 3 | 2 | M | 5 |
| 56 | Tab switching in admin: switching between admin tabs (Today/Fans/Page/Updates/Stats) must use a crossfade or slide transition — not an instant content swap | ADM | 3 | 2 | M | 5 |
| 57 | Campaign mode change animation: when the artist changes campaign mode in Campaign HQ, the mode description text must crossfade to the new copy — `opacity: 1 → 0` then set new text then `opacity: 0 → 1` | ADM | 3 | 2 | M | 5 |
| 58 | Gig mode enable: enabling gig mode must have a distinct activation animation — the campaign chip should flash amber once and the mode desc should slide up from below — the moment deserves ceremony | ADM | 4 | 3 | M | 5 |
| 59 | Landing demo phone animation: the interactive proof demo phone on landing.html (4 states + theme cycle) must use smooth transitions between states — not instant swaps | LND | 4 | 2 | M | 5 |
| 60 | Landing CTA button hover: the primary CTA on landing must have a hover state with the same treatment as the profile primary CTA — subtle background lighten + `transition: background-color var(--dur) var(--ease-decel)` | LND | 3 | 1 | L | 5 |
| 61 | Wizard step transition: moving between wizard steps must use a horizontal slide (current step slides left, new step slides in from right) — not an instant content replacement | STR | 4 | 3 | M | 5 |
| 62 | Wizard done screen animation: "Your page is live." should fade in letter by letter or word by word (subtle stagger) — a significant moment deserves more than a flat fade-in | STR | 4 | 3 | M | 5 |
| 63 | Notifications/nudge card appearance: nudge cards in admin must animate in from the top with a smooth entrance — they should not appear on page load if admin is slow to render | ADM | 3 | 2 | M | 5 |
| 64 | Error message shake: form validation error (E11 in STATUS.md — delayed reveal 400ms after shake) must use a `@keyframes shake` that moves the input `translate(−4px, 0) → (4px, 0)` × 2 — the shake communicates rejection without being harsh | V8 | 4 | 1 | L | 5 |
| 65 | Clipboard copy flash: when a copy button is tapped, the button text changes "Copy" → "Copied" and the button briefly highlights in success colour — verify the revert back to "Copy" happens after 2 seconds | ADM | 3 | 1 | L | 5 |
| 66 | `@view-transition` guard: all 3 pages that use `@view-transition` must have the `@supports (view-transition-name: none)` guard confirmed in S15 — verify the guard is still in place after recent session edits | ALL | 3 | 1 | L | 5 |
| 67 | Intersection Observer cleanup: every `IntersectionObserver` created in JS must be disconnected when its target element is removed from the DOM — memory leaks from orphaned observers accumulate on single-page navigation | ALL | 3 | 2 | M | 5 |
| 68 | `requestAnimationFrame` loop cleanup: every rAF loop (artist name scroll compression A11) must store the frame ID and cancel it with `cancelAnimationFrame(id)` when the component is no longer visible | V8 | 3 | 2 | M | 5 |
| 69 | GPU-composited animations only: all animations on able-v8.html must only animate `transform` and `opacity` — not `width`, `height`, `padding`, `margin`, `top`, `left` which force layout recalculations — audit every `@keyframes` for non-composited properties | V8 | 4 | 3 | M | 5 |
| 70 | `will-change` usage: use `will-change: transform` only on elements that are about to animate, applied just before the animation starts and removed after — not as a permanent attribute on all cards | V8 | 3 | 2 | M | 5 |
| 71 | Scroll performance: the artist name compression animation (A11) uses scroll events — verify it uses `passive: true` in the event listener `{ passive: true }` to not block scrolling | V8 | 4 | 1 | L | 5 |
| 72 | Modal open/close animation: admin modals (if any) must enter with `scale(0.95) opacity(0) → scale(1) opacity(1)` over `--dur-slow` — the scale + fade combination feels intentional, not a simple fade | ADM | 3 | 2 | M | 5 |
| 73 | Bottom sheet swipe animation: if admin uses bottom sheets, the sheet must animate from `translateY(100%)` to `translateY(0)` with `--ease-decel` — not spring (a sheet appearing from off-screen with spring overshoot feels like a bug) | ADM | 3 | 2 | M | 5 |
| 74 | Admin connections glow: the accent glow on connected platform state (UI PATH-TO-10 item 10) must use a `box-shadow` pulse animation — `@keyframes connection-glow: 0% { box-shadow: 0 0 0 0 ... } 100% { box-shadow: 0 0 0 6px rgba(..., 0) }` | ADM | 3 | 3 | M | 5 |
| 75 | Lifecycle stage transition: when `data-stage` changes (new → active, or active → gig), any CSS that depends on `data-stage` must transition — the accent cues per stage must fade in, not snap | ADM | 3 | 2 | M | 6 |
| 76 | Platform pill shimmer (D15): `.pill-shimmer-once` confirms a first-load shimmer on platform pills — verify this is session-flagged (fires once) and not firing on every page load | V8 | 3 | 1 | L | 6 |
| 77 | Artwork placeholder shimmer (F15): accent shimmer on artwork placeholder loading state is confirmed — verify the shimmer stops once the artwork loads and doesn't continue as a background animation | V8 | 3 | 1 | L | 6 |
| 78 | Tab scroll sync (I7): tab selection on the profile syncs the tab indicator with scroll position — verify the indicator moves with `transition: transform var(--dur) var(--ease-decel)` not instant jump | V8 | 3 | 1 | L | 6 |
| 79 | Accordion animation in landing FAQ: FAQ expand/collapse must use CSS `max-height` transition — `max-height: 0 → max-height: 500px` with `overflow: hidden` — not a JS height calculation | LND | 3 | 1 | L | 6 |
| 80 | Fan counter in admin: the fan count stat card must animate when a new fan signs up (if admin is open at the time) — verify the counter animation re-fires when `able_fans.length` changes, not just on first load | ADM | 3 | 3 | M | 6 |
| 81 | QR code download: `downloadSbQR()` must give visual feedback (button "Downloading…" → "Downloaded") — the download is instant but the copy matters | ADM | 2 | 1 | L | 6 |
| 82 | Section collapse/expand in admin: if admin sections can be collapsed/expanded, the collapse must animate — `height: auto → height: 0` using the CSS custom property trick with `--section-height` and `transition: height` | ADM | 2 | 3 | M | 6 |
| 83 | UTM copy toast: after copying a UTM link, a toast must appear ("Copied to clipboard") and auto-dismiss after 2s — not just a button label change | ADM | 3 | 1 | L | 6 |
| 84 | World map animation: the world map calendar view moments must animate in on load — each moment pin should scale from `scale(0)` to `scale(1)` with a stagger | ADM | 2 | 3 | M | 6 |
| 85 | Pricing card hover on landing: pricing cards must lift on hover — `transform: translateY(-4px)` with `box-shadow` increase — over `--dur-slow` with `--ease-decel` | LND | 3 | 1 | L | 6 |
| 86 | Feature comparison table row hover: rows in the competitive comparison table on landing must have a subtle hover state — not a full background change, just `opacity: 0.8 → 1` on the tick/cross icons | LND | 2 | 1 | L | 6 |
| 87 | Vibe selection animation in wizard: when the artist selects a vibe in start.html, the selected vibe card must scale up slightly (`scale(1.02)`) with a spring — physical selection feedback | STR | 3 | 2 | M | 6 |
| 88 | Feel selection animation: same as vibe — feel quadrant selection must have a spring press-and-hold feedback | STR | 3 | 2 | M | 6 |
| 89 | Accent colour picker: when the artist changes accent in admin (and profile preview updates), the whole profile preview should subtly pulse once to draw attention to the colour change | ADM | 2 | 3 | M | 6 |
| 90 | Gig mode depletion bar (C16): the depletion bar must animate continuously from full to empty over 24 hours — verify it uses `transition: width` that recalculates every minute or uses a CSS animation with a fixed 86400s duration | ADM | 3 | 2 | M | 6 |
| 91 | Pre-release countdown pulse: when the countdown reaches under 1 hour, the countdown text should pulse slightly — `scale(1.0) → scale(1.02) → scale(1.0)` on a 2s interval — to signal urgency | V8 | 3 | 3 | M | 6 |
| 92 | No `setTimeout` for animations: animations must use CSS transitions and `@keyframes`, not `setTimeout` chains — setTimeout-based animation is choppy and non-composited | ALL | 4 | 3 | M | 6 |
| 93 | `transitionend` event cleanup: any JS that listens for `transitionend` to perform a post-animation action must use `{ once: true }` in the event listener to prevent accumulation | ALL | 3 | 2 | M | 6 |
| 94 | Content visibility: `content-visibility: auto` on long-scroll sections (snap cards, events grid, merch grid below the fold) can improve FCP by skipping layout of off-screen content — implement with `contain-intrinsic-size` to prevent scroll bar jumping | V8 | 3 | 3 | M | 6 |
| 95 | Motion design documentation: document ABLE's motion personality in `docs/systems/MICRO_INTERACTIONS_SPEC.md` — which easing for which type of motion, when to use spring vs decel, performance rules — this prevents drift across sessions | ALL | 3 | 2 | L | 6 |
| 96 | Animation performance audit: use Chrome DevTools Performance tab to measure animation frame rates on able-v8.html during scroll — target 60fps; identify any sub-60fps section and optimise | V8 | 4 | 3 | M | 6 |
| 97 | Reduced motion test: set `prefers-reduced-motion: reduce` in Chrome DevTools and verify all 4 pages render without any motion — no flashing, no bouncing, no transitions | ALL | 5 | 2 | L | 6 |
| 98 | Safari animation compatibility: some `@keyframes` with `scale()` shorthand fail in Safari 15 — use `transform: scale()` instead of just `scale()` in keyframe blocks | ALL | 4 | 1 | L | 6 |
| 99 | `animation-fill-mode: forwards`: any entrance animation must use `animation-fill-mode: forwards` to prevent the element from snapping back to its initial state after the animation ends | ALL | 3 | 1 | L | 6 |
| 100 | Phase 7 closure: run a Playwright interaction test that clicks every interactive element on each page and verifies: (1) hover state appears, (2) active state fires on click, (3) no layout shift occurs during transition, (4) focus ring appears on keyboard tab — zero visual regressions | ALL | 5 | 4 | M | 6 |

---

## Phase 8 — Security & Data
*Before any real user data is stored. A single XSS or a leaked service key makes every other improvement irrelevant.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | Supabase key grep: grep all HTML, JS, and config files for `sb_secret_`, `service_role`, `eyJ` (JWT prefix), and any key longer than 40 chars that is not the public anon key — zero instances; if found, rotate immediately | ALL | 5 | 1 | H | 1 |
| 2 | Anon key exposure: the Supabase anon key (`sb_publishable_pRmYph3-GYIeZEeKyY1_sg_5sOPkQp_`) is intentionally public-facing — but it must only appear in files that run in the browser; verify it does NOT appear in server-side Netlify function code (which would indicate a mistaken use of the anon key where the service key is needed) | ALL | 5 | 1 | H | 1 |
| 3 | `innerHTML` with untrusted data: grep all 4 pages for `innerHTML` — every instance must either: (a) use only hardcoded HTML strings, or (b) call `escHtml()` on all user-provided values before insertion | ALL | 5 | 2 | H | 1 |
| 4 | `escHtml()` function: verify `escHtml()` is defined in `shared/able.js` and correctly escapes `&`, `<`, `>`, `"`, `'` — the function must use `document.createTextNode()` or a manual replacement map, not `innerHTML` itself | ALL | 5 | 2 | H | 1 |
| 5 | Artist name in DOM: the artist name is user-provided and rendered in multiple DOM locations (hero, sticky bar, admin greeting) — verify every render path uses `textContent = name` or `escHtml(name)` not `innerHTML = name` | V8 | 5 | 2 | H | 1 |
| 6 | Bio in DOM: artist bio is user-provided and rendered as a text block — verify `textContent` or `escHtml()` is used, never raw `innerHTML = bio` | V8 | 5 | 2 | H | 1 |
| 7 | Snap card content in DOM: snap card title and body are user-provided — verify all snap card render paths use `textContent` for text nodes | V8 | 5 | 2 | H | 1 |
| 8 | Release title in DOM: release card titles are user-provided (imported from Spotify or typed manually) — verify render path escapes the title | V8 | 5 | 1 | H | 1 |
| 9 | CTA label in DOM: CTA button labels are user-provided — verify render path escapes labels before insertion | V8 | 5 | 1 | H | 1 |
| 10 | Show venue in DOM: show venue name is user-provided — verify render path in world map and events section escapes venue | V8 | 4 | 1 | H | 1 |
| 11 | Credit name in DOM: credit names (user-entered in admin) rendered on the profile — verify `textContent` used, not `innerHTML` | V8 | 4 | 1 | H | 1 |
| 12 | URL parameters in DOM: `?ref=`, `?src=`, and `?artist=` params are reflected in the DOM (e.g. source attribution text, artist slug in links) — verify all URL param values are escaped before DOM insertion | V8 | 5 | 2 | H | 1 |
| 13 | CSP `script-src`: `netlify.toml` or `_headers` must set `Content-Security-Policy: script-src 'self' https://cdn.jsdelivr.net https://app.posthog.com 'nonce-{nonce}'` — no `unsafe-inline` in `script-src`; if inline scripts are needed, use `nonce` | FN | 5 | 4 | H | 1 |
| 14 | CSP `style-src`: CSP must include `style-src 'self' https://fonts.googleapis.com 'unsafe-inline'` — `unsafe-inline` is required for CSS custom properties and dynamic style injection; document this as an accepted risk | FN | 4 | 2 | M | 1 |
| 15 | CSP `img-src`: `img-src 'self' data: https://images.unsplash.com https://i.scdn.co https://jgspraqrnjrerzhnnhtb.supabase.co` — limit image sources to known hosts only | FN | 4 | 2 | M | 1 |
| 16 | CSP `font-src`: `font-src https://fonts.gstatic.com` — only Google Fonts CDN for external fonts | FN | 3 | 1 | L | 2 |
| 17 | CSP `connect-src`: `connect-src 'self' https://jgspraqrnjrerzhnnhtb.supabase.co https://app.posthog.com https://api.resend.com` — no wildcard `*` in connect-src | FN | 4 | 2 | M | 2 |
| 18 | CSP `frame-src`: `frame-src https://open.spotify.com https://w.soundcloud.com https://www.youtube.com https://www.tiktok.com` — allow only known embed origins | FN | 4 | 1 | L | 2 |
| 19 | `X-Frame-Options: DENY` on admin.html: admin must not be embeddable in an iframe — set `X-Frame-Options: DENY` in `_headers` for `/admin` route | FN | 5 | 1 | L | 2 |
| 20 | `X-Content-Type-Options: nosniff`: all pages must have this header to prevent MIME sniffing attacks | FN | 3 | 1 | L | 2 |
| 21 | `Strict-Transport-Security`: `Strict-Transport-Security: max-age=31536000; includeSubDomains` must be set in `_headers` to prevent HTTP downgrade attacks | FN | 4 | 1 | L | 2 |
| 22 | `Referrer-Policy: strict-origin-when-cross-origin`: prevents full URL leakage to third parties while allowing same-origin referrer | FN | 3 | 1 | L | 2 |
| 23 | `Permissions-Policy`: disable unused browser APIs — `Permissions-Policy: camera=(), microphone=(), geolocation=(self)` — ABLE does not use camera or mic; geolocation is only needed for fan.html Near Me | FN | 3 | 1 | L | 2 |
| 24 | oEmbed SSRF protection: `oembed-proxy.js` must validate the incoming `url` against an allowlist — `['open.spotify.com', 'soundcloud.com', 'music.apple.com', 'bandcamp.com']` — before making any outbound fetch; a redirect-following fetch to an arbitrary URL is an SSRF attack vector | FN | 5 | 2 | H | 2 |
| 25 | oEmbed URL validation: the allowlist check must validate the full hostname, not just `url.includes('spotify.com')` which can be bypassed with `evil.com?url=spotify.com` — use `new URL(url).hostname` and check against the allowlist array | FN | 5 | 1 | H | 2 |
| 26 | Linktree import input: `linktree-import.js` must validate that the incoming URL is a valid `linktr.ee` hostname before fetching — prevent arbitrary URL fetch | FN | 4 | 1 | H | 2 |
| 27 | Spotify import input: `spotify-import.js` must validate `open.spotify.com` hostname before any API call | FN | 4 | 1 | H | 2 |
| 28 | Ticketmaster import input: `ticketmaster-import.js` must validate the artist name/ID input — max 100 chars, alphanumeric + spaces only — before passing to the Ticketmaster API | FN | 3 | 1 | M | 2 |
| 29 | Email input validation server-side: `fan-confirmation.js` must validate the email address format server-side before calling Resend — use a simple regex: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` — never trust client-side validation alone | FN | 5 | 1 | M | 2 |
| 30 | String length limits on all Netlify function inputs: every string input to every Netlify function must have a max length check — artist name: 100 chars, bio: 300 chars, email: 254 chars (RFC 5321 max), URL: 2048 chars | FN | 4 | 2 | M | 2 |
| 31 | `JSON.parse` safety: every `JSON.parse()` call in JS must be in a try/catch — localStorage data can be corrupted by browser storage quota errors or manual edits — verify all parse calls are wrapped | ALL | 5 | 2 | M | 3 |
| 32 | `safeLS()` global coverage: grep all 4 pages for `localStorage.getItem` not wrapped in `safeLS()` — every raw `localStorage.getItem` that's followed by `JSON.parse` is a potential crash | ALL | 5 | 2 | M | 3 |
| 33 | `safeSet()` global coverage: grep all 4 pages for `localStorage.setItem` not wrapped in `safeSet()` — every raw `setItem` can throw `QuotaExceededError` | ALL | 4 | 2 | M | 3 |
| 34 | localStorage quota strategy: when `QuotaExceededError` is caught in `safeSet()`, the strategy must: (1) prune `able_clicks` to last 200 events, (2) prune `able_views` to last 200 events, (3) retry the write — the artist's profile and fan list must never be pruned | ALL | 5 | 3 | M | 3 |
| 35 | localStorage quota monitoring: add a dev-mode `console.warn` when `able_clicks + able_views` combined exceed 2MB — proactive warning before the 5MB limit is hit | ALL | 2 | 1 | L | 3 |
| 36 | Unhandled promise rejections: add `window.addEventListener('unhandledrejection', e => console.error('Unhandled:', e.reason))` to all pages in dev mode — in production, this should still catch and log errors silently to prevent blank screens | ALL | 4 | 2 | M | 3 |
| 37 | `try/catch` on all async functions: every `async` function that makes a network call (`fetch`) must have a `try/catch` that handles failure gracefully — the failure mode must be a degraded UI, not a blank screen | ALL | 5 | 3 | M | 3 |
| 38 | Import failure handling: if Spotify import or Linktree import fails (network error, function timeout), `start.html` must show an error state and allow the artist to proceed with manual entry — not leave the wizard stuck at the import step | STR | 5 | 2 | M | 3 |
| 39 | Email send failure handling: `fan-confirmation.js` Netlify function — if Resend fails, the function must return a 200 (not 500) and log the error; a fan sign-up must succeed even if the confirmation email fails to send | FN | 5 | 1 | M | 3 |
| 40 | `RESEND_API_KEY` missing handling: `fan-confirmation.js` must check `if (!process.env.RESEND_API_KEY)` and return a mock success response — so the fan sign-up still works in dev environments without the key set | FN | 4 | 1 | L | 3 |
| 41 | PostHog error handling: `posthog.capture()` must be wrapped in try/catch — a PostHog event failure must not throw an uncaught error | V8 | 3 | 1 | L | 3 |
| 42 | No `eval()` anywhere: grep all 4 pages for `eval(` — zero instances; `eval` is both a security risk and a CSP violation | ALL | 5 | 1 | H | 3 |
| 43 | No `document.write()`: grep for `document.write(` — zero instances | ALL | 5 | 1 | H | 3 |
| 44 | No `new Function()` with user input: grep for `new Function(` — the only permitted use is in the parse-check dev tool; no user input must ever be passed to `new Function()` | ALL | 5 | 1 | H | 3 |
| 45 | URL redirect validation: if any redirect logic uses URL parameters (e.g. `?redirect=/admin`), validate the redirect target is a same-origin path before navigating — prevent open redirect attacks | ALL | 4 | 2 | M | 3 |
| 46 | FCP under 2s goal: run Lighthouse on able-v8.html with a 4G mobile throttle — FCP must be under 2s; if not, identify the blocking resource (likely a font or large JS block in `<head>`) | V8 | 4 | 3 | M | 4 |
| 47 | Render-blocking resources: verify no `<script>` tag in `<head>` lacks `defer` or `async` — all non-critical scripts must be deferred | ALL | 5 | 2 | M | 4 |
| 48 | Google Fonts preconnect: verify `<link rel="preconnect" href="https://fonts.googleapis.com">` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` appear in `<head>` before the fonts `@import` on all 4 pages | ALL | 4 | 1 | L | 4 |
| 49 | Critical CSS inline: the above-the-fold CSS for the hero section on able-v8.html should be inlined in `<style>` to avoid a render-blocking stylesheet request — estimate the critical path CSS and inline at minimum the background colour, hero font, and hero layout | V8 | 4 | 3 | M | 4 |
| 50 | HTML gzip size: verify the HTML file size is within budget — `able-v8.html` must be ≤340KB gzipped per CONTEXT.md performance budget; run `gzip -c able-v8.html | wc -c` | V8 | 5 | 1 | L | 4 |
| 51 | Admin HTML size: `admin.html` is the largest file due to feature density — verify it's under 340KB gzipped; if over, identify the largest inline resource (likely Base64 encoded assets or large inline SVGs) | ADM | 4 | 1 | L | 4 |
| 52 | Image format: all static images (OG cards, artwork placeholders, logo) must be WebP with JPEG fallback using `<picture>` — verify `og-default.jpg` will be converted to WebP and served with a `<picture>` element | ALL | 3 | 2 | M | 4 |
| 53 | Unsplash image parameters: all Unsplash artwork URLs must include `?w=800&q=75&fm=webp&auto=format` to ensure WebP delivery at appropriate resolution — raw Unsplash URLs without parameters serve unoptimised images | V8 | 4 | 1 | L | 4 |
| 54 | LCP element: the LCP element on able-v8.html is likely the hero artwork image — ensure it has `loading="eager"` and `fetchpriority="high"` attributes to prioritise its load | V8 | 4 | 1 | L | 4 |
| 55 | CLS prevention: artwork images must have explicit `width` and `height` attributes (or `aspect-ratio` CSS) to prevent CLS when they load — a missing dimension causes layout shift when the image loads | V8 | 4 | 1 | L | 4 |
| 56 | Supabase client init: the Supabase JS client is initialised with the anon key — verify `createClient(url, anonKey)` is called once and the client is stored in a module-level variable, not re-initialised on every function call | ALL | 3 | 1 | L | 4 |
| 57 | Supabase RLS: when Supabase backend is wired, Row Level Security must be enabled on all tables — verify the `profiles`, `fans`, `clicks` tables have RLS policies that prevent one artist reading another artist's data | FN | 5 | 4 | H | 4 |
| 58 | Supabase anon key capability: the anon key allows `SELECT` on public data and `INSERT` on fan sign-ups — verify no anon key operation can `UPDATE` or `DELETE` fan records directly; all destructive operations require auth | FN | 5 | 2 | H | 4 |
| 59 | Magic link expiry: the Supabase magic link expires after 1 hour (per CONTEXT.md copy spec: "It expires in 1 hour") — verify the Supabase auth configuration has `token_expiry: 3600` (1 hour in seconds) | FN | 4 | 1 | M | 4 |
| 60 | No sensitive data in URL params: verify no URL parameter in any page flow passes sensitive data (auth tokens, email addresses) in plaintext — `?email=user@example.com` in a URL is visible in browser history and server logs | ALL | 4 | 1 | M | 4 |
| 61 | `og:image` `https://` guard: confirmed in STATUS.md (V8 SEO P0 fix) — regression-check that `injectSEO()` still enforces https:// on the og:image URL; a `data:` URI or `http://` URL produces a broken OG card | V8 | 4 | 1 | L | 5 |
| 62 | Supabase `views` table 404: `injectSEO()` / view tracking calls `/rest/v1/views` which 404s (known issue in STATUS.md) — the 404 must be caught silently; verify the catch block does not throw to the user | V8 | 3 | 1 | L | 5 |
| 63 | No console.log in committed code: grep all 4 pages for `console.log(` — zero in committed code; `console.error` for caught errors is acceptable | ALL | 3 | 1 | L | 5 |
| 64 | No debugger statements: grep all 4 pages for `debugger;` — zero instances | ALL | 4 | 1 | L | 5 |
| 65 | Fan email storage: fan emails are stored in `able_fans` in plaintext in localStorage — this is acceptable for V1 (no server-side storage) but must be documented in `privacy.html` as a first-party browser storage mechanism | V8 | 4 | 2 | M | 5 |
| 66 | `consentVersion` in fan object: every fan sign-up must write `consentVersion: '1.0'` (or the current version of the GDPR consent text) to the fan object — allows proving which consent text each fan saw | V8 | 5 | 1 | L | 5 |
| 67 | `optIn` field: fan objects must include `optIn: true` written at sign-up — this is the explicit consent record; verify the field is being written, not just `consentVersion` | V8 | 5 | 1 | L | 5 |
| 68 | `consent_ts` field: fan objects must include `consent_ts: Date.now()` — the Unix timestamp of when consent was given; verified as spec'd but regression-check | V8 | 4 | 1 | L | 5 |
| 69 | Netlify function environment variables: verify `netlify.toml` does not hardcode any API keys — all keys must be in Netlify environment variables, not in the config file | FN | 5 | 1 | H | 5 |
| 70 | `.env.example` file: the `.env.example` file exists (shown in git status) — verify it lists all required environment variable names with placeholder values but no real keys | FN | 3 | 1 | L | 5 |
| 71 | `.gitignore` for `.env`: verify `.env` and `.env.local` are in `.gitignore` — no real keys must ever be committed | FN | 5 | 1 | H | 5 |
| 72 | CORS on Netlify functions: verify Netlify functions return correct CORS headers — `Access-Control-Allow-Origin: https://ablemusic.co` (not `*`) for functions that handle sensitive data | FN | 4 | 2 | M | 5 |
| 73 | Function timeout handling: Netlify functions have a 10s timeout on the free tier — all functions must respond or fail within 9s; the Ticketmaster import is the most likely to timeout on slow external APIs | FN | 3 | 2 | M | 5 |
| 74 | Rate limiting consideration: without a backend, an attacker can call `fan-confirmation.js` rapidly to abuse Resend's free tier (100/day) — consider a simple token rate limit or check origin header | FN | 3 | 2 | M | 5 |
| 75 | Input sanitisation for HTML: any data from user input that might be placed into HTML attributes (e.g. `href`, `src`) must be validated — a `javascript:` URL in a CTA URL field could become an XSS vector when rendered as `<a href="javascript:...">` | ALL | 5 | 2 | H | 5 |
| 76 | URL scheme validation: CTA URLs, platform links, support URLs, and ticket URLs entered in admin must be validated to only allow `https://` and `http://` schemes — block `javascript:`, `data:`, `vbscript:` | ADM | 5 | 2 | H | 5 |
| 77 | Supabase project URL in frontend: the Supabase project URL `https://jgspraqrnjrerzhnnhtb.supabase.co` is visible in frontend code — this is public and intended for the anon key flow; verify it's not confused with a secret endpoint | ALL | 2 | 1 | L | 5 |
| 78 | Netlify function logs: verify Netlify function logs are accessible in the Netlify dashboard and that errors are visible — this is the primary debugging tool post-launch for email failures | FN | 3 | 1 | L | 5 |
| 79 | Data migration schema verification: compare current `able_v3_profile` structure from a real localStorage snapshot to the schema in `docs/systems/data-architecture/SPEC.md` — document any discrepancy before Supabase migration begins | ALL | 5 | 2 | M | 6 |
| 80 | localStorage key naming: verify no localStorage key has been renamed since session 1 — `able_fans`, `able_clicks`, `able_views`, `able_v3_profile`, `able_shows`, `able_dismissed_nudges`, `able_tier`, `able_gig_expires`, `admin_visit_dates` — grep all files for any old key name | ALL | 5 | 1 | M | 6 |
| 81 | `able_starred_fans` removal from code: this key is deprecated (CONTEXT.md) — verify no new code writes to `able_starred_fans`; the star toggle must write to `fan.isStarred` in the `able_fans` array | ADM | 3 | 1 | L | 6 |
| 82 | Migration helper function: write and document a `migrateToSupabase()` function spec in `docs/systems/data-architecture/SPEC.md` — it must read all localStorage keys, format them for Supabase insert, call the API, and only clear localStorage after successful confirmation | ALL | 4 | 3 | M | 6 |
| 83 | LCP budget: run Lighthouse with network throttling set to "Slow 4G" — LCP must be ≤2.5s; if over, the most likely cause is a render-blocking Google Fonts request or an unoptimised hero image | V8 | 5 | 3 | M | 6 |
| 84 | INP budget: Interaction to Next Paint ≤200ms — measure by clicking the fan capture submit button and verifying the success state appears within 200ms | V8 | 4 | 2 | M | 6 |
| 85 | CLS budget: Cumulative Layout Shift ≤0.10 — verify by running Lighthouse on able-v8.html; the main CLS risk is images without explicit dimensions loading below the fold | V8 | 4 | 2 | M | 6 |
| 86 | Service worker security: when a service worker is added, its `install` event must use a cache name with a version suffix — `able-v1-cache` — and the `activate` event must delete all old cache versions to prevent serving stale CSP headers | FN | 3 | 2 | M | 6 |
| 87 | HTTPS-only: all external requests from Netlify functions must use `https://` — no `http://` API calls; verify every `fetch()` in function files uses https | FN | 4 | 1 | M | 6 |
| 88 | API key rotation plan: document in `CONTEXT.md` the procedure for rotating the Supabase anon key and Resend API key — anon key rotation requires updating all 4 HTML files; document which constant to update | ALL | 3 | 2 | L | 6 |
| 89 | Netlify deploy key: verify Netlify deploy uses a deploy key specific to the `v2-simplified` branch, not a personal access token — prevents accidental deployment of the wrong branch | FN | 3 | 1 | L | 6 |
| 90 | `_headers` file completeness: verify `netlify.toml` or `_headers` sets security headers on all routes, not just `/` — particularly on `/admin` (strictest headers), `/api/*` (CORS + no-cache), and `/*.html` | FN | 5 | 2 | M | 6 |
| 91 | Content-Security-Policy report-only mode: before enforcing CSP in production, run in `Content-Security-Policy-Report-Only` mode for one week to capture any violations without breaking the product | FN | 3 | 2 | M | 6 |
| 92 | Subresource Integrity for CDN scripts: add `integrity` and `crossorigin` attributes to the Supabase CDN `<script>` tag — `<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2" integrity="sha384-..." crossorigin="anonymous">` | ALL | 4 | 2 | M | 6 |
| 93 | PostHog data residency: PostHog is configured with `app.posthog.com` (US hosted) — if GDPR compliance requires EU data residency, consider switching to `eu.posthog.com` or self-hosting | FN | 4 | 2 | M | 6 |
| 94 | Netlify functions HTTP method check: every Netlify function must check `event.httpMethod` and return 405 for unexpected methods — a POST-only function must explicitly reject GET requests | FN | 3 | 2 | M | 6 |
| 95 | Function input size limit: add a check in every Netlify function for request body size — `if (event.body.length > 10000) return { statusCode: 413 }` — prevents large payload attacks | FN | 3 | 1 | M | 6 |
| 96 | Session fixation: when Supabase magic link auth is added, verify the session token is regenerated after login — a fixed session ID from pre-auth can be exploited | FN | 4 | 2 | H | 6 |
| 97 | Dependency vulnerability scan: run `npm audit` on any Netlify function dependencies (`package.json`) — all critical and high severity vulnerabilities must be resolved before launch | FN | 4 | 2 | M | 6 |
| 98 | `localStorage` vs `sessionStorage`: verify that truly session-only data (`able_session_viewed`, fan visit flags) uses `sessionStorage` not `localStorage` — mixing them means session data persists across tabs and browser restarts | ALL | 3 | 2 | M | 6 |
| 99 | Security documentation: create a `docs/systems/SECURITY-CHECKLIST.md` file listing all security controls in place (CSP, input validation, XSS prevention, key management) — serves as a handoff document if a security reviewer inspects the codebase | ALL | 3 | 3 | L | 6 |
| 100 | Phase 8 closure: run OWASP ZAP or a similar automated security scanner against the Netlify preview URL of `able-v8.html` and `admin.html` — fix all high and critical findings before launch | ALL | 5 | 4 | H | 6 |

---

## Phase 9 — Legal & Accessibility
*Must be correct before the first real user touches the product. GDPR failure is a fine. WCAG failure is a lawsuit and a user left behind.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | GDPR Article 7 consent on fan capture: the fan sign-up form must display visible consent text before the submit button: "By signing up, you agree to receive updates from [Artist Name]. ABLE stores your email on their behalf. [Privacy policy]" — "Just the artist. No spam." is COPY not consent and is not GDPR compliant alone | V8 | 5 | 2 | M | 1 |
| 2 | Artist as data controller: the GDPR consent text must name the artist as the data controller and ABLE as the data processor — verify `privacy.html` includes this distinction and the fan capture form consent text references the correct roles | V8 | 5 | 2 | M | 1 |
| 3 | No pre-ticked boxes: the fan capture form must NOT have a pre-ticked consent checkbox — if using a checkbox, it must be unchecked by default; if using implied consent (submit button), the consent text adjacent to the button must be sufficient | V8 | 5 | 1 | M | 1 |
| 4 | `privacy.html` existence and link: verify `privacy.html` exists (shown in git status as a new file) and is linked from the fan capture form and the site footer — an unlinked privacy policy is not compliant | V8 | 5 | 1 | L | 1 |
| 5 | `privacy.html` content — Article 13: must cover: (1) data controller identity (artist), (2) data processor identity (ABLE), (3) what data is collected (email, consent timestamp, sign-up source), (4) legal basis for processing (consent), (5) how to withdraw consent (unsubscribe), (6) how to request data export, (7) how to request deletion, (8) how long data is retained | V8 | 5 | 4 | M | 1 |
| 6 | `terms.html` existence and link: verify `terms.html` exists (shown in git status) and is linked from the site footer on all pages — a missing ToS leaves ABLE with no legal protection | ALL | 5 | 1 | L | 1 |
| 7 | `terms.html` content: must cover: (1) acceptable use (no illegal content, no impersonation), (2) content ownership (artist owns their content), (3) fan data ownership (artist owns fan list), (4) ABLE's right to suspend (breach of ToS), (5) limitation of liability, (6) governing law (UK law) | ALL | 5 | 4 | M | 1 |
| 8 | Fan data export — every tier: CSV export of `able_fans` must be accessible in admin.html on every tier including free — verify `checkTierGate()` does NOT gate the export button — fan data ownership is a core product promise | ADM | 5 | 1 | L | 1 |
| 9 | CSV export fields: the export must include: email, signup timestamp (ISO format), source attribution (`?ref=` value), campaign state at signup, `consentVersion` — verify `buildFanCSV()` or equivalent includes all these fields | ADM | 5 | 2 | M | 1 |
| 10 | Fan unsubscribe in email: every fan email (confirmation, future broadcasts) must include an unsubscribe link — in `fan-confirmation.js`, verify the email template includes a link to an unsubscribe page or mechanism | FN | 5 | 2 | M | 1 |
| 11 | Fan unsubscribe mechanism: an unsubscribe action must remove the fan from `able_fans` localStorage (V1) and mark them as unsubscribed in Supabase (V2) — verify an unsubscribe page or endpoint exists | FN | 5 | 3 | M | 1 |
| 12 | No non-essential cookies: verify no third-party cookie is set without consent — PostHog may set a cookie; verify PostHog is configured with `persistence: 'localStorage+cookie'` and that the cookie is first-party (`.ablemusic.co` domain) | ALL | 4 | 2 | M | 1 |
| 13 | localStorage is not a cookie: UK ICO guidance confirms localStorage does not require a cookie consent banner when used for first-party functional purposes — document this decision in `docs/systems/legal-compliance/SPEC.md` so it's not reconsidered each session | ALL | 3 | 1 | L | 1 |
| 14 | WCAG 2.2 AA — keyboard navigation: every page must be fully navigable by Tab key alone — verify Tab order is logical (follows visual order) and there are no keyboard traps except inside modals | ALL | 5 | 4 | M | 2 |
| 15 | Skip link: all pages must have a "Skip to main content" link as the first focusable element — verify `<a href="#main" class="skip-link">Skip to main content</a>` exists; session 18 improved the skip link on able-v7.html — verify this is on V8 and admin | ALL | 5 | 2 | L | 2 |
| 16 | Tab order on admin: admin.html bottom nav is `position: fixed` — verify it appears in a logical tab order relative to the page content; ideally at the end of the tab order so keyboard users can navigate content first | ADM | 4 | 2 | M | 2 |
| 17 | Focus trap in modals: admin modals/sheets must trap focus (Tab key cycles through modal focusable elements only) and release on close (Escape key returns focus to the trigger element) | ADM | 5 | 3 | M | 2 |
| 18 | World map focus trap: `admin.html World map focus trap + keyboard navigation (WCAG 2.2)` is confirmed in STATUS.md — regression-check after any world map changes | ADM | 4 | 1 | L | 2 |
| 19 | No `tabindex > 0`: no element must have `tabindex="1"` or higher — positive tabindex values override the natural tab order and break keyboard navigation; only `tabindex="0"` (adds to natural order) and `tabindex="-1"` (removes from order) are acceptable | ALL | 4 | 2 | L | 2 |
| 20 | `tabindex="-1"` on modal backdrop: the modal backdrop must have `tabindex="-1"` to prevent it from receiving focus, while still being focusable programmatically for the focus trap | ADM | 3 | 1 | L | 2 |
| 21 | Form accessibility — labels: every form field must have an associated `<label>` with a matching `for` attribute, OR an `aria-label` attribute, OR an `aria-labelledby` referencing a visible label — no unlabelled inputs | ALL | 5 | 3 | M | 2 |
| 22 | Fan capture email label: the fan email input must have a visible or accessible label — `<label for="fanEmail">Your email</label>` or `aria-label="Your email address"` | V8 | 5 | 1 | L | 2 |
| 23 | Error message association: when a form field has an error, the error message must be associated with the field via `aria-describedby` — `<input aria-describedby="emailError">` and `<div id="emailError" role="alert">` | V8 | 5 | 2 | M | 2 |
| 24 | `aria-required` on required fields: all required form fields must have `aria-required="true"` — this is announced by screen readers as "required" | ALL | 4 | 2 | L | 2 |
| 25 | `aria-invalid` on error state: when a field has a validation error, set `aria-invalid="true"` on the input — this is announced by screen readers as "invalid" | ALL | 4 | 2 | L | 2 |
| 26 | `role="alert"` on errors: error messages must use `role="alert"` — this causes screen readers to announce the error immediately without requiring the user to navigate to it | ALL | 5 | 2 | M | 2 |
| 27 | `aria-live="polite"` on success: success messages (fan signed up, profile saved) must use `aria-live="polite"` — announced when the user is not actively interacting | ALL | 4 | 2 | M | 2 |
| 28 | `aria-live="polite"` on loading states: loading state copy must be in an `aria-live` region — "Importing your Spotify profile…" must be announced to screen readers | ALL | 3 | 2 | M | 2 |
| 29 | Fan cap progress bar ARIA: `admin.html V8 Fan cap enforcement in admin + progress bar with ARIA live region` confirmed in STATUS.md — verify the ARIA live region still fires on fan cap progress update | ADM | 4 | 1 | L | 2 |
| 30 | Image `alt` text: every `<img>` tag must have either a non-empty `alt` attribute (for meaningful images) or `alt=""` with `role="presentation"` (for decorative images) — verified at WCAG 1.1.1 | ALL | 5 | 2 | L | 3 |
| 31 | Artwork image alt: the hero artwork on able-v8.html must have `alt="[Artist Name] — [Release Title]"` or similar meaningful alt text, not `alt="artwork"` | V8 | 4 | 1 | L | 3 |
| 32 | Icon alt text: all meaningful icons (platform logos in pills, nav icons) must have `aria-label` on the button/link containing them — `<button aria-label="View on Spotify">` | ALL | 5 | 2 | L | 3 |
| 33 | Decorative icons: purely decorative icons (bullets, dividers, flourishes) must have `aria-hidden="true"` | ALL | 4 | 1 | L | 3 |
| 34 | SVG accessibility: all inline SVGs that are meaningful must have `<title>` element and `role="img"` — decorative SVGs must have `aria-hidden="true"` | ALL | 4 | 2 | M | 3 |
| 35 | `aria-expanded` on accordions: FAQ accordion on landing.html must have `aria-expanded="true/false"` on the trigger button that toggles the panel — screen readers announce this state | LND | 4 | 1 | L | 3 |
| 36 | `aria-controls` on accordions: the FAQ trigger button must have `aria-controls="panel-id"` referencing the panel it controls | LND | 3 | 1 | L | 3 |
| 37 | `aria-expanded` on campaign mode: the campaign mode selector in admin must use `aria-expanded` if it has a dropdown or accordion behaviour | ADM | 3 | 1 | L | 3 |
| 38 | `aria-selected` on tab navigation: the admin bottom nav tabs must use `role="tab"` and `aria-selected="true/false"` to communicate the active tab to screen readers | ADM | 4 | 2 | M | 3 |
| 39 | `role="tabpanel"` on tab content: each content panel corresponding to an admin tab must have `role="tabpanel"` and `aria-labelledby` referencing the tab button | ADM | 3 | 2 | M | 3 |
| 40 | `aria-current="page"` on nav: the active page in site-wide navigation (if any) must have `aria-current="page"` on the nav link | ALL | 3 | 1 | L | 3 |
| 41 | Heading hierarchy: all 4 pages must have a logical heading hierarchy — one `<h1>` per page, `<h2>` for major sections, `<h3>` for sub-sections — no skipping levels (e.g. `<h1>` → `<h3>` with no `<h2>`) | ALL | 5 | 3 | M | 3 |
| 42 | `<h1>` on profile: able-v8.html must have exactly one `<h1>` — the artist name — verified in session 18 but regression-check | V8 | 4 | 1 | L | 3 |
| 43 | `<h1>` on admin: admin.html must have one `<h1>` — either "ABLE" or "Dashboard" — that is present in the DOM even if visually hidden via `sr-only` class | ADM | 4 | 1 | L | 3 |
| 44 | `<h1>` on landing: landing.html `<h1>` must be the main page headline — the artist pain hook — not a hidden off-screen element | LND | 4 | 1 | L | 3 |
| 45 | `<h1>` on start.html: wizard must have a page-level `<h1>` that either remains constant ("Set up your page") or updates per step — not absent entirely | STR | 3 | 1 | L | 3 |
| 46 | `privacy.html` heading hierarchy: session 19 fixed heading hierarchy in privacy.html and terms.html — regression-check | V8 | 3 | 1 | L | 3 |
| 47 | Fieldset on related inputs: campaign state radio buttons in admin must be wrapped in `<fieldset>` with `<legend>Campaign mode</legend>` — this groups them correctly for screen readers | ADM | 4 | 2 | M | 4 |
| 48 | Fieldset on vibe selection: the vibe selection in start.html must use `<fieldset><legend>Your sound</legend>` wrapping the vibe radio/button inputs | STR | 3 | 2 | M | 4 |
| 49 | Fieldset on feel selection: same as vibe — `<fieldset><legend>Your feel</legend>` | STR | 3 | 2 | M | 4 |
| 50 | `<button>` not `<div onClick>`: all interactive elements must use semantic HTML — `<button>` not `<div onclick>`, `<a>` not `<span onclick>` — grep for `onclick` on non-interactive elements | ALL | 5 | 3 | M | 4 |
| 51 | `<a>` vs `<button>`: links (`<a>`) navigate to a URL; buttons (`<button>`) perform an action — verify no `<a href="#">` is used for button actions (should be `<button>`), and no `<button>` is used for navigation (should be `<a>`) | ALL | 4 | 3 | M | 4 |
| 52 | Navigation landmark: the admin bottom nav must use `<nav aria-label="Main navigation">` — screen readers announce landmark regions | ADM | 4 | 1 | L | 4 |
| 53 | Main landmark: each page must have `<main>` wrapping the primary content — this is a WCAG landmark requirement | ALL | 4 | 1 | L | 4 |
| 54 | Header landmark: the page header area must use `<header>` — the site `<header>` (logo + nav) | ALL | 3 | 1 | L | 4 |
| 55 | Footer landmark: the page footer must use `<footer>` — including the "Made with ABLE" footer on the artist profile | ALL | 3 | 1 | L | 4 |
| 56 | `role="complementary"` on admin sidebar/panels: Campaign HQ and other secondary admin panels that are supplementary to the main content could use `<aside>` or `role="complementary"` | ADM | 2 | 2 | L | 4 |
| 57 | VoiceOver test on able-v8.html: run the page through VoiceOver (iOS) and verify: (1) artist name is announced as `<h1>`, (2) hero CTAs are announced as buttons with their label, (3) fan capture form is navigable and the email input is announced with its label | V8 | 5 | 3 | M | 4 |
| 58 | VoiceOver test on admin.html: run through VoiceOver and verify: (1) tab navigation announces active tab, (2) Campaign HQ radio buttons are grouped, (3) fan list rows are announced with email and star status | ADM | 5 | 3 | M | 4 |
| 59 | Screen reader fan count: the fan count stat card in admin must announce correctly via screen reader — `<div aria-label="47 fans signed up">47</div>` with a visually hidden label | ADM | 3 | 2 | M | 4 |
| 60 | Screen reader countdown: the pre-release countdown in able-v8.html must announce as text — `<div role="timer" aria-live="off" aria-label="3 days until release">3</div>` — `aria-live="off"` prevents it from announcing every second | V8 | 4 | 2 | M | 4 |
| 61 | `aria-label` on icon-only buttons: all icon-only buttons (close, delete, edit, star fan, dismiss nudge) must have `aria-label` describing the action — verified in session 18 but regression-check | ALL | 5 | 2 | L | 4 |
| 62 | `aria-hidden` on decorative elements: the `✦` character in "Made with ABLE ✦" and any purely decorative emoji must have `aria-hidden="true"` — they add no information for screen reader users | V8 | 3 | 1 | L | 4 |
| 63 | Status message copy for screen readers: when campaign state changes, the change must be announced — an `aria-live="polite"` region that reads "Campaign mode changed to Gig" when gig is enabled | ADM | 3 | 2 | M | 5 |
| 64 | `aria-pressed` on toggle buttons: the gig mode toggle button must have `aria-pressed="true/false"` — this communicates the toggle state to screen readers | ADM | 4 | 1 | L | 5 |
| 65 | `aria-checked` on custom checkboxes: if any admin UI uses a custom checkbox (not a native `<input type="checkbox">`), it must have `role="checkbox"` and `aria-checked` | ADM | 4 | 1 | L | 5 |
| 66 | Admin "noindex" tag: `admin.html` already has `<meta name="robots" content="noindex,nofollow">` per STATUS.md — regression-check this is still present | ADM | 4 | 1 | L | 5 |
| 67 | Fan.html "noindex" tag: same — already present per STATUS.md — regression-check | FN | 3 | 1 | L | 5 |
| 68 | Start.html robots: `start.html` should also have `noindex` to prevent the wizard from appearing in search results — verify | STR | 3 | 1 | L | 5 |
| 69 | Colour-independent information: verify all information conveyed by colour alone (e.g. a red error border) also has a text or icon component — WCAG 1.4.1 | ALL | 5 | 2 | L | 5 |
| 70 | Text resize to 200%: all 4 pages must remain usable when browser text size is increased to 200% — verify no content is truncated, no overlap occurs, no functionality is lost | ALL | 4 | 3 | M | 5 |
| 71 | Zoom to 400%: at 400% browser zoom (WCAG 1.4.10 Reflow), all content must be available in a single column without horizontal scroll — verify by setting browser zoom to 400% | ALL | 4 | 3 | M | 5 |
| 72 | Target size: all interactive elements must have a CSS computed click/tap area of at least 24×24px (WCAG 2.2 Target Size minimum) with 44×44px recommended — run a Playwright script to measure all interactive element computed sizes | ALL | 5 | 4 | M | 5 |
| 73 | `autocomplete` for personal data: the fan email field must have `autocomplete="email"` — WCAG 1.3.5 Identify Input Purpose requires autocomplete on fields collecting personal information | V8 | 4 | 1 | L | 5 |
| 74 | Lang attribute: all 4 HTML pages must have `<html lang="en">` — screen readers use this to select the correct pronunciation engine | ALL | 5 | 1 | L | 5 |
| 75 | Lang for foreign content: if any page includes content in a different language (e.g. a French artist's bio), that element must have `lang="fr"` | V8 | 2 | 2 | L | 5 |
| 76 | Reading order: the DOM order of elements must match the visual order — verify no CSS uses `order` property in flexbox in a way that diverges reading order from visual order | ALL | 4 | 2 | M | 5 |
| 77 | `abbr` for abbreviations: any abbreviation (CTA, UTM, QR) in user-facing copy must be expanded on first use or use `<abbr title="Call to Action">CTA</abbr>` | ALL | 2 | 2 | L | 5 |
| 78 | Time format: show dates and times rendered in HTML must use `<time datetime="2026-04-14T19:30">Monday 14 April, 7:30pm</time>` — machine-readable datetime attribute | V8 | 3 | 2 | L | 5 |
| 79 | WCAG automated scan: run `axe-core` (via Playwright MCP browser evaluate) against all 4 pages and document all violations — target zero violations at WCAG 2.2 AA level | ALL | 5 | 3 | M | 6 |
| 80 | `pa11y` command line scan: run `pa11y --standard WCAG2AA https://[preview-url]/able-v8.html` and capture the report — fix all errors (not warnings) before launch | ALL | 5 | 2 | M | 6 |
| 81 | Cookie consent for PostHog: PostHog sets a first-party cookie — under UK GDPR guidance, strictly necessary cookies don't require consent, but analytics cookies do — determine if PostHog tracking qualifies as strictly necessary or requires consent | ALL | 4 | 3 | M | 6 |
| 82 | Privacy policy link in footer: every page must have a footer link to `privacy.html` — verify the "Made with ABLE" footer on able-v8.html includes this link | V8 | 4 | 1 | L | 6 |
| 83 | Terms of service link in footer: every page must have a footer link to `terms.html` — verify all 4 pages' footers include this | ALL | 4 | 1 | L | 6 |
| 84 | Age verification: ABLE's target users (artists) are 18+ in practice but no age gate exists — the Terms of Service must state the minimum age (16 in the UK under GDPR Article 8) | ALL | 3 | 2 | L | 6 |
| 85 | GDPR right of access: `privacy.html` must include contact email for GDPR Subject Access Requests — typically `privacy@ablemusic.co` | V8 | 4 | 1 | L | 6 |
| 86 | GDPR right to erasure: the fan list in admin must include a delete button per fan row that removes the fan from `able_fans` permanently — this is the GDPR right to erasure (right to be forgotten) | ADM | 5 | 2 | M | 6 |
| 87 | GDPR right to portability: the CSV export button is the portability mechanism — verify the export format is machine-readable (CSV, JSON) not just a PDF | ADM | 4 | 1 | L | 6 |
| 88 | Data retention policy: `privacy.html` must state how long fan data is retained — "Your data is retained for as long as the artist's ABLE account is active. You can request deletion at any time." | V8 | 4 | 2 | L | 6 |
| 89 | ICO registration: if ABLE processes personal data on behalf of artists (as a data processor), registration with the UK ICO may be required — research and document the requirement; estimated cost £40-£60/year | ALL | 5 | 3 | M | 6 |
| 90 | PECR compliance: the fan confirmation email constitutes "direct marketing" under PECR (UK Privacy and Electronic Communications Regulations) — the fan must have explicitly opted in; implied consent from a bio link sign-up may not be sufficient — get legal clarity on this | FN | 5 | 3 | M | 6 |
| 91 | Error pages: custom 404 and 500 error pages must be defined in `netlify.toml` — `[[redirects]] from = "/*" to = "/404.html" status = 404` — the default Netlify 404 page is jarring | FN | 3 | 2 | L | 6 |
| 92 | Social card alt: the `og:image` on each page must have an `alt` attribute in the `<meta>` for platforms that support it (Twitter/X `twitter:image:alt`) | ALL | 3 | 1 | L | 6 |
| 93 | WCAG compliance statement: add a brief WCAG 2.2 AA compliance statement to `privacy.html` or an `accessibility.html` page — "ABLE aims to meet WCAG 2.2 AA. Report accessibility issues to accessibility@ablemusic.co" | ALL | 2 | 2 | L | 6 |
| 94 | Screen reader testing report: document the results of a VoiceOver (iOS) test on able-v8.html and admin.html in `docs/systems/qa-testing/` — specific pass/fail per WCAG success criterion | ALL | 4 | 3 | L | 6 |
| 95 | Keyboard navigation test report: document the results of a full keyboard navigation test (Tab, Shift+Tab, Enter, Space, Escape, Arrow keys) on all 4 pages | ALL | 4 | 3 | L | 6 |
| 96 | Contrast test report: document all confirmed WCAG contrast ratios for each text/background pair on each page and theme in `docs/systems/COLOUR-AUDIT.md` (cross-reference Phase 6) | ALL | 4 | 3 | L | 6 |
| 97 | GDPR compliance sign-off: before first real user, James must personally review and approve the GDPR consent mechanism, privacy policy, and data deletion mechanism — document the approval with a date in `docs/systems/legal-compliance/SPEC.md` | ALL | 5 | 2 | L | 6 |
| 98 | WCAG audit sign-off: before first real user, run a full WCAG 2.2 AA audit using axe-core + manual VoiceOver test — document the results and confirm zero critical violations | ALL | 5 | 4 | M | 6 |
| 99 | Legal review: have `terms.html` reviewed by a UK-qualified solicitor or legal tech service (e.g. Docular) before launch — a self-drafted ToS may have gaps in UK consumer law compliance | ALL | 5 | 5 | M | 6 |
| 100 | Phase 9 closure: run a combined GDPR + WCAG checklist walkthrough: (1) complete a fan sign-up as a test fan, (2) verify the consent text was shown, (3) verify the fan appears in admin with correct fields, (4) export the fan CSV, (5) delete the fan, (6) verify deletion removes them from the fan list | ALL | 5 | 3 | M | 6 |

---

## Phase 10 — Launch Readiness
*The final gate. Every item in this phase is something that will break in front of a real user if not checked. Do them in order.*

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | Cross-page data flow test: complete a full end-to-end journey and verify data flows correctly — (1) open `start.html` in a clean browser, (2) complete wizard (Spotify import OR manual), (3) verify `able_v3_profile` is written correctly (check in console), (4) open `able-v8.html` and confirm the profile renders, (5) open `admin.html` and confirm all data loads without errors | ALL | 5 | 2 | M | 1 |
| 2 | Wizard → profile link: the wizard done screen "See your page →" CTA must link to `able-v8.html` (not `able-v7.html`) — this was corrected in session 17; verify no new regressions | STR | 5 | 1 | L | 1 |
| 3 | Admin → profile link: the "View your page" button in admin topbar must link to `able-v8.html` (not `able-v7.html`) — 5 stale refs were corrected in session 17; confirm all 5 are still correct | ADM | 5 | 1 | L | 1 |
| 4 | Deploy branch: all V8 work is on `v2-simplified` branch; the live site deploys from `main` — James must explicitly sign off the merge; document what to check before merging: (1) no P0 bugs open, (2) all current-session test results green, (3) `docs/STATUS.md` accurate | ALL | 5 | 3 | H | 1 |
| 5 | `netlify.toml` routing: verify the `netlify.toml` correctly routes artist slugs to `able-v8.html` — `[[redirects]] from = "/:slug" to = "/able-v8.html" status = 200` with `force = false` so static files are served correctly | FN | 5 | 2 | M | 1 |
| 6 | Netlify functions directory: verify `netlify.toml` `[functions] directory = "netlify/functions"` (or wherever the functions live) is correctly set — a misconfigured functions directory means all Netlify functions return 404 | FN | 5 | 1 | H | 1 |
| 7 | Static file 404s: verify all static file references in all 4 pages resolve correctly — `shared/tokens.css`, `shared/able.js`, `assets/og/og-landing.png`, `og-default.jpg` — request each file on the Netlify preview and verify 200 response | ALL | 5 | 2 | M | 1 |
| 8 | `og-default.jpg` deployment: the file exists locally (`17KB`, Gemini-generated) — verify it is committed to git and will be deployed to `https://ablemusic.netlify.app/og-default.jpg` — this is a known open item in STATUS.md | ALL | 5 | 1 | M | 1 |
| 9 | RESEND_API_KEY environment variable: verify this is set in the Netlify dashboard under Environment Variables — without it, `fan-confirmation.js` silently skips sending; this is James's manual task at resend.com/domains + netlify.com/sites | FN | 5 | 1 | H | 1 |
| 10 | Resend domain verification: SPF, DKIM, and DMARC DNS records for `ablemusic.co` must be set at the DNS registrar per Resend's domain verification guide — until this is done, confirmation emails will land in spam or not send | FN | 5 | 2 | H | 1 |
| 11 | Email deliverability test: after domain verification, send a test fan confirmation email and verify: (1) it arrives in inbox (not spam), (2) the artist name appears correctly, (3) the fan dashboard link has the correct `?artist=slug&ref=email-confirm` parameter, (4) the "Stop getting updates" unsubscribe link works | FN | 5 | 2 | M | 1 |
| 12 | PWA manifest.json: verify `manifest.json` exists and is correctly linked in all 4 pages — `<link rel="manifest" href="/manifest.json">` — the manifest must include `name`, `short_name`, `start_url`, `icons`, `display: standalone`, `theme_color`, `background_color` | ALL | 5 | 2 | M | 2 |
| 13 | PWA icons: verify PWA icons at 192×192 and 512×512 exist at the paths specified in `manifest.json` — icons must not be broken or blurry on real devices | ALL | 5 | 2 | M | 2 |
| 14 | iOS meta tags for PWA: all pages must have `<meta name="apple-mobile-web-app-capable" content="yes">` and `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">` — these are required for the app to open full-screen when installed | ALL | 4 | 1 | L | 2 |
| 15 | iOS home screen icon: `<link rel="apple-touch-icon" href="/icons/icon-180x180.png">` must be present — iOS ignores the manifest icons and requires this specific tag | ALL | 4 | 1 | L | 2 |
| 16 | PWA `start_url`: `manifest.json` `start_url` must be set to the correct page — likely `/able-v8.html` or `/` (redirected to the artist's page) — not an internal development URL | ALL | 4 | 1 | M | 2 |
| 17 | Service worker cache: STATUS.md notes "No service worker: PWA is installable but not offline-capable" — implement a basic service worker that caches the HTML, CSS, and JS of `able-v8.html` for offline viewing; fan sign-up offline should queue and send when back online | V8 | 4 | 4 | M | 2 |
| 18 | Service worker registration: if a service worker is added, it must be registered with `navigator.serviceWorker.register('/sw.js')` and the registration must be wrapped in a feature detection check | ALL | 4 | 2 | M | 2 |
| 19 | Service worker update: the service worker must handle updates gracefully — new SW must wait for old SW to release control; `skipWaiting()` should not be used without informing the user | ALL | 3 | 2 | M | 2 |
| 20 | Offline fallback: if a service worker is added, the offline fallback page must show the cached profile data (from localStorage) with a message "You're offline — showing your last saved page" rather than the browser's default offline error | V8 | 3 | 3 | M | 2 |
| 21 | OG card — able-v8.html: verify the `og:title`, `og:description`, `og:image`, and `og:url` are correct for the artist profile — `og:title` must be the artist name, `og:description` must be a truncated bio | V8 | 5 | 2 | M | 3 |
| 22 | OG card — admin.html: admin must have `noindex` + `og:title: "ABLE Dashboard"` — no sensitive profile data in OG meta; already confirmed `noindex` in STATUS.md | ADM | 3 | 1 | L | 3 |
| 23 | OG card — landing.html: `og:title` must be "ABLE — Your fans, your relationship." or similar; `og:image` must be the landing-specific OG image `og-landing.png` at 1200×630 | LND | 5 | 2 | M | 3 |
| 24 | OG card — start.html: wizard should have a basic OG meta with `noindex` and `og:title: "Set up your ABLE page"` | STR | 2 | 1 | L | 3 |
| 25 | `og:image` `https://` URL: all `og:image` values must be absolute `https://` URLs — `data:` URIs are fixed (session 13) but regression-check that `injectSEO()` still enforces this | V8 | 5 | 1 | L | 3 |
| 26 | Twitter card meta: all 4 pages must have `<meta name="twitter:card" content="summary_large_image">` and `<meta name="twitter:image">` matching the OG image | ALL | 4 | 1 | L | 3 |
| 27 | OG card test: test each page's OG card on a real sharing tool — opengraph.xyz or `https://cards-dev.twitter.com/validator` — verify the image appears at correct aspect ratio (1200×630) and the title/description are correct | ALL | 5 | 2 | M | 3 |
| 28 | Page `<title>` tags: all 4 pages must have unique `<title>` tags — 50-60 characters each — `able-v8.html`: "[Artist Name] — Music, Shows & More on ABLE", `landing.html`: "ABLE — The music-native bio link for independent artists", `admin.html`: "ABLE Dashboard", `start.html`: "Set up your ABLE page" | ALL | 4 | 1 | L | 3 |
| 29 | `<meta description>` tags: all 4 pages must have unique meta descriptions — 150-160 characters; `able-v8.html` description must be the artist bio truncated to 155 chars | ALL | 4 | 2 | M | 3 |
| 30 | `<link rel="canonical">` tags: all 4 pages must have canonical tags pointing to their production URL — `https://ablemusic.co/able-v8.html` for the profile, etc. — verify `injectSEO()` sets the canonical correctly | ALL | 4 | 1 | L | 3 |
| 31 | `meta description id` P0 bug: CONTEXT.md P0 bug list includes "`able-v8.html` — `<meta name="description">` has no `id` → add `id="meta-description"`" — verify this is fixed and `injectSEO()` can update the description dynamically | V8 | 4 | 1 | L | 3 |
| 32 | SEO for artist profile: able-v8.html must render the artist name and bio in the HTML source (not only via JS) for search engine indexing — if profile data is written by JS after page load, Google may not index it; consider server-side rendering or `<noscript>` fallback | V8 | 4 | 3 | H | 3 |
| 33 | `robots.txt`: verify a `robots.txt` file exists at the domain root allowing crawling of the landing page and artist profiles, while disallowing admin and wizard pages | FN | 3 | 1 | L | 3 |
| 34 | Sitemap: a basic `sitemap.xml` at the domain root improves crawlability — at minimum, include `landing.html` and `privacy.html` | FN | 3 | 2 | L | 3 |
| 35 | Safari iOS 17 test: open `able-v8.html` on a real iPhone running iOS 17+ in Safari — verify: no layout breaks, no JS errors in Safari console, fan sign-up works, theme renders correctly | V8 | 5 | 3 | M | 4 |
| 36 | Chrome Android test: open `able-v8.html` on an Android device in Chrome — verify: tap targets work, no horizontal scroll, PWA install prompt appears | V8 | 5 | 3 | M | 4 |
| 37 | Chrome desktop test: open all 4 pages in Chrome desktop — verify: layout scales correctly to 1440px, no visual regressions | ALL | 4 | 2 | L | 4 |
| 38 | Firefox desktop test: open all 4 pages in Firefox — verify: `backdrop-filter` fallback works, no CSS or JS errors in console | ALL | 4 | 2 | L | 4 |
| 39 | Safari macOS test: open all 4 pages in Safari on macOS — verify: font rendering is consistent with Chrome, no layout breaks | ALL | 4 | 2 | L | 4 |
| 40 | `localStorage` access in incognito: verify all 4 pages work in incognito mode where localStorage may be quota-limited — Safari in private mode throws a `SecurityError` on `localStorage.setItem`; verify `safeSet()` catches this | ALL | 5 | 2 | M | 4 |
| 41 | JavaScript disabled test: open `able-v8.html` with JavaScript disabled (Chrome DevTools) — the page should render at minimum the artist name, bio, and a message like "Enable JavaScript to see the full experience" — not a completely blank page | V8 | 3 | 3 | M | 4 |
| 42 | No console errors on page load: open all 4 pages in Chrome and verify the console shows zero errors on a clean page load — warnings are acceptable, errors must be investigated | ALL | 5 | 2 | M | 4 |
| 43 | No console errors after interactions: click through all major admin actions (save profile, enable gig mode, add snap card, export fans) and verify zero console errors during any interaction | ADM | 5 | 2 | M | 4 |
| 44 | Network requests audit: open Chrome DevTools Network tab and verify no 404s, no 500s, no slow requests (>2s) on any page load — particular attention to: Google Fonts, Supabase, PostHog, Netlify functions | ALL | 5 | 2 | M | 4 |
| 45 | First-artist full walkthrough: perform the complete first-artist journey manually in a fresh browser profile: (1) arrive at landing, (2) click primary CTA, (3) complete wizard (try both Spotify import and manual), (4) arrive at profile, (5) open admin, (6) add a CTA, (7) enable gig mode, (8) share profile URL, (9) sign up as a fan in incognito, (10) return to admin and see the fan | ALL | 5 | 4 | M | 4 |
| 46 | First-artist experience quality: after completing the walkthrough in #45, rate each step: did it feel intentional, premium, and ABLE? Note any step that felt like a form or a SaaS product and fix it before launch | ALL | 5 | 2 | M | 4 |
| 47 | Producer seeding walkthrough: simulate the producer acquisition mechanic — (1) set up a producer profile (admin.html with freelancer layers), (2) create a release with a credit (name, role, handle), (3) view the artist profile and click the credit link — verify the credit link works and the producer's page loads correctly | V8 | 5 | 3 | M | 4 |
| 48 | Fan journey walkthrough: simulate the fan journey — (1) arrive at artist profile from a UTM-tagged link (`?src=instagram`), (2) scroll through profile, (3) sign up as a fan, (4) receive confirmation email, (5) click the fan dashboard link in the email, (6) verify fan.html loads with the artist pre-followed | V8 | 5 | 3 | M | 5 |
| 49 | Gig mode journey: simulate a day-of-show scenario — (1) add a show with today's date and doors time in 1 hour, (2) verify auto-gig activates at doors time, (3) verify admin greeting shows "You're on tonight.", (4) sign up as a fan in gig mode, (5) verify the gig-specific fan capture copy appeared | ALL | 5 | 3 | M | 5 |
| 50 | Pre-release journey: simulate a pre-release scenario — (1) set release date 3 days in future, (2) verify profile shows countdown and pre-save CTA, (3) verify admin Campaign HQ shows "Pre-release" mode with explanation, (4) advance clock past release date, (5) verify profile auto-switches to "Live" state | ALL | 5 | 3 | M | 5 |
| 51 | 14-day window test: advance the clock to release date + 15 days and verify the profile auto-switches from "Live" to "Profile" state — this is the most common developer regression on the campaign state machine | V8 | 5 | 2 | M | 5 |
| 52 | Netlify deploy preview: create a deploy preview from the `v2-simplified` branch and share the preview URL — verify all 4 pages load correctly on the preview URL, not just locally | FN | 5 | 2 | M | 5 |
| 53 | DNS and domain: verify `ablemusic.co` DNS is correctly pointed to Netlify and HTTPS is active — the SSL certificate must be valid (not self-signed or expired) | FN | 5 | 2 | H | 5 |
| 54 | Custom domain routing: verify `ablemusic.co/[slug]` correctly serves `able-v8.html` — the slug routing in `netlify.toml` must work on the custom domain, not just the `.netlify.app` domain | FN | 5 | 2 | M | 5 |
| 55 | Netlify function cold start: Netlify functions on the free tier have cold start latency (~300ms) — verify that `fan-confirmation.js` handles the cold start delay gracefully (the fan sign-up must not time out the form from the user's perspective) | FN | 4 | 2 | M | 5 |
| 56 | Resend 100/day cap risk: the free Resend tier allows 100 emails/day — if the first artist's launch generates >100 fan sign-ups on day 1, confirmation emails will queue or fail; document this risk and the upgrade threshold (Resend Pro at $20/mo removes the cap) | FN | 4 | 2 | M | 5 |
| 57 | PostHog events verification: after deploying to the Netlify preview, visit `able-v8.html` and sign up as a fan — verify `page_viewed` and `fan_signed_up` events appear in the PostHog dashboard at `app.posthog.com` | V8 | 4 | 2 | M | 5 |
| 58 | UptimeRobot monitoring: set up UptimeRobot to monitor `ablemusic.co`, `ablemusic.co/admin.html`, and the fan-confirmation Netlify function endpoint — the free tier allows 5-minute checks; P0 from STATUS.md free-stack section | FN | 4 | 2 | L | 5 |
| 59 | First 10 artists target: the validation gate is 10 paying artists in 30 days via direct DM — document the outreach script and the specific 10 producers/artists to contact on launch day | ALL | 5 | 3 | L | 5 |
| 60 | Landing page → wizard conversion: verify the landing page primary CTA correctly opens `start.html` — test the click-through on the Netlify preview | LND | 5 | 1 | L | 5 |
| 61 | Landing pricing CTAs: verify all three pricing tier CTAs on landing.html link to the correct destination — free tier → `start.html`, Artist → `start.html` (same wizard, tier upgradeable in admin), Artist Pro → `start.html` | LND | 4 | 1 | L | 5 |
| 62 | Admin sign-in flow: verify the "Sign in" link on landing.html and admin.html loads the Supabase magic link auth flow — until Supabase auth is wired, this should link to a "coming soon" message or the wizard | LND | 4 | 2 | M | 5 |
| 63 | Mobile installation test: on a real iPhone, add `ablemusic.co` to the home screen — verify: (1) the icon shows correctly (not a blank rectangle or grey square), (2) the app opens in full-screen mode (no Safari chrome), (3) the profile renders correctly | ALL | 5 | 3 | M | 5 |
| 64 | Android installation test: same as above on Android Chrome — verify PWA install prompt fires and home screen icon is correct | ALL | 4 | 3 | M | 5 |
| 65 | Dark mode system preference: on an iPhone set to dark mode, visit `ablemusic.co` and verify the correct ABLE dark theme is applied immediately without a flash of a different theme | V8 | 3 | 2 | M | 5 |
| 66 | docs/STATUS.md accuracy: verify every item listed as "done" in STATUS.md is actually done — walk through the 10 most recent completions and spot-check each one in the code | ALL | 4 | 2 | L | 6 |
| 67 | CONTEXT.md accuracy: verify the active file list in CONTEXT.md matches reality — `able-v8.html` should be listed as active (not `able-v7.html`); the superseded files should match what's actually in `_archive/` | ALL | 4 | 1 | L | 6 |
| 68 | `PROJECT_INDEX.md` currency: verify the section IDs, JS function names, and localStorage schema in `PROJECT_INDEX.md` match the current codebase — stale function references mislead future build sessions | ALL | 3 | 2 | L | 6 |
| 69 | `docs/FILE-STRUCTURE.md` currency: verify the 347-doc map is current — any new docs added in recent sessions must be added to the file structure map | ALL | 3 | 2 | L | 6 |
| 70 | `CONTEXT.md` P0 bug list currency: the P0 bug list in CONTEXT.md must be updated to reflect all fixed bugs — items marked as "done" must be moved to the "Already done" list | ALL | 4 | 1 | L | 6 |
| 71 | `docs/STATUS.md` known issues update: the known issues list must be current — the `ablemusic.netlify.app` deployment gap, `RESEND_API_KEY` missing, and Supabase views table issues are listed; verify none have been silently fixed without being removed from the list | ALL | 3 | 1 | L | 6 |
| 72 | Supabase connection URL currency: verify `CONTEXT.md` still shows the correct Supabase project URL `https://jgspraqrnjrerzhnnhtb.supabase.co` — if the project has been reset or recreated, all references must be updated | ALL | 3 | 1 | M | 6 |
| 73 | README or onboarding doc: before inviting a co-founder or VA, create a concise "start here" document that explains how to run the project locally, the file structure, and the key rules — link to `CONTEXT.md` as the primary orientation | ALL | 4 | 3 | L | 6 |
| 74 | `START-BUILDING.md` currency: verify `START-BUILDING.md` reflects the current session protocol — the 10-minute pre-build protocol (git status, read DESIGN-SPEC, confirm localStorage keys, Playwright screenshot, parse-check, theme spot-check, 375px check, banned-phrase scan) | ALL | 3 | 1 | L | 6 |
| 75 | DECISION-LOG.md entry: add an entry for the V8 migration decision (able-v7.html → able-v8.html) and the key decisions made in sessions 11-19 — ensures no context is lost between sessions | ALL | 3 | 2 | L | 6 |
| 76 | No `able-v7.html` stale references in code: grep all active pages for `able-v7.html` — must return zero hits; all links must reference `able-v8.html` | ALL | 5 | 1 | L | 6 |
| 77 | Playwright smoke test suite: verify the QA test suite (92/92 green per STATUS.md session 14) still passes after all Phase 1-9 changes — run `npx playwright test` and confirm no regressions | ALL | 5 | 3 | M | 6 |
| 78 | Playwright test coverage: add Playwright tests for the new features added in sessions 15-19 that are not yet covered — specifically: artist-welcome.js email, credits live link rendering, post-gig greeting, lifecycle stage system | ALL | 4 | 4 | M | 6 |
| 79 | Performance budget final check: run Lighthouse on `able-v8.html` on the Netlify preview URL (not localhost) — LCP ≤2.5s, INP ≤200ms, CLS ≤0.10, HTML ≤340KB gzipped — all must pass | V8 | 5 | 2 | M | 6 |
| 80 | Admin performance budget: run Lighthouse on `admin.html` — admin is JS-heavy due to localStorage reads; verify it renders meaningfully within 2s on a mid-range Android device | ADM | 4 | 2 | M | 6 |
| 81 | `able-v8.html` file rename check: CONTEXT.md notes `able-v7.html` is superseded by `able-v8.html` (2026-03-17) — verify the actual HTML file is named `able-v8.html` and not still `able-v7.html`; the git status shows `able-v7.html` exists but it may be the old file scheduled for archive | ALL | 5 | 1 | H | 6 |
| 82 | Archive old versions: `able-v3.html`, `able-v6.html`, and `able-v7.html` (superseded) should be moved to `_archive/` — they currently sit at the project root which creates confusion; confirm with James before archiving | ALL | 3 | 1 | M | 6 |
| 83 | `index.html` redirect: verify `index.html` redirects correctly — likely to `landing.html` or a slug-based artist page — not to `able-v7.html` which is superseded | ALL | 5 | 1 | M | 6 |
| 84 | Analytics baseline: before launch, document the baseline metrics in `docs/STATUS.md` — what `page_viewed` count, `fan_signed_up` count, and `cta_tapped` count mean at 0 — this provides a comparison point post-launch | ALL | 3 | 1 | L | 6 |
| 85 | Growth channel activation: verify the Made with ABLE referral slug is working — a fan who visits `able-v8.html` from the "Made with ABLE" footer should have `source: 'made-with-able'` in their attribution | V8 | 4 | 2 | M | 6 |
| 86 | Affiliate link tracking: if an affiliate program exists (1 month free per conversion), verify the affiliate tracking parameter is captured — `?ref=affiliate-[code]` stored in fan attribution and artist sign-up | ALL | 3 | 2 | M | 6 |
| 87 | Competitor differentiation check: verify the three unique differentiators are clearly visible on landing.html — (1) campaign-aware profile, (2) direct fan ownership 0% cut, (3) artist identity at depth — any visitor comparing ABLE to Linktree should understand these within 10 seconds | LND | 5 | 2 | L | 6 |
| 88 | Support email: verify `support@ablemusic.co` or equivalent email address is set up and listed in `privacy.html` and `terms.html` — artists need a contact email before signing up | ALL | 4 | 2 | M | 6 |
| 89 | Social links: verify the ABLE social media links in landing.html footer point to real, active accounts (not placeholder `#` hrefs) — broken social links signal an unmaintained product | LND | 3 | 1 | L | 6 |
| 90 | Waitlist 500 target: the marketing strategy has a 500-person waitlist target before Product Hunt — document the current waitlist count in `docs/STATUS.md` and confirm the waitlist mechanism is working on `landing.html` | LND | 4 | 2 | M | 6 |
| 91 | Exit criteria for launch: define the explicit criteria for "ready to launch" in `docs/STATUS.md` — suggested: (1) zero P0 bugs, (2) Phase 9 legal items complete, (3) Resend domain verified, (4) 5 manual test artists onboarded successfully, (5) UptimeRobot running | ALL | 5 | 2 | L | 6 |
| 92 | Backup and recovery: document the disaster recovery plan in `docs/systems/data-architecture/SPEC.md` — if localStorage data is lost (browser wipe, device change), what is the artist's data recovery path in V1 (answer: none, which is why Supabase Phase 2 is important) | ALL | 3 | 2 | L | 6 |
| 93 | Version string: add a `ABLE_VERSION` constant (e.g. `'1.0.0'`) to `shared/able.js` — log it to the console in dev mode — this helps identify which version is running in production when debugging | ALL | 2 | 1 | L | 6 |
| 94 | Feature flags: document which features are "on" for V1 launch and which are "post-launch" — particularly: PostHog (on), Ticketmaster import (on), fan.html real data (off — Supabase needed), freelancer profiles (off) | ALL | 3 | 2 | L | 6 |
| 95 | Demo mode: consider adding a `?demo=true` mode to `able-v8.html` that loads sample data without localStorage — allows sharing a "try ABLE" link that doesn't require a full wizard setup | V8 | 4 | 3 | M | 6 |
| 96 | Press kit: create a simple press kit page or downloadable PDF with: product description (the validated one-liner), screenshots, pricing, contact — needed for any Product Hunt or press outreach | ALL | 3 | 3 | L | 6 |
| 97 | `docs/DECISION-LOG.md` pre-launch entry: add a "Pre-launch sign-off" entry documenting that all 9 audit phases were completed, the date, and the person who signed off — legal and strategic provenance | ALL | 3 | 1 | L | 6 |
| 98 | First 10 artists onboarding rehearsal: before public launch, personally onboard 5 artist friends through the full wizard → profile → admin flow and collect qualitative feedback on the 3 highest-friction moments — iterate before opening to strangers | ALL | 5 | 4 | L | 6 |
| 99 | Launch communication: draft the launch DM/email to the first 10 target producers — keep it under 100 words, include the product URL, and make it feel personal, not a newsletter blast | ALL | 4 | 2 | L | 6 |
| 100 | Phase 10 closure — launch decision: review all 900 audit items, confirm all Wave 1 items in all phases are complete, confirm all Impact 5 items in all phases are complete, and make the final launch call: is ABLE ready for the first real artist? Document the decision in `docs/STATUS.md`. | ALL | 5 | 2 | L | 6 |

---

*End of PRE-LAUNCH-1000.md — 900 improvement points across 9 phases.*
*Work in phase order: 1 → 3 → 4 → 5 → 6 → 7 → 8 → 9 → 10.*
*Within each phase: Wave 1 first, then Wave 2, etc. Within each wave: Impact 5 before Impact 4.*
