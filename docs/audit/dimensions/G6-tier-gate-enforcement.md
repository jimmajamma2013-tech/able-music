# Dimension G6 — Tier Gate Enforcement
**Category:** Core Product Logic & User Flows
**Phase:** 1 (HIGHEST PRIORITY — do before visual)

Every feature behind a tier gate must actually gate. The gate system is implemented in admin.html via `getCurrentTier()` (reads `able_tier` from localStorage), `tierAtLeast()` (compares against `TIER_ORDER`), `checkTierGate()` (validates named feature), and `applyGates()` (scans DOM for `.tier-gate` and `.gold-lock` elements). Critical issue: `able_tier` is readable and writable by anyone with DevTools access — gates are client-side only until Supabase RLS is added.

## 100 Improvement Points

| # | Improvement | Page | Impact | Effort | Risk | Wave |
|---|---|---|---|---|---|---|
| 1 | `getCurrentTier()` (admin.html:7039) reads `localStorage.getItem('able_tier') || 'free'`. Any user can open DevTools and run `localStorage.setItem('able_tier', 'label')` to unlock all features. This is documented: "V1 note: reads from localStorage (can be spoofed). V2 will use Supabase RLS." (admin.html:7066). Ensure this comment exists and add a visible note in admin for the developer: "Tier enforcement is client-side until Supabase auth is wired." | admin.html | High | Low | None | 1 |
| 2 | `TIER_ORDER = { free: 0, artist: 1, 'artist-pro': 2, label: 3 }` (admin.html:7037). Confirm all 4 tier values are consistent with the values stored by the payment flow when it is added. If Stripe uses `'artist_pro'` (underscore) vs `'artist-pro'` (hyphen), the tier gate would fail for all pro users. Establish the canonical tier string format now. | admin.html | High | Low | None | 1 |
| 3 | `applyGates()` (admin.html:7082) is called at page load. It removes `.tier-gate` and `.gold-lock` classes for elements the current tier can access. But it does NOT re-run when the tier changes mid-session (e.g., artist upgrades in another tab). Add a `storage` event listener that re-calls `applyGates()` when `able_tier` changes in localStorage. | admin.html | High | Med | None | 2 |
| 4 | The gold lock overlay on advanced analytics (admin.html:3386): `.gold-lock[data-tier="artist-pro"]`. For a free user, `applyGates()` leaves the overlay in place. For an artist-pro user, it removes the overlay and enables the content. Confirm the gold-blur filter is correctly removed when unlocked: `blur.style.filter = ''` (admin.html:7111). | admin.html | High | Low | None | 1 |
| 5 | The gold lock overlay on email broadcasts (admin.html:3496): `.gold-lock[data-tier="artist-pro"]`. Confirm this gate is correctly applied and that the content behind the blur is truly inaccessible — keyboard focus, tab order, and screen readers should not reach it. Confirm `aria-hidden="true"` is set on the blurred content. | admin.html | High | Med | None | 1 |
| 6 | The `.tier-gate-content` element has `aria-hidden` and `tabindex="-1"` set in CSS or by `applyGates()`. Confirm that for gated content, `aria-hidden="true"` prevents screen readers from reading the blurred content, and `tabindex="-1"` prevents keyboard focus from reaching interactive elements behind the overlay. | admin.html | High | Low | None | 1 |
| 7 | `applyGates()` removes the overlay and re-enables content when the artist has access. The `querySelectorAll('[disabled]')` on blur elements (admin.html:7113) re-enables disabled form inputs. Confirm that ALL interactive elements within the blurred content (buttons, inputs, links) are disabled while gated and re-enabled on unlock. | admin.html | High | Med | None | 1 |
| 8 | Snap card gate: free tier shows 1 snap card; remaining cards are blurred (able-v8.html:9747–9763). This is implemented directly in the render function, not via `applyGates()`. Confirm this matches the `GATE_COPY['snap-cards']` requirement of Artist tier. The v8 snap card gate uses `FREE_SNAP_LIMIT = 1` (v8:9750) — confirm this constant matches the product spec (1 snap card free, unlimited on Artist). | able-v8.html | High | Low | None | 1 |
| 9 | Free tier fan cap: 100 fans (admin.html:7051 GATE_COPY). The cap is enforced at sign-up time in v8 (`FREE_FAN_CAP = 100`, v8:8600). But in admin, the fan list is not gated — free tier artists can see all 100 fans. Confirm that accessing and starring fans is free-tier. | admin.html | Low | None | None | 3 |
| 10 | Campaign modes gate: `GATE_COPY['campaign-modes']` (admin.html:7048) requires Artist tier. But the Campaign HQ section in admin appears for ALL tiers. The arc nodes (profile/pre-release/live/after) are clickable for all. Only the pre-release and gig mode nudges (§9.1 moments 2 and 3) hint at Artist tier requirements. Confirm whether Campaign HQ itself is gated or just the email features. | admin.html | High | Med | High | 1 |
| 11 | If Campaign HQ campaign modes (pre-release, live, gig) are gated at Artist tier, then free tier artists should not be able to set a release date or activate gig mode. Confirm whether free tier can use auto-switching or if auto-switching requires Artist tier. | admin.html | High | Med | High | 1 |
| 12 | Per CLAUDE.md: "Free: Basic profile, 1 snap card, 100 fan sign-ups, basic stats." Campaign modes are listed under Artist tier ("campaign modes"). This means free tier artists CANNOT use pre-release/live/gig modes. The Campaign HQ arc nodes must be gated for free users. Currently they are not. | admin.html | High | Med | High | 1 |
| 13 | If campaign modes are gated at Artist, the auto-switch logic (`computeAutoState()`) must also be gated. A free artist setting a release date should NOT see their page switch to pre-release mode automatically. This is a significant product decision that must be confirmed and documented. | Both | High | High | High | 1 |
| 14 | The email broadcasts gate: `.gold-lock[data-tier="artist-pro"]` on the broadcasts section (admin.html:3496). Confirm there is no way to trigger a broadcast via admin for a free or artist-tier user even if they inspect the DOM and remove the overlay. The send-broadcast button must also be gated in JS, not just CSS-hidden. | admin.html | High | Med | None | 1 |
| 15 | The advanced analytics gate: `.gold-lock[data-tier="artist-pro"]` on the analytics section (admin.html:3386). Confirm the `renderAdvancedAnalytics()` function is never called for non-pro users even if the DOM overlay is removed. Gate the function call itself: `if (!tierAtLeast('artist-pro')) return;`. | admin.html | High | Med | None | 1 |
| 16 | Export fans gate: `GATE_COPY['export-fans']` requires Artist Pro. The export function is called from admin. Confirm that `exportFansCSV()` (or equivalent) includes a `checkTierGate('export-fans')` check at the start. | admin.html | High | Low | None | 1 |
| 17 | Fan CRM gate: `GATE_COPY['fan-crm']` requires Artist Pro. The CRM features (star, level, filter by source) — confirm which of these are Pro-only. The basic fan list (email + timestamp) should be visible to all tiers; CRM features (star, level) may be Artist+. | admin.html | High | Med | High | 1 |
| 18 | Connections gate: `GATE_COPY['connections']` requires Artist. The connections panel in admin must not be accessible to free users. Confirm a `.tier-gate[data-tier="artist"]` wrapper exists around the connections section. | admin.html | High | Low | None | 1 |
| 19 | Support packs gate: `GATE_COPY['support-packs']` requires Artist Pro. Confirm the support packs section in admin and v8 is gated correctly. A free user inspecting the DOM should not be able to add support packs. | Both | High | Med | None | 1 |
| 20 | Multiple pages gate: `GATE_COPY['multiple-pages']` requires Label. This feature does not yet exist in the UI. Document it as a future gate so that when it is built, the gate is implemented from day one. | admin.html | Low | None | None | 3 |
| 21 | Team access gate: `GATE_COPY['team-access']` requires Label. Same as #20 — future feature, document gate requirement. | admin.html | Low | None | None | 3 |
| 22 | `checkTierGate()` (admin.html:7068) returns `true` (access granted) for unknown feature names: `console.warn(...); return true` (admin.html:7071). This fails open — an unregistered feature is accessible to everyone. This is intentional for development but dangerous in production. Consider failing closed (`return false`) for production builds. | admin.html | High | Med | None | 1 |
| 23 | `GATE_COPY` object (admin.html:7047) defines human-readable headlines for each gate. These copy strings must comply with the ABLE copy philosophy: no "Upgrade" without specifics, name the value. Review each headline: 'snap-cards' → "Add as many Updates as you want" ✓. 'campaign-modes' → "Set a release date, run a live window, or go into gig mode" ✓. Audit all 12 for copy compliance. | admin.html | Med | Low | None | 2 |
| 24 | The upgrade prompt at 80% of fan limit (CLAUDE.md spec) is at `count >= 80` in `updateUpgradeBar()` (admin.html:7133). But fan cap nudge in admin is at `>= 95` (admin.html:3843). And the fan-limit-banner in v8 is at `>= 95`. Three different thresholds for different UI surfaces. Determine the canonical threshold for each surface. | Both | Med | Low | None | 2 |
| 25 | The upgrade bar (`#upgradeBarText`, admin.html:7128–7134) shows a message when `count > 0` and near the cap. Confirm the upgrade bar is only shown for free tier: `if (getTier() !== 'free') return;`. An Artist-tier user at 2000 fans should not see an upgrade bar. | admin.html | High | Low | None | 1 |
| 26 | `applyTierPill()` (admin.html:7118) updates tier badge elements (`.tier-badge`). These are display-only. Confirm that the tier badge never drives access control — it is decorative and reads from `getCurrentTier()`. | admin.html | Low | None | None | 3 |
| 27 | The gold lock overlay CSS (admin.html:970+): `.gold-lock-overlay` is absolutely positioned over `.gold-blur` content. Confirm that `.gold-blur` has `position: relative` parent or that the overlay covers the full content area. If the blurred content overflows its container, parts of it may be visible outside the overlay. | admin.html | Med | Low | None | 2 |
| 28 | `.gold-blur` applies `filter: blur(3px)` and `pointer-events: none; user-select: none` (admin.html:997). Confirm that `pointer-events: none` also disables form submission from elements behind the blur. HTML form submission bypasses `pointer-events` — the form `submit` event can still be triggered via keyboard. Use `disabled` attribute on inputs and `tabindex="-1"` on buttons. | admin.html | High | Med | None | 1 |
| 29 | The `.gold-lock-overlay .glo-sub` copy (admin.html:988) describes the value of the locked feature. Confirm this text is not just "Upgrade" — it must name the specific value. Audit all `glo-sub` instances against the `GATE_COPY` descriptions. | admin.html | Med | Low | None | 2 |
| 30 | The `.gold-lock-overlay .glo-btn` (admin.html:989) is the upgrade CTA within the overlay. Confirm it opens the correct upgrade page or modal for the feature's required tier. "See Artist plans" and "See Artist Pro plans" are valid only if those pages exist. | admin.html | Med | Low | None | 2 |
| 31 | `applyGates()` uses `document.querySelectorAll('.tier-gate[data-tier]')` (admin.html:7084). This runs at page load. If new `.tier-gate` elements are added to the DOM after load (e.g., via JS rendering), `applyGates()` will not process them. Add a MutationObserver or re-call `applyGates()` after dynamic renders. | admin.html | High | Med | None | 2 |
| 32 | The nudge use limit (admin.html:8038): free tier gets 3 "nudges" (identity nudge buttons). A free user has `nudgeUsesLeft = Math.max(0, 3 - used)`. The `able_nudge_uses` key tracks this. Confirm this limit is correctly applied and the counter increments per use. | admin.html | Med | Low | None | 2 |
| 33 | The nudge use limit (point #32) is stored in `able_nudge_uses` (admin.html:8081). This key is not in the canonical schema. Add it. | admin.html | Low | None | None | 1 |
| 34 | `isArtistPlus = tierAtLeast('artist')` (admin.html:3839). This is used to show/hide the fan cap nudge. Confirm `tierAtLeast('artist')` returns `true` for both 'artist', 'artist-pro', and 'label' tiers. It does via `TIER_ORDER` comparison. Correct. | admin.html | Low | None | None | 3 |
| 35 | The admin lifecycle section (page-home data-stage, admin.html:7235) computes `stage` based on fan count, gig status, and first-ever visit. The `stage` affects section prominence but is NOT a tier gate. Confirm `stage` is separate from `tier` throughout the codebase. | admin.html | Low | None | None | 3 |
| 36 | When a free user reaches 100 fans and the sign-up form shows "Sign-ups are paused" (v8:8608), the fan cannot know this is a tier issue. The artist-facing message in admin should be more actionable: "You've reached 100 fans on the free plan. Your next 1,900 are ready when you are." This is in `GATE_COPY['fan-list']` — confirm the admin actually shows this message when the cap is hit. | admin.html | High | Low | None | 1 |
| 37 | The free tier snap card gate in v8 (v8:9747–9763): snap cards beyond `FREE_SNAP_LIMIT = 1` are blurred with a lock overlay. Confirm the lock overlay has a `.gold-lock-overlay` pattern consistent with admin's gate overlays. | able-v8.html | Med | Low | None | 2 |
| 38 | `snap-card--locked` CSS class (v8:3493+) — confirm the blur and lock icon are applied. The locked snap card preview shows through the blur to give the artist a taste of the feature (value teaser). Confirm the blurred content is real snap card content, not dummy content. | able-v8.html | Med | Low | None | 2 |
| 39 | For v8 snap card gate, the overlay copy says "Upgrade to Artist to add more updates" or similar. Confirm this copy matches ABLE voice (no "Upgrade" without context, name the value). | able-v8.html | Med | Low | None | 2 |
| 40 | Confirm that the snap card gate in v8 applies only to the ARTIST'S OWN view (owner context), not to fan views. Fans should see all published snap cards — no tier-gate overlay for fans. The gate is about the ARTIST's ability to create more, not about fans seeing them. | able-v8.html | High | Med | None | 1 |
| 41 | When an artist upgrades from free to Artist tier, the `able_tier` key is updated. `applyGates()` should be re-called to remove all gates. This requires either a page reload or a reactive gate check. Confirm the upgrade flow triggers a re-render. | admin.html | High | Med | None | 2 |
| 42 | `checkTierGate('fan-list')` returns `false` for a free user with 100 fans but the GATE_COPY headline says "You've reached 100 fans." Confirm this gate is actually triggered by fan count, not just tier. `checkTierGate` only checks tier level — the fan count check is separate in `updateUpgradeBar()`. These two systems must coordinate. | admin.html | High | Med | None | 1 |
| 43 | The upgrade bar text changes based on remaining fan capacity (admin.html:7132–7134). Confirm the text is human and direct: "Your list is at 47. Your next 53 fans are ready when you are." not just "47/100 fans." | admin.html | Med | Low | None | 2 |
| 44 | `GATE_COPY` has 12 features defined. Each has `headline`, `cta`, and `tier`. Confirm all 12 are actually used somewhere in the admin UI — no orphaned gate definitions. | admin.html | Low | Low | None | 3 |
| 45 | The gate copy CTA buttons (`glo-btn`) all have specific copy ("See Artist plans", "See Artist Pro plans", "See Label plans"). Confirm these CTAs link to an actual upgrade page (`/pricing`, `/plans`, etc.) and not `#` placeholders. | admin.html | High | Low | None | 1 |
| 46 | When the artist dismisses the upgrade bar (`#upgradeBarText`), it's stored as `able_upgrade_bar_dismissed = '1'` (admin.html:7336, 7352). Confirm this dismissal persists across sessions. Confirm it resets when the artist reaches 100 fans (cap hit) to re-show the message. | admin.html | Med | Low | None | 2 |
| 47 | The broadcast gate (`.gold-lock[data-tier="artist-pro"]` at admin.html:3496) has a personalised version that shows the artist's real fan count. Confirm this personalisation is applied after `applyGates()` determines the tier. The personalisation comment is at admin.html:6444: "§9.1 moment 5 — broadcast lock overlay: personalise with real fan count." | admin.html | Med | Low | None | 2 |
| 48 | The fan near-full banner in v8 (`#fan-limit-banner`, v8:5518) shows only for free tier owners. Confirm `isOwner && isFree` check in v8 before showing the banner. It is at v8:10792. Correct. | able-v8.html | Low | None | None | 3 |
| 49 | `isFree(profile)` in v8 (v8:6415–6417) reads `profile.tier || 'free'`. This reads from `able_v3_profile.tier` not from `able_tier` (the separate key). Confirm `able_v3_profile.tier` is always in sync with `able_tier`. They may diverge. | Both | High | Med | High | 1 |
| 50 | Two tier sources: `able_tier` (standalone key, admin.html:7039) and `able_v3_profile.tier` (field in profile object, v8:8599). When the artist upgrades, both must be updated. If only one is updated, the two gates disagree. Establish `able_tier` as the single source of truth and read it everywhere. | Both | High | Med | High | 1 |
| 51 | In v8, `_profile.tier || 'free'` (v8:8599) reads tier from the profile object. The profile object is `safeLS('able_v3_profile', {})`. If `able_v3_profile.tier` is missing but `able_tier` is 'artist', the fan cap check would incorrectly apply the free tier cap. Fix: read `able_tier` from localStorage directly in the fan cap check. | able-v8.html | High | Low | High | 1 |
| 52 | The `getIsFree(profile)` function (v8:6415): `return !profile || !profile.tier || profile.tier === 'free'`. This returns `true` (is free) when `profile.tier` is absent. If an Artist-tier artist has `profile.tier` not set, they are incorrectly identified as free. Write `able_tier` to `able_v3_profile.tier` in `applyTierPill()` to keep them in sync. | Both | High | Med | High | 1 |
| 53 | Confirm that the snap card gate in admin (the "1 snap card free" limit) is also enforced on the SAVE side — not just the display side. If a free artist uses DevTools to bypass the gate UI and create a second snap card, does the JS save function check the tier? | admin.html | High | Med | None | 1 |
| 54 | Confirm that adding a snap card in admin calls `checkTierGate('snap-cards')` before executing the save. If the gate check is absent from the save function, the UI gate is the only protection. | admin.html | High | Low | None | 1 |
| 55 | The `nudgeUsesLeft` counter (admin.html:7993) enforces a 3-nudge limit for free tier on identity nudges. Confirm this counter is reset when the artist upgrades to Artist tier (`nudgeUsesLeft = null` for Artist+). | admin.html | Med | Low | None | 2 |
| 56 | `applyGates()` runs once on page load (within the main init sequence). Confirm it runs AFTER `applyTierPill()` and AFTER `getCurrentTier()` has read the correct tier value from localStorage. If migrations run and update the tier, `applyGates()` must run after migration. | admin.html | High | Low | None | 1 |
| 57 | When a fan cap is reached and the sign-up form shows the paused message (v8:8608), a free artist's fans who try to sign up see a cold, unexplained pause. The paused message should vary: "Join the list is paused for now." This protects the artist's brand. Confirm the copy. | able-v8.html | Med | Low | None | 2 |
| 58 | The `able_dismissed_nudges` array (admin.html:4489) contains IDs of dismissed nudges. Nudges related to tier gates (pre-release trial nudge, gig mode trial nudge) should be in this array when dismissed. Confirm they use the `able_dismissed_nudges` key, not `sessionStorage`. | admin.html | Med | Low | None | 2 |
| 59 | The pre-release trial nudge uses `sessionStorage` (key: `_preReleaseNudgeDismissed`), not `able_dismissed_nudges`. The gig mode trial nudge also uses `sessionStorage` (key: `_gigModeNudgeDismissed`). These are session-only dismissals. If they should persist across sessions, change to `able_dismissed_nudges`. Decide and document. | admin.html | Low | Low | None | 2 |
| 60 | For Supabase V2: tier will be stored server-side (in `profiles.tier` or a separate `subscriptions` table). The client should read tier from the JWT or a secure API call, not from localStorage. Plan this migration path. Until then, document clearly that tier gates are spoofable. | admin.html | High | High | None | 3 |
| 61 | When the artist's Stripe subscription lapses (future), the tier should downgrade. With localStorage, there is no mechanism for this. Plan: on admin load, if Supabase is available, fetch the tier from Supabase and update `able_tier`. The fetch must happen before `applyGates()`. | admin.html | High | High | None | 3 |
| 62 | Confirm the Label tier features (`multiple-pages`, `team-access`, `aggregate-analytics`, `api-access`) are all absent from the current admin UI — no DOM elements with `data-tier="label"` yet. If they are present (even as stubs), they must be gated correctly. | admin.html | Med | Low | None | 2 |
| 63 | When the artist is on Artist Pro tier and all gold lock overlays are removed, confirm there is no residual `aria-hidden` or `tabindex="-1"` on previously locked content. `applyGates()` removes these attributes (admin.html:7092–7096). Verify the removal is complete. | admin.html | Med | Low | None | 2 |
| 64 | The `gold-lock-overlay .glo-btn:hover` (admin.html:996) has `transform: translateY(-2px)` and `box-shadow`. Confirm this hover state does not cause layout shift that bleeds outside the overlay boundary. | admin.html | Low | Low | None | 3 |
| 65 | The `.tier-gate` pattern is the newer pattern (admin.html:7084). The `.gold-lock` pattern is legacy (admin.html:7101). Confirm there is a plan to migrate all `.gold-lock` to `.tier-gate` for consistency. Currently both exist. | admin.html | Low | Med | None | 3 |
| 66 | `applyGates()` is called in the main init sequence. Confirm that section renders (like `renderFansSection()`, `renderAnalytics()`) that run AFTER `applyGates()` do not re-add gated elements. Dynamic renders must respect tier gates. | admin.html | High | Med | None | 1 |
| 67 | The export fans gate (`GATE_COPY['export-fans']` = artist-pro). But the export button exists in admin. Confirm it is either: (a) hidden for non-pro users, (b) behind a `.tier-gate` wrapper, or (c) checked in the click handler. One of these must be implemented. | admin.html | High | Low | None | 1 |
| 68 | Confirm that free tier artists can see their own fan list (email + timestamp) but cannot access pro CRM features (star, level, filter). The `loadFansSection()` function should check tier and show/hide CRM columns accordingly. | admin.html | High | Med | None | 1 |
| 69 | The free tier shows "basic stats" only (per CLAUDE.md). Confirm which stats are basic vs advanced. Basic: views, clicks, fan count. Advanced: source breakdown, campaign breakdown, geographic data, device types. Confirm the advanced analytics section is behind the artist-pro gate. | admin.html | High | Med | High | 1 |
| 70 | The source breakdown chart in admin (`#campaignBreakdown`, admin.html:3482) — is this basic or advanced stats? If it's visible to all tiers, that may be inconsistent with the pro gate on advanced analytics. Determine the correct tier for each analytics component. | admin.html | Med | Med | None | 1 |
| 71 | Write a tier gate test plan: for each of the 12 features in `GATE_COPY`, verify: (1) free user cannot access it, (2) required-tier user can access it, (3) inspection of DOM after gate removal shows no residual locked state, (4) upgrade mid-session removes the gate. Document in `docs/audit/tests/G6-tier-gate-tests.md`. | admin.html | High | Med | None | 1 |
| 72 | Confirm that `able_tier` is written by the correct flow (onboarding, payment, admin settings) and never set to an invalid value. Invalid values: misspelled strings, null, 0. | admin.html | High | Med | None | 1 |
| 73 | Default tier on first load: `localStorage.getItem('able_tier') || 'free'`. A new artist with no `able_tier` key defaults to free. Confirm this is correct and that the onboarding flow does not set `able_tier` to a non-free value unless the artist has paid. | Both | High | Low | None | 1 |
| 74 | The `admin_ever_visited` key (admin.html:7012) is unrelated to tier but is used in the lifecycle stage computation. Confirm it is NOT used in any tier gate logic. | admin.html | Low | None | None | 3 |
| 75 | `able_upgrade_bar_dismissed` key (admin.html:7335) is not in the canonical schema. Add it. | admin.html | Low | None | None | 1 |
| 76 | The upgrade bar (`updateUpgradeBar()`, admin.html:7127) only shows if `count > 0`. A new free user with 0 fans sees no upgrade bar. This is correct — no pressure on day zero. Confirm. | admin.html | Low | None | None | 3 |
| 77 | The fan cap message in `GATE_COPY['fan-list']` (admin.html:7051): "You've reached 100 fans. Your next 1,900 are one step away. Artist." The copy ending with "Artist." as a period is a unique convention in this codebase — it names the tier that unlocks the feature as a standalone sentence. Ensure ALL 12 gate copy strings follow this convention. | admin.html | Med | Low | None | 2 |
| 78 | `GATE_COPY['connections']` headline: "Link to the people you've made music with. Artist." (admin.html:7050). Confirm this is the actual text shown in the connections section tier gate. | admin.html | Low | Low | None | 3 |
| 79 | The tier pill labels include emoji: `'✦ Free'`, `'✦ Artist'` (admin.html:7121). CLAUDE.md rule: no emoji in dashboard copy. Replace `✦` with a plain text badge or CSS-styled marker. | admin.html | Low | Low | None | 2 |
| 80 | When the artist's profile data has `tier: 'artist'` in `able_v3_profile` (set by an old flow), but `able_tier` key is absent, `getCurrentTier()` returns 'free' (reads `able_tier`, not profile). This discrepancy must be caught and resolved in migration. Add: if `able_tier` is absent but `able_v3_profile.tier` is set, write `able_v3_profile.tier` to `able_tier`. | admin.html | High | Med | None | 1 |
| 81 | The `able_nudge_uses` key (admin.html:8038) — confirm the 3 free-tier nudge uses applies only to identity nudges (the personality knobs: Darker, More space, etc.), not to Campaign HQ nudges or onboarding nudges. | admin.html | Med | Low | None | 2 |
| 82 | Confirm that the broadcast section gold lock overlay copy is personalised to the artist's real fan count as specified in comment at admin.html:6444. If the artist has 47 fans, the overlay should say "Send a message to your 47 fans directly." | admin.html | Med | Med | None | 2 |
| 83 | When `applyGates()` removes a `.gold-lock` class from the analytics section, confirm the analytics render functions are then called to populate the previously-locked section with real data. The content behind the lock was blurred — data was still rendered. Unlocking just removes the blur and overlay. Confirm. | admin.html | Med | Low | None | 2 |
| 84 | For the Label tier, `tierAtLeast('label')` returns true only if `getCurrentTier() === 'label'`. Confirm `TIER_ORDER['label'] = 3` is the highest. If a new tier is added later (e.g., 'enterprise'), `TIER_ORDER` must be updated. | admin.html | Low | Low | None | 3 |
| 85 | `checkTierGate()` logs a `console.warn` for unknown features (admin.html:7071). In production, `console.warn` should be suppressed or redirected to an error tracking service. Add a production guard. | admin.html | Low | Low | None | 3 |
| 86 | The Campaign HQ section — if campaign modes are indeed Artist-tier only (per point #12), a free artist should see a tier gate overlay on Campaign HQ with the `GATE_COPY['campaign-modes']` headline. Currently there is no `.tier-gate` wrapper around Campaign HQ. This is a significant missing gate. | admin.html | High | Med | High | 1 |
| 87 | Gig mode activation from the Shows page (`#gigToggleCard`, admin.html:3095) — confirm this is also tier-gated at Artist. If Campaign HQ gig toggle is gated but the Shows page gig toggle is not, the artist can bypass the Campaign HQ gate via the Shows page. | admin.html | High | Med | None | 1 |
| 88 | The "Make a free page" copy in landing.html — confirm the free tier value proposition matches what the product actually delivers (basic profile, 1 snap card, 100 fans, basic stats). No false promises. | landing.html | Med | Low | None | 2 |
| 89 | When the free tier limit of 100 fans is about to be reached (e.g., 95 fans), the nudge fires. But there is no interim nudge at 50 fans (halfway to the limit). Add a softer nudge at 50 fans that mentions the Artist plan without urgency. | admin.html | Low | Med | None | 3 |
| 90 | The "artist.html" tier upgrade flow — does an upgrade page/modal exist? If not, all upgrade CTAs (`glo-btn`, upgrade bar CTA) go to `#` or a placeholder. This is a launch blocker. | admin.html | High | High | None | 1 |
| 91 | Confirm that all `.glo-btn` upgrade CTAs across admin have consistent `href` values — they all should point to the same pricing page. If they each point to different URLs, the upgrade experience is fragmented. | admin.html | High | Low | None | 1 |
| 92 | When the artist uses the "Copy link" feature to share their profile URL, confirm no tier-specific content appears in the shared URL. The profile URL is public regardless of tier — tier only gates admin features. | able-v8.html | Low | None | None | 3 |
| 93 | Confirm that v8 snap card limit is checked per the `able_tier` key (not `able_v3_profile.tier`). `getIsFree(profile)` in v8 reads from `profile.tier` which may be stale. Use `localStorage.getItem('able_tier') || 'free'` in v8 instead. | able-v8.html | High | Low | High | 1 |
| 94 | Label tier features (`aggregate-analytics`, `api-access`, `multiple-pages`) — confirm none of these leak through the gate for artist-pro users. `tierAtLeast('label')` returns false for `artist-pro` (order 2) since `TIER_ORDER['label'] = 3`. Correct. | admin.html | Low | None | None | 3 |
| 95 | Confirm that fan-facing profile (v8) has NO tier gate overlays visible to fans. Tier gates are admin-only. Fans should never see "Upgrade to Artist" on a fan profile page. | able-v8.html | High | Low | None | 1 |
| 96 | The only tier-gate visible to fans on v8 is the fan-limit-banner (`#fan-limit-banner`, v8:5518) which is in owner context only (shown only when `isOwner && isFree`). Confirm `isOwner` detection is correct: reads `able_artist_id` vs `profile.artistId`. | able-v8.html | High | Low | None | 1 |
| 97 | When the artist views their own profile (owner context) on v8, the snap card lock overlay is visible to them. Fans seeing the artist's profile should NOT see the lock overlay — only the published, unlocked snap cards. Confirm `isOwner` gate correctly wraps the snap card lock. | able-v8.html | High | Med | None | 1 |
| 98 | Confirm that `isOwner` detection in v8 is spoofable in the same way as tier gates (DevTools can set `able_artist_id`). Document that v1 trust model is client-side and Supabase auth + RLS is required for v2. | able-v8.html | Low | None | None | 3 |
| 99 | Tier gate system documentation: create `docs/systems/tier-gates/SPEC.md` (or confirm it already exists) with the full table of gated features, their required tiers, their copy strings, and where each gate is implemented. | Both | High | Med | None | 1 |
| 100 | Write a tier gate audit checklist in `docs/audit/tests/G6-tier-gate-tests.md`: for each of the 12 GATE_COPY features, record: implemented (Y/N), implementation location, gate type (DOM/JS/CSS), spoofable (Y/N), passes manual test (Y/N). | admin.html | High | Low | None | 1 |

## Wave Summary

| Wave | Points | Focus |
|---|---|---|
| Wave 1 (critical bugs) | 1, 2, 4, 5, 6, 7, 8, 10, 11, 12, 13, 14, 15, 16, 22, 25, 28, 36, 40, 42, 45, 49, 50, 51, 52, 53, 54, 56, 66, 67, 68, 69, 70, 71, 72, 73, 75, 80, 86, 87, 90, 91, 93, 95, 96, 97, 99, 100 | Campaign modes gate gap, two-tier-source bug, DOM-only gates without JS check |
| Wave 2 (improvements) | 3, 17, 18, 19, 23, 24, 27, 29, 30, 31, 32, 33, 37, 38, 39, 41, 43, 46, 47, 55, 57, 58, 59, 62, 63, 67, 68, 77, 79, 81, 82, 83, 88, 89 | Gate copy audit, dynamic render re-gate, upgrade flow, analytics tier split |
| Wave 3 (docs / future) | 9, 20, 21, 26, 34, 35, 44, 60, 61, 64, 65, 74, 76, 78, 84, 85, 89, 92, 94, 98 | Documentation, Supabase migration plan, Label tier future features |
