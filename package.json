{
  "name": "able-music",
  "version": "1.0.0",
  "description": "ABLE — Artist Before Label. Vanilla HTML/CSS/JS, no bundler.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:copy": "playwright test tests/copy.spec.js",
    "test:a11y": "playwright test tests/a11y.spec.js",
    "test:interactions": "playwright test tests/interactions.spec.js",
    "serve": "serve . -p 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "serve": "^14.2.6"
  }
}
